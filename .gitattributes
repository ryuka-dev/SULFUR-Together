# Normalize line endings: LF in the repo, native on checkout.
* text=auto

# C# / project files
*.cs       text diff=csharp
*.csproj   text
*.slnx     text
*.props    text
*.md       text
*.gitignore text
*.gitattributes text

# Binary assets — never normalized.
*.png      binary
*.dll      binary
