# Versioning & Phase Labels — the governing convention

This repo uses **two** numbering systems. They are not the same thing; mixing them up is what makes labels
like "RM-2a" look opaque. This document is the single source of truth for both.

---

## 1. Plugin version (semver) — the release number

- Defined in **`src/ModInfo.cs`** (`ModInfo.Version`) and **`Thunderstore/manifest.json`**. Current: **`1.0.1`** (Version 1.0.1 — Public Beta bugfix release).
- This is the only number a *player* ever sees (Thunderstore, BepInEx load line).
- Bumped **only at a Thunderstore release milestone**, MAJOR.MINOR.PATCH:
  - **PATCH** (0.2.0 → 0.2.1): bug-fix release. During Public Beta, all players should still update to the same version when a fix touches network messages.
  - **MINOR** (0.2.0 → 0.3.0): new synced feature(s) shipped; may change the net protocol (see §3).
  - **MAJOR**: reserved for a broad rewrite / breaking redesign.
- Bumping it is a deliberate release act (commit subject `Bump to X.Y.Z`), **not** something every dev change does.

## 2. Phase labels — the internal dev increments

Day-to-day work is tracked as **phases**, not version bumps. A phase is one coherent slice of work (design →
implement → verify). Phases are what commit messages, memory notes, and `[LogTag]` lines refer to.

### Label grammar

```
Phase <major>.<minor>-<AREA><iteration><revision>      e.g.  Phase 5.4-E2 , Phase 5.4-G7b
<AREA>-<iteration><revision>                            e.g.  RM-2a , PF-0      (named areas, not under a 5.x number)
```

- **`<major>.<minor>`** — the broad track (mirrors the era of work, loosely aligned with the eventual semver
  minor). E.g. `5.4` = the boss-authority track.
- **`<AREA>`** — a short code for the sub-system. A letter when it lives under a numbered track
  (`5.4-E` = boss **E**ncounter start, `5.4-F` = boss damage, `5.4-G` = **W**itch phases), or a **named code**
  when it's a standalone area:
  - **PF** = boss **P**re-**F**ight flow (room seal / dialog / teleport / convergence)
  - **RM** = **R**oom **M**embership substrate (who is in the boss room)
- **`<iteration>`** — a number for the n-th distinct step of that area (`RM-1`, `RM-2`). Bigger = later.
- **`<revision>`** — a trailing lowercase letter for a fix/revision of that same step (`RM-2a`, `5.4-G7b`).
  `RM-2a` = Room-Membership area, step 2, revision a.

### Lifecycle status (used in memory + the registry below)

`designed` → `implemented` → `deployed` (built + copied to both profiles) → `verified` (passed a real co-op
test, with the Log number) → `committed <hash>`. Behaviour changes are **only committed after `verified`**.

## 3. Net protocol version

Each networked message carries its own `*Version` byte in its codec (see `NetBossEncounterMessages.cs`,
`NetMessageType.cs`). When a wire format changes, bump that message's version byte — independent of the plugin
semver. A plugin MINOR bump is the moment to assume clients/hosts on older protocol bytes are incompatible.

---

## 4. Active phase registry

The current line of work. Older phases (Phase 1.x–4.x) live in `DevelopmentPlan.md`; the full boss line
(5.3–5.7) is indexed in the agent memory `MEMORY.md`. Keep this table updated as the active work moves.

| Label | What it is | Status |
|-------|------------|--------|
| **PF-0** | Read-only pre-fight convergence + arena-door probes (`[BossPreFight]`/`[ArenaDoor]`) | committed |
| **PF Plan B** | Gate the Cousin fight start on the intro **dialog being dismissed** (host-authoritative); restores the SP pause-gate that co-op's no-pause mode removed | verified Log133 · committed `d3130d1` |
| **PF Plan B (late-client fix)** | Gate StartFight on the boss's real `FightStarted`, not our `_fightCommitted` (which a level-step Reset can clear), so a late intro's Cinematic lock still gets cleared — no freeze | verified Log135 · committed `1cc779c` |
| **RM-1** | Room-membership substrate: host-authoritative "who is in the boss room" set, fed by each end crossing the boss trigger (msgs 51/52, `[RoomMembership]`). **Observe-only**, changes no behaviour | verified Log134/135 · committed `1cc779c` |
| **RM-2a** | Fix the membership/fight-committed maps being wiped mid-encounter by a per-GoToLevel `Reset()`: `Reset(fullSession:false)` preserves the per-`chapter:level:seed` state; only a session reset or a genuine level change clears it | verified Log137 (no member drop; churn not re-triggered) · committed `d5c748a` |
| **RM-2b** | First consumer — scope the synced cutscene to in-room players (fix "not-in-room player pulled into the cutscene") | **shelved** (needs headless host start; edge case hasn't bitten) |
| **RT3-Cousin-arms** | Route `GoblinCousinArm` through the RT3-A boss-add pipeline (was wrongly in `_specialAdds`) so the client mirrors one host-authoritative puppet arm instead of running its own — fixes double-spawn, double damage, desync. Client mud-ball **visual** = the arm's own `CousinArm.ThrowProjectile` de-fanged + target-fixed via `CousinArmPatches` (anim-event aligned). The throw is also turned into an **all-players AoE** (one physical ball per `GameManager.Players` entry: host real damage, client de-fanged); vanilla lob arc kept (each ball lands on its own target). Config `EnableCousinArmSync`. | verified Log138 (sync) / Log144 (visual aligned) / Log145 (AoE) · committed `541cea4` |
| **F3-Reload** | Fix the client F3-ing to the level both ends are already in (reload-in-place). The client's load gate self-released on the host's STALE scene request and reloaded locally → divergent fresh instance (Log147: laggy, link breaks, save persists the split; host never even got a relay). Client (`NetClientLoadGate`): a client-led transition whose target == the client's current scene (`_clientLedReloadInPlace`) only releases on a host request received after `_clientLedEpoch` → it relays + waits instead of self-reloading. Host (`HandleClientTransitionRequest`): a same-scene relay (only ever an explicit reload — a catch-up follows the existing broadcast and never relays) now RE-LEADS the reload (`TryFollow`, new seed) instead of `already-at-target→ignore`, so both regenerate together (resets the in-progress fight, by design). Config `EnableClientReloadInPlaceRelay` (default on, needs `EnableClientTransitionRelay`). The reload-in-place release keys on the host request's **seed differing** from the client's current seed (proof the host actually re-generated) — a timestamp-only check released the client on the host's same-seed gen-input echo, leaving it one seed behind + a rapid double-reload NRE (Log149). | verified Log150 (client ignores same-seed echo → releases on host's new-seed reload → both converge each F3, 0 Error/NRE) · committed |
| **Ghost-load-freeze fix** | `RemotePlayerRegistryManager.Tick` suppresses headless ghost Players while the host `GameState` is Loading/Uninitialized — a camera-less ghost re-registered mid-load made vanilla `LevelGeneration.ShowLevelNode` NRE on `Players[i].weaponCamera`, hanging the loading screen at 17/17 (Log139/140). Config `SuppressGhostsWhileLoading`. | verified Log141–145 (no recurrence) · committed `46fdce5` |
| **PF-ArmDefer** (RT3-Cousin-arms issue 1) | Defer the Cousin intro arm to the dialog-close fight commit (vanilla timing) instead of letting co-op's no-pause behavior tree pop it *during* the dialog. Prefix-block `CousinHelper.SpawnArm` while the fight loop hasn't begun (`OnLocalIntroArmSpawn` / `_cousinFightLoopStarted`); replay it once at `CommitFightStartLocal` under the reentry guard (`TryReplayIntroArm` / `_introArmReplayed`). Host's replay flows through RT3-A + broadcasts, client's binds to it (one puppet, no double-spawn); the `fight-loop-started` gate (not `FightStarted`) blocks a late-client's trailing duplicate. Config `DeferBossIntroArm` (default on, needs `GateBossFightOnDialogClose`). Log tag `[BossArmDefer]`. | verified Log147/148 (both ends: `blocked → replayed ok=True` → intro arm BOUND seq=0, Reappear arms unaffected, 0 Error/NRE) · committed |
| **RT3-Cousin-arms client-visual-all-balls** | Client now also spawns a visual-only mud ball toward every remote player's proxy position (`NetService.ForEachRemotePlayerPosition` → `CousinArmPatches.ClientVisualThrowAt`, a re-impl of the arm's ballistic throw with `explicitDamage=0`), so a client sees the boss attacking everyone, not just itself. Damage stays host-authoritative. | verified Log146 (remoteVisual=1, 0 errors) · committed `04fd831` |
| **LD-1 / LD-1b** | Combat-room **gate** sync — peer-authoritative effect mirror of `MetalGate.Close/Open` (LD-1, msg 53) and the `GameObject.SetActive("door")` variant via PlayerTrigger (LD-1b, msg 54), keyed by world pos. `[GateSync]`/`[DoorSync]`. | verified Log153–156 · committed `181bfc7` |
| **DB-1** | **Inter-chunk door** (`DoorBlocker`) sync — peer-authoritative effect mirror of a player finishing a hold-to-open, keyed by world pos (msg 79, ProtocolVersion→5). Registry scoped to doors that start closed via `ActivateDoorBlocker(isAnClosingDoor:false)`; the mirror invokes the door's real `OnFinishedHolding`. Trap doors' slam-shut deliberately out of scope. `[ChunkDoor]`. | verified in-game (both directions + simultaneous open) · committed `a5a6d16` |
| **LD-2a** | Arena lockdown **membership + timer** (host-authoritative): who crossed each seal trigger (in-room), first cross = t0, t+5/t+10 timeline. Observe-only (decision-logging). msg 55, `[ArenaLockdown]`. | verified Log157 · committed `b6b2c52` |
| **LD-2b / LD-2c** | Force-seal **barrier** (invisible two-way `BoxCollider` at the out-of-room door) + **popup-confirm/boss-death teleport** in. Host `NetArenaCommand` (msg 56: Seal/Popup/Release/Notify), confirm key + Native UI Lib banner/toasts (soft dep, reflection). | verified Log158 (confirm path) · committed `0dbfaa5`/`336cd71`/`5213ba9` |
| **LD-2d** | **Grace period**: hold the MetalGate open ~5 s (prefix-block `Close` while a local grace window is active) so teammates can still walk in together; host `CloseDoor` (msg 56 kind 5) closes every end's gate at t0+5 s, then the barrier seals whoever's still out. Config `EnableArenaGracePeriod`. SetActive-door (Lucia) still closes at t0. | verified Log159–160 | 
| **LD-2e** | In/out is **doorway-crossing parity** (pure event), not distance/trigger-volume: `ArenaDoorwaySensor` on the seal trigger counts the local player's full traversals (refcounted + debounced so one pass = one toggle), odd = inside. Drives the t+5/t+10 self-eval; teleport-in forces parity inside. Plus a first-crosser `NotifyEntered` toast. | verified Log160–161 | 
| **RM-2b** | **Boss intro cutscene scoped to in-room** (Cousin): out-of-room ends don't play the dialog/camera-turn/Cinematic-lock (suppressed via flag + patches on `ModifyControllerLock`/`ModifyPlayerInvulnerability`/`Player.RotateCameraTowardsPosition` + `Npc.Interact` block) — the boss still **appears** on every end: an out-of-room end force-appears it headlessly (direct `Introduction()`, suppressed) the moment the intro commit arrives. A late entrant (walk-in or lockdown teleport) **catches up** the dialog by opening `Npc.Interact` directly — but ONLY for an already-appeared boss (`introPlayed`), so it never hijacks the first crosser's native flow. Catch-up gate derives "session active" from reliable state (in-room + !`_fightCommitted` + !`SafeStarted` + `appeared`), not fragile flags; session sets live in the fullSession-preserved Reset bucket (survive same-level churn). Config `GateBossDialogToInRoom`. `[BossDialogCutscene]` | **verified Log173** (both directions: out-of-room boss appears + no cutscene; mid-join/teleport catch-up; in-room normal; 0 errors) | 
| **Boss invuln invariant** | The boss stays **invulnerable until the fight commits** — co-op's no-pause let `CousinHelper.DoneAppearing` (rise-anim end) clear the boss invuln mid-dialog; a DoneAppearing postfix re-asserts it while `!_fightCommitted`, cleared at `CommitFightStartLocal`. `[BossInvuln]` | verified (no pre-commit damage seen across subsequent Cousin encounters) · committed | 
| **Symmetric NPC activation** | Enemies near a remote player wake on **both** ends (client fills the activation buffer from its proxies, no ghost registry — its enemies are host-driven puppets). `EnableMultiPlayerNpcActivation` (now both ends). | verified Log163 (Test 1) | 
| **RT3-Cousin-arms-Anim** | **Cousin arm idle/disappear animation fix.** The arm is a scripted prop (appear→idle→attack→disappear) driven by its **own behaviour tree**, not a locomotion enemy. Routing it through the RT3-A puppet pipeline (PF-ArmDefer/RT3-Cousin-arms) disabled the arm's BT, and the puppet animator-mirror only reproduces host **attack** windows (`TryPopulateHostCombatAnimatorStates` runs only in the host combat-action branch) — so the client arm's **idle + disappear** (non-combat, BT-driven) were never reproduced and the Animator looped its default **Appear** state (appear + attack looked right). Fix: keep `GoblinCousinArm` **always** special-excluded in `BossDynamicSpawnManifest.IsSpecialAdd` (never a puppet) so its BT drives the full sequence faithfully on every end. De-fang + group-AoE throw still handled by `CousinArmPatches` (gated on `EnableCousinArmSync`, client throw = 0 dmg → damage host-authoritative); double-spawn still prevented by the intro-arm defer + special host-only skip (one local arm per end). **LogOutput174: insufficient alone** — the arm had a *second* puppet path (host sends ordinary enemy-state snapshots by SpawnIndex → client late-binds → `EnsureClientEnemyPuppetMode` disables the BT, `[EnemyPuppet] Activated ... Arm`). Second fix: skip the whole client snapshot/puppet application for `GoblinCousinArm` (`IsSelfAnimatingClientBossAdd`, a `continue` beside the WitchPhase2 skip in the snapshot loop) so the BT runs locally; death still via `EnemyDeathMirror`. | verified Log175 (arm puppet activations=0, 0 NRE both ends, idle/disappear correct) · committed `7271ce6` | 

| **UI-1** | First feature of the **UI track** (see [CoopUiPlan.md](CoopUiPlan.md)). In-game co-op event toasts via SULFUR Native UI Lib's toast surface (reflection seam, soft dep → log-only without the lib): a player joins/leaves, link on/off, host toggle. Default heading "Together". `CoopToasts`, config `EnableCoopToasts`. `[CoopUi]`/`[CoopToast]`. Known follow-up (deferred): host-side join/leave keys on the socket handshake (fires at startup, re-fires on reconnect) — better tied to the explicit link event via a small client→host signal. | verified (in-game "Linked to host" toast; join/leave fire + invoke lib, log-confirmed) · committed `89ab1e4` |
| **UI-3a** | Connect-UI prerequisite: `NetService` lifecycle extracted into `CoopConnection` (start/stop/**restart** at runtime, not just `Awake`). `Start`/`Stop` already do full per-session reset, so a clean restart = Stop + fresh instance + Start; bridges re-attach (or null when off). Plugin's Awake/Update/OnDestroy route through it. No behaviour change (networking still auto-applies the configured mode at load). | verified (load-bearing under every UI-3c/d/e connect/disconnect cycle since) · committed |
| **UI-3b** | In-game **connect page** in the native Options screen (`SulfurOptionsApi`): Role (Host/Client), player name, host address, port, max players, connection key. **Save settings** persists the rows only (no connection change); **Connect** = handshake + enter room (saves, marks networking on, starts the role, and as a client links in so the player actually joins); **Disconnect** leaves + closes the socket. Editing a row alone does nothing. `Connect` is idempotent — unchanged settings don't restart the socket (so re-pressing won't kick connected peers; `CoopConnection` tracks a settings signature). Status line from `NetService.GetConnectionSummary`. Compiled behind `#if NATIVE_UI_LIB` (compile-time lib reference via `NativeUiLibDll` in LocalPaths), registered at runtime only when the lib is loaded — runtime dep stays soft. `CoopConnectPage`. | superseded by **UI-3c** |
| **UI-3c** | Connect page **reworked onto the SULFUR Native UI Lib 0.10 live-update handles** (`SulfurTextHandle` / `SulfurButtonHandle` / `SulfurListHandle`; the §8 prerequisite, verified by the lib's 0.10 comprehensive test, host Log182.5 — 18/18 PASS), to the canonical target layout in [CoopUiPlan.md](CoopUiPlan.md) §5. This slice covers the **no-unknowns body**: Role dropdown replaced by mutually-exclusive **Create game / Join game** buttons (enabled state driven live by `CoopConnection.CurrentMode` — Join blocked once a room exists, Create blocked while a client); a **live status line** (host/client text, "Connecting…", colored) and **footer status** updated in place each tick; a **live read-only player list** (`NetService.GetPlayerRows`, host-first name + slot + state — ping/kick deferred §7) via a list handle; a **join-failure reason line** surfaced from `NetConnectFeedback` (host `RejectPeer` reason / connection-failed / start-failure) instead of log-only; **local prefs** (working `EnableCoopToasts` toggle; HUD-status / other-names as `Coming soon` placeholders; rescue + confirm-enter keys shown read-only); **session settings** as deferred `Coming soon` placeholders (§7); a single **end-session button** folded into the Create/Join row, label host/client-aware — **Close room** (host) / **Leave** (client), both hard → `CoopConnection.Stop`; **About** with version + open-source-repo URL button (Ko-fi placeholder, no URL yet). The "Start co-op" section was renamed **Connection** (it manages the whole connection, not just starting); the separate soft "Close co-op world" / "Room control" section was dropped as redundant (two close buttons added no value). Live handles driven from `Plugin.Update` via `CoopConnectPage.Tick` (throttled, no full `Rebuild`; list refreshed only on roster-signature change). **No auto-start / explicit join (Log185):** networking no longer auto-starts from config at launch — `CoopConnection.Initialize` now goes to Off (was `Apply(NetConfig.GetMode())`), so opening the game never silently hosts or auto-connects; the session starts only from Create/Join. Create/Join are **gated to in-game** (disabled until a save is loaded; a hint row shows "Load a save first…"). The signal is the **active Unity scene** — `SceneManager.GetActiveScene().name == "GameScene"`: SULFUR runs all gameplay (hub + every generated level) in one persistent `GameScene` loaded by `LoadGameScene`, while the title screen is its own `MainMenu` scene. This is the only stable signal — `loadingMode` is useless (a save loaded into the hub uses `loadingMode=Menu`, identical to the title backdrop — Log189) and PlayerUnit is useless (the menu backdrop has one). Earlier loadingMode-based attempts wrongly allowed Create from the title (Log187) then wrongly blocked it in a loaded run (Log188/189). **Closing the menu around co-op loads (`CoopMenu`):** a host-driven level load shows a black-screen fade; a pause/options menu left open over it lets the player wedge the game with Space. The menu is two-level — `PauseMenu` (Resume / Options / Quit) hosts the `OptionsScreen` sub-page where the connect page lives — so `GameManager.ResumeGame()` alone is NOT enough (it closes the pause menu but leaves the options sub-screen up, wedging the menu state — Log192). `CoopMenu.CloseIfOpen` does the correct two-step `OptionsScreen.Hide()` (guarded by `OptionsScreen.IsShown`) then `GameManager.ResumeGame()` (mirrors ESC-out-of-options then ESC-to-resume); the open-check is `OptionsScreen.IsShown` (sub-screen) plus an active `PauseMenu` component (pause-menu screen), so it is a safe no-op when no menu is open. (Type/method names confirmed via Mono.Cecil metadata: `OptionsScreen` and `UI.PauseMenu` — an earlier guess at `PauseMenu.HideOptions` was wrong; that method is unrelated DevTools/MenuManager.) Called from `OnJoin` (active side) and from `NetManualSceneFollower.TryFollow` (the host-driven/auto-follow/relay chokepoint) so the **passively dragged** side also closes its menu. Pure game reflection, always compiled (no UI-lib dep). Create leaves the menu open (the host watches the live player list; Create triggers no load). **Join = link only:** pressing Join links the client *synchronously* (before the async handshake); the host sends its current scene request on handshake, so the now-linked client's existing **auto-follow** (`TryAuthorizeAutoFollow`, gated on `ClientLinked`) brings it into the host's scene. An earlier cut also fired a proactive follow → it **double-loaded** with auto-follow and corrupted the join (Log185: `joined` flickered True→False, `inHostScene=False`); removed — auto-follow alone is the single entry. **Run-state priming (Log186):** because networking now starts *after* the save is loaded, the host's `GoToLevel` fired before its `NetService` existed, so its run-state was `<unknown>:-1` and the on-handshake scene request was empty → the joining client couldn't auto-follow until the host's next periodic broadcast (~10s). Fix: `NetRunStateBridge` caches the last `GoToLevel` even while networking is off, and `CoopConnection.Apply` calls `PrimeServiceFromCache` right after `Start` to replay the cached level + re-read the current seed (latch reset via `NetLevelSeed.ResetReportLatch`) into the run-state — so the host's handshake scene request is valid immediately and the client auto-follows *on join*. **No-auto-start consequence fixes (Log198/199) — "client can't see host on first join":** the same "networking starts after the save is loaded" shift caused two more bugs, both fixed by replaying cached state on `Start`. (a) Priming the run-state seed was wrong for the **client** — it captured the client's pre-join seed and latched it, and a same-named follow then stuck that stale seed in the run-state so the proxy seed-match check rejected the host → priming restricted to the **host** only. (b) The host's `AddPlayer` fired before `NetService` existed, so the host **never captured its local player and never broadcast its transform** until it next changed levels (Log199: host had no "Local player transform captured", so the client received nothing — 0 proxies, 0 rejects); `NetRunStateBridge` now caches the local player object too and `CoopConnection.Apply` calls `PrimeLocalPlayer` on `Start` (both roles). A gated (`LogRemotePlayerBody`) `[RemoteProxyDiag]` (per-proxy visible/pos/age + REJECTED scene/seed) and the proxy-create position log were added during the hunt and kept. **Deferred to a later slice:** Steam-name auto-seed and the host LAN-IP display (the two §5 feasibility-unknowns). `CoopConnectPage`, `NetConnectFeedback`. | verified in-game (Log190 connect/join · Log200 host visible to client) |
| **UI-3c-JoinClose** | **Close the connect menu only on a *successful* join, not on press.** `OnJoin` no longer closes the menu immediately — a failed/rejected handshake now keeps the page open so its `NetConnectFeedback` error line stays visible. The close is deferred: `OnJoin` sets `_closeMenuOnJoinSuccess`, and `PollJoinClose` (run from the existing throttled `Tick`) resolves it once the attempt settles — close on `CurrentMode==Client && LastError empty`, drop the request (menu stays) otherwise; `OnCloseRoom` clears the pending flag. The black-screen-load wedge the old eager close guarded against is still covered host-side by `NetManualSceneFollower.CloseIfOpen("host-driven-follow")`, which on a real join always runs before its fade. `CoopConnectPage`. | verified in-game (user) · committed |
| **RT3-Cousin-arms-Room** | **Out-of-arena and downed players aren't hit by the Cousin arm's group throw** (same intent as the downed-untargetable rule). The arm group-attack differs per end — host throws one *real* ball per `GameManager.Players` entry (its own player + each client ghost proxy), the client throws at its local player + a visual ball per remote proxy. `CousinArmPatches.ThrowProjectile_Pre` skips downed targets (`NetPlayerLifeManager`) and out-of-arena ones. **First attempt used the boss-trigger room membership (`_roomMembers`) and FAILED (Log176/177): it's seed-keyed (churns on reload), wiped mid-encounter by `Reset()`, and only catches boss-trigger crossers (never a late-walk-in host) — so it never held both players at once.** Re-done on **ArenaLockdown** membership, which is keyed by stable arena position, accumulates everyone incl. late walk-ins (doorway sensor), and was proven correct in the same logs (`members=[client-1,host]`). Host filters real throws by the authoritative `_locks[arena].InRoom`; a new `NetArenaCommand` kind `Membership=7` broadcasts the set so clients filter their visual balls. **Local presence uses the membership set too (`members.Contains(LocalPeerId)`), NOT doorway parity** — Log178 showed the doorway sensor miscounts re-entries (`count=2 inside=False` then `count=3 inside=True`), making a re-entering client skip its own throw; the set is fed by the reliable seal-trigger crossing and already contains the local player. Downed players are skipped (`NetPlayerLifeManager`). **The arm's AiAgent is exempted from the downed-untargetable null** (`CousinArmPatches.IsCousinArmAiAgent`): on the client the local player is the arm's only targetable Unit, so nulling it froze the arm when the client went down (Log179) — exempting keeps it throwing visual balls at the alive host while `IsTargetAttackable` still prevents hitting the downed player. Fail-open (downed filter still applies) when no active arena membership is known. Config `ExcludeOutOfRoomPlayersFromBossAttacks` (needs `EnableArenaLockdown`). | verified Log180 (all 4 scenarios incl. client-downed → boss keeps hitting host; 0 NRE both ends) · committed | 
| **UI-3d** | **The two §5 feasibility-unknowns, now resolved.** (1) **Steam-name auto-seed:** the connect page fills "Player name" from the local Steam persona when it's still the generic default/empty — SULFUR ships Steamworks.NET and already fetches the name at startup ("Steam initialized, playing as …"), so `SteamIdentity` reads the same source by reflection (global `SteamManager.Initialized` guard + `Steamworks.SteamFriends.GetPersonaName()`); a name the player set themselves is left untouched, and a missing/uninitialized Steam falls back to the configured name. (2) **Host LAN-IP display:** while hosting, a live row shows this machine's LAN IPv4 + port so peers know what to type into "Host address" — `NetLocalAddress` derives it via the UDP-connect trick (OS picks the outbound interface; correct with VPN/VM adapters present), cached, fallback to the first non-loopback host IPv4. Both pure reflection / BCL, soft-dependency safe. `CoopConnectPage`, `SteamIdentity`, `NetLocalAddress`. Loc rows 16/18. | verified Log202 (both ends resolve their own persona — host デスボックス, client 房屋中介(中國語意味), CJK renders; host LAN-IP row shown; 0 errors) · committed |
| **UI-3e** | **Connect page auto-saves; the footer "Save settings" button removed.** The big bottom-right footer button was easy to miss, so it's gone (`SetFooter` no longer called → no footer created, which also frees the scroll space it reserved). Settings now persist automatically: the four input fields still write only to drafts on edit, and `AutoSaveDrafts` (run from the throttled `Tick`) calls `SaveSettings` whenever the draft signature changes — a built-in ~0.4s debounce, no per-keystroke disk churn, validation/fallbacks unchanged (an unparsable port leaves the old one). Baseline seeded from the freshly loaded drafts so merely opening the page (incl. the UI-3d Steam-name seed) doesn't rewrite config — only a real edit saves. The out-of-game `OnCreate`/`OnJoin` footer-status calls (now dead) were dropped; the gate-hint row already explains the disabled buttons. Loc row 16. `CoopConnectPage`. | verified in-game (user) · committed |
| **5.7-NP2** | **Seamless un-pause when a co-op session starts with a menu already open.** The NP add-blocking prefix only stops *future* pause padlocks; a menu opened *before* the session (e.g. the Options menu you press "Create game" from, which leaves the menu open) had already placed its `Paused` padlock, so the world stayed frozen until that menu was closed and reopened. Fix: `PauseControlPatches.Tick` (driven from `Plugin.Update`) watches the `SuppressPause()` false→true edge and retroactively removes the four gameplay padlocks (`Inventory`/`Paused`/`DevTools`/`Dialog`) via `ModifyGamePauseState(_, false)` so gameState falls back to Running while the menu UI stays up — no close/reopen needed. Removal is idempotent (a later menu-close removing an absent padlock is a no-op). Pure reflection (`GameManager.Instance` resolved as property-or-field). Gated by the same `DisablePauseInMultiplayer`; logged under `[PauseControl]` when `LogPauseSuppression`. | verified Log201 (both ends: "released held gameplay pause padlocks" on session start, menu stays open + world resumes, 0 errors) · committed |
| **JoinFollow-cfg** | **`HostLinkedByDefault` hardcoded `Fixed<bool>(true)` (retired from the `.cfg`).** The host's link master switch defaulting to a `.cfg` value was a silent join-breaker: with it `false`, `SendHostSceneRequest` early-returns so the host broadcasts no scene request → a joining client never gets a target → it links but can't auto-follow ("进不去"), with no error or hint. Root-caused only after a user set it off (via an external config editor that took effect on restart) and bisected it by hand. Now `CoopConfig` binds `new Fixed<bool>(true)` and `"HostLinkedByDefault"` is in `CoopSettingsStore.RetiredCfgKeys` so `PruneRetiredCfgKeys` physically deletes any stale/off value from the `.cfg` on load. A temporary single-player host is still available in-game via `HostLinkToggleKey`. Repays migration tech-debt: a legacy `NetworkSceneAuthority` key whose off state could disable the whole feature. | user-diagnosed + built 0-err/deployed · verified (join/auto-follow has worked cleanly across every subsequent co-op session — EMP/TB/LD-Sandstorm/RS/STEAM tracks) |
| **EMP-2b** | **Client Emperor phase-1 worm hitch root-caused + stopgapped.** The long-standing client-only ~1 fps in the Emperor fight was finally pinned to the client's OWN autonomous `EmperorBossWorm.FixedUpdate` physics (native): worm alive = ~1 fps, worm dead / level-reload / single-player = smooth; network-independent (persists after Leave-room and host-close); F3/pause stops it. `[EmperorWormPerf]` ≤13 ms, `[UpdateProf]` ≤99 ms, `gc0Δ` 0–1 → the cost is the raw rigidbody integration, not our C#/GC/particles/colliders (corrects `EmperorBossAudit.md` §8.5, which had it as an un-root-caused "burrow" hitch and had *wrongly* concluded not-freezing was right). **Two changes:** (1) **send-side** — the host no longer collects/sends enemy-state snapshots for the 10 worm sections (`IsEmperorWormSectionSnapshot` skip in `CollectHostEnemyStateSnapshots`); they were never applied on the client (queued thousands, applied zero) — pure waste + a contributor to the receive load. (2) **stopgap** — a **linked** client prefix-blocks `EmperorBossWorm.StartMovement` (`EmperorWormDiagnostics.StartMovement_SuppressPre`, registered unconditionally before the diagnostics gate; not config-gated) so `rb` stays kinematic and `FixedUpdate` bails → 0 hitches (Log244). Host / single-player / unlinked-solo client run the worm normally. **Accepted cost:** the worm is pre-placed underground and only emerges via `StartMovement`, so a suppressed worm never appears on the client. Superseded by **EMP-3a** (§8.6 head-streaming: keep the worm kinematic + visible, drive the head from a host stream). | verified Log243 (root cause) / Log244 (0 hitches, worm invisible) · committed `f470d48` |
| **EMP-3a** | **Client Emperor phase-1 worm head-streaming — the real fix (supersedes the EMP-2b stopgap; worm is now visible + moving + synced + non-laggy).** `EmperorWormDiagnostics` hooks `EmperorBossWorm.FixedUpdate`: host captures the head, a linked client drives its local worm and skips native. `NetEmperorWormSync` streams the head at **20 Hz** (`HostEmperorWormHead`, msg 57, Unreliable — head pos + yaw + seq); the client keeps `rb` kinematic (native physics never integrates) and runs only the cheap `UpdateWormSections` locally. **Head-streaming alone did NOT fix the lag — two more iterations did** (Log245–249, each pinned from `[EmperorWormHead]`/`[EmperorWormFrame]`/`[UpdateProf]`): (1) **the residual was PhysX broadphase churn** — driving the head teleported ~11 kinematic colliders across the static arena every substep (cost ∝ head-jump distance × colliders, matching the user's stairs-vs-open-floor observation); `EnsureWormVisualOnly` disables the head + 9 non-weakpoint section colliders, keeping only the current tail (`lastActiveIndex`) hittable → **1 fps → playable** (Log248). Corrects §8.5's "not the broadphase": native integration was the EMP-2b cause, going kinematic then exposed a second broadphase cost. (An earlier guess to pin section *rigidbodies* kinematic was a no-op — `pinned 0/10`, they already spawn kinematic.) (2) **Snapshot interpolation** — `OnHeadReceived` keeps the last two samples, `DriveClientWorm` renders between them ~one interval (~50 ms) in the past → removes the 20 Hz teleport-flicker with a small fixed delay (not a velocity-proportional trail; `Unreliable` drops handled by clamping `t`). **Result:** the "fixed 10 000× never fixed" client Emperor lag resolved — sustained ~1 fps → occasional ~155 ms blips (Log249), which correlate to fight-start init / a one-shot asset-scan probe / the diagnostic build's own `[ProbeSummary]` disk flush / general co-op combat mirroring, **not** the worm. Deferred: section-destruction sync (EMP-3b), death + phase-2 handoff (EMP-3c). `NetEmperorWormSync`, `[EmperorWormHead]`. | verified Log249 (playable + smooth motion) · committed |
| **EMP-3b** | **Section-destruction sync.** The client's local `WeakpointHit` never actually fires (its damage is host-authoritative — see EMP-3d), so it never derives a `DestroySection` call itself — a host postfix on the real `DestroySection(index)` broadcasts (`HostEmperorWormSectionDestroy`, msg 58, ReliableOrdered); the client mirrors by invoking the SAME native `DestroySection`+`MoveVulnerableSectionUp()` pair on its own worm (index recomputed from its own `lastActiveIndex`, which only ever changes in response to this event, so it stays in lockstep with the host's) — reuses the real gib/frag/invuln/list-shrink/tail-teleport logic instead of re-deriving the health-floor-top-up math. **Bug fix bundled in** (found while wiring this): `EnsureWormVisualOnly` (EMP-3a) picked the hittable weakpoint by indexing the UNSHRUNK `wormNpcs` list with the SHRINKING `lastActiveIndex` — correct only before the first section died, after which it pointed at an already-destroyed section and left the real (still alive) tail unhittable on the client. Fixed to key off the stable `lastSectionNpc` field (by GameObject id) instead. | verified Log253 (host index 8→1 broadcasts ↔ client mirror seq1-8 lockstep, 0 exceptions) · committed `3fd41fa` |
| **EMP-3c** | **Terminal death + phase-1→phase-2 handoff.** A host postfix on `DeathAnimation()`'s kickoff (fires the instant `WeakpointHit` starts the coroutine, not 5 s later) broadcasts (`HostEmperorWormDeath`, msg 59, ReliableOrdered, one-shot); the client mirrors by starting the SAME native `DeathAnimation` coroutine on its own worm via `StartCoroutine`. Reusing the real method — instead of hand-rolling the death sequence — means the client automatically gets the vanilla lerp-to-roomCenter/shake/slam/explosion/gib, `npc.Die()`, `CleanupDestroyedSections()`, **and `EmperorBossFightHelper.StartPhase2()`** (the phase-2 spider handoff) for free. The client's head-stream driver (`DriveClientWorm`) stops touching `transform`/`rb.position` once death is applied, so it doesn't fight the coroutine's own lerp. Scope note: this only made Phase 2 (the spider) **activate** on both ends together — full spider sync followed as EMP-6. | verified Log253 (both ends `DeathAnimation` + all 10 sections removed, client worm head destroyed, StartPhase2 handoff on both ends) · committed `3fd41fa` |
| **EMP-3d** | **Client→host worm damage authority (the blocker behind EMP-3b/3c).** Log250-252: host `hitRecv=0`, client `clientHitSent=0` — client→host worm damage was fully broken. Root cause: the vulnerable tail is a runtime `SpawnUnitAsync` add (not in the level-load manifest) and the Emperor isn't a registered boss encounter, so the ordinary roster `ClientHitRequest` path **quarantines it as "client-only"** (16-31× per session) → not a puppet → hits never leave the client. Retires §7b's "damage already routes (Log216)" assumption (true only before EMP-3a head-streaming). Fix = the §7.5 design done explicitly: a client hit on the tail (matched by GameObject vs the client worm's `lastSectionNpc`) is forwarded (`ClientEmperorWormHit`, msg 60, ReliableOrdered) and suppressed locally; the host applies it to its **real** `lastSectionNpc` via `BossDamageReflect.TryApplyRealDamage` (source = `GameManager.Instance.PlayerUnit`) → vanilla `onDamageRecieved → WeakpointHit` advances health/section-destroy/death. Inserted in `Npc_ReceiveDamage_Pre` before the roster path; single-target (only the tail's collider is enabled). `[EmperorWorm] client -> host worm hit` / `host applied client worm hit`. | verified Log253 (`host applied ... ok=True vanilla=True`, tailHp 0.996→0.000, unblocked EMP-3b/3c) · committed `3fd41fa` |
| **EMP-4** | **Boss-fight-start dialog sync (host-authoritative), the Cousin/Lucia-style gate applied to the Emperor.** Reverse-engineered entrypoint (Log254): `StartMovement` (the worm's fight-start) is invoked by the last multiple-choice dialog option's NodeCanvas callback — same chain shape as Cousin/Lucia, not a `PlayerTrigger`/`TriggerFight`/animation event. Gate it: a linked client prefix-blocks its own local `StartMovement` and requests (`ClientEmperorFightStart` msg61); the host commits (own pick inline, or a client request via reentry) and broadcasts (`HostEmperorFightStart` msg62) so every end runs the same native `StartMovement` together — keyed by worm instanceId (idempotent across reload). On commit, the P1 dialog is disabled on every end (`Npc.dialog=null` on the worm-head Npc, same primitive as Cousin's `TryRemoveDialogInteractable`) so it can't be re-triggered mid-fight. | verified Log255/256 (both host-first and client-first commit paths, dialog closes on every end, no re-trigger) · committed `98b487a` |
| **EMP-6 (a–g)** | **Emperor phase-2 spider — full host-authoritative sync**, reusing the worm's building blocks (audit §9). **6a** reverse-engineered the spider (kinematic, fixed-waypoint path — no ballistic physics, so none of the worm's physics-spiral risk) + observe-only probe. **6b** implemented position streaming (root-transform + waypoint index, `NetEmperorSpiderSync` msgs 63-67), fight-start gate (EMP-4 analog on `Initialize`), client→host damage authority (EMP-3d analog), and defend/rapid-fire/death discrete-event mirroring. **6c** fixed 3 real bugs: boss chased only the host (now retargets to nearest of all players via a reused anchor Transform), P2's health bar froze (P2 arena has no net-run-state so the generic enemy-state mirror never runs — added an explicit absolute-health field on the transform stream), and a late-joining player re-triggered the host's `Initialize` (dialog wasn't nulled — fixed same as the worm). **6d** fixed the P2 dialog not closing on commit (missing `FinalizeLocalDialog`/`Graph.Stop`, same primitive the worm needed for EMP-4), added a client hit-flash (was silently suppressed with the local damage), and streamed the P1 worm's tail health the same way as P2 (both arenas lack net-run-state). **6e** diagnosed the host's multi-encounter frame drops as the same native `EmperorBossWorm.FixedUpdate` ballistic-physics cost as the original client hitch (not a leak — a census probe proved exactly 1 live worm/spider throughout); see **EMP host hitch** below — still open. **6g** teleports a late-arriving P2 player (the underground pit has no arena-lockdown gate) to the fight-starter's live position via the now-dead spider dialog trigger. | verified Log259-267 (position lockstep, damage 18/18 applied, defend/rapid-fire/death mirrored, nearest-player targeting, dialog no longer re-opens, Log273 for 6g late-arrival pull) · committed `32f5427` (6a-6e) / `0fc2fbb` (6g) |
| **EMP-6f** (attempted, reverted) | Host-side kinematic worm drive — the direct fix for the host hitch below (skip native ballistic `FixedUpdate` on the host too, same as the client, driving position from a cheap scripted approximation while reusing all native decision methods). **The direction worked** (Log266: `[EmperorWormFrame]` 100+ → 4, all stale): the host stall genuinely is the native rigidbody integration, confirmed on both ends now. **Reverted, not just paused:** porting the client's `EnsureWormVisualOnly` collider-disable to the host broke `AllSectionsBelowGround()` (it reads the now-disabled `AboveGroundTrigger` colliders) → the underground-travel cycle never fired → fall/reset loop → new hitches from the reset loop itself (Log267). Small, scoped fix if revisited (audit §8.10) — not attempted again since. | implemented, tested, **reverted** — `git restore` to the EMP-6d state; not in current code |
| **EMP-7** | **Worm attacks in-room players in turn, not just the host.** The vanilla phase-1 worm always targets its own local `player` field (set to the host's player in `StartMovement`), so in co-op it only ever chased the host. Host-only fix (the worm runs native on the host): all worm targeting funnels through that single field, so a reused anchor Transform is kept on the current in-room target's live position (`HostUpdateWormTarget`, per frame) and advanced to the next in-room player on every jump (`HostAdvanceWormTarget`, a `JumpToNextTarget` prefix). In-room set = `ArenaLockdownManager.TryGetActiveArenaInRoom` intersected with players actually present; fail-open to the host player if no set is known. Client unaffected (head-streamed). | verified Log268 (clean round-robin host ↔ client, in-room set updates live, 0 exceptions) · committed `61dac85` |
| **EMP host hitch** (known issue, **still open**) | The host drops to 1-3 fps (`[EmperorWormFrame]` dt=1000-3400 ms) across repeated Emperor encounters (escalates per encounter, survives same-level reload). **Not an EMP-3 regression:** EMP-3b/3c/3d broadcasts fired 0× that session, 0 exceptions, `gc0Δ≈0`, and `[UpdateProf]` = 50-118 ms while the stall is 1000-3400 ms → the stall is **native (physics/render), outside our C#** — the same "raw `EmperorBossWorm.FixedUpdate` ballistic physics too heavy under co-op overhead → fixed-step spiral" that §8.5 pinned on the *client*, now on the authoritative host. **A census probe (EMP-6e, Log264/265) ruled out a leak** (exactly 1 live worm/spider throughout every reload). **The direct fix was found and proven (EMP-6f, Log266) but reverted** over an unrelated collider bug — see **EMP-6f** above; the fix direction (host-side kinematic drive) is known-good, just not currently in the tree. Needs a real Unity Profiler or a redo of EMP-6f with the collider bug fixed (per §8.5/§8.10). | **deferred / known — open** |
| **RS-2** | **Run Stats Cards — the loading-screen card overlay (UI phases 3+4 base).** Self-built uGUI ScreenSpaceOverlay canvas (first in this repo; the Native UI Lib surfaces don't fit a full-screen card display): `src/UI/RunStatsOverlay/` — `RunStatsOverlayManager` (lifecycle: shows while `PendingRunEndDisplay && IsLoadingLikeState && session active && GameScene`, hides the instant any goes false, no time cap; sortingOrder recomputed topmost per show), `RunStatsCanvasBuilder` (Viewport `RectMask2D` + scrolling Track), `RunStatsCardView` (name + 7 always-visible stat rows, height derived from content via VerticalLayoutGroup+ContentSizeFitter — a fixed-height first cut with `childControlHeight=false` left rows at Unity's default 100px and spilled outside the card), `RunStatsCarouselController` (one focus step per input, clamped, eased), `RunStatsInputReader`, `RunStatsCardHoverAnimator`, `RunStatsCursorControl` (save-once/restore-once cursor free while shown, re-asserted each tick against native re-locks). Native-language font sampled per Run off a live game `TextMeshProUGUI` (same trick as the UI Lib toasts — CJK fallback chain for free). Cards' `raycastTarget` all off (they sat topmost and ate clicks meant for native UI underneath — Log374 showed hub-dialog raycasts landing on `RunStatsCard`). **Two hard-won platform lessons:** (1) Log375 — an exception thrown per-frame inside the interaction code silently aborted `Tick` before the shown-flag committed, wedging cards+cursor on screen forever, invisible because this profile doesn't pass Unity's exception log through to BepInEx → lifecycle and interaction are now separate failure domains (interaction exceptions log once via `WarnOnce` and can never corrupt show/hide), which is also what made the next log name the real culprit; (2) Log376 — **this game's player build has legacy `UnityEngine.Input` fully DISABLED** (`InvalidOperationException: switched active Input handling to Input System package`); BepInEx `KeyboardShortcut` still works (BepInEx's own abstraction) but raw `Input.*` calls throw — all overlay input reads (`wheel/pointer/gamepad`) go through `Mouse.current`/`Gamepad.current`, with per-source one-shot self-disable guards and the InputSystem-touching bodies isolated in non-inlined methods so even type-load failures are containable. | verified Log377 (show/hide/second-run/cursor/hover all working, both ends) · committed `b5ecbb1` |
| **RS-4** | **Run Stats Cards — fire-lake card styling + the Balatro-style hover feel + carousel step clamp.** Card palette moved to the mod's fire-lake language: charred body (distinct from pure black), old-silver border for other players, sulfur-gold border/name/glow for the local player's own card (replacing the earlier full-tan body). Hover interaction rebuilt around one shared under-damped spring (`RunStatsSpring`, ω=14 ζ=0.75): cursor-relative aim from card center drives Z-lean, directional squash, ~5% lift, counter-sliding drop shadow and a warming highlight, with a soft overshoot on release. **Platform lesson:** a ScreenSpaceOverlay canvas renders orthographically — real X/Y RectTransform rotation projects with zero perspective (Log377 "only the scale shows"), so the 3D card feel must be faked with Z-lean + directional foreshortening + lift + shadow parallax. Carousel focus clamp corrected from `count-1` to `count-4` (visible-page count) so a fitting row can't scroll at all and an overflowing one stops with a full last page. Also removed the 25 s force-hide: the loading screen's own presence is the only display clock. | verified Log378 (hover feel + one-step scroll, both ends) · committed `9f076e1` |
| **RS-4b** | **Four presentation fixes on RS-4.** (1) Hovered-card bottom clipping: the Track sat bottom-flush against the `RectMask2D` edge, so hover growth crossed the mask — Track now vertically centered in a taller (560px) viewport, ~100px slack both sides. (2) Card group vertically centered on screen (+60px upward bias clearing the native "press to continue" prompt; height-matched scaler keeps it safe on odd aspect ratios) instead of bottom-anchored. (3) Responsive card gap: 44px reference units at 16:9+, easing to 26px near square (`ActiveCardSpacing`, recomputed per rebuild, shared with the carousel pitch math). (4) The truncated 45%-of-card "ember highlight" slab reworked into a fixed 62px title band exactly behind the player-name row, warming slightly on hover (position static). | verified in-game (user) · committed `097385c` |
| **RS-5** | **Dev-only End-key simulated-player injector.** Each End press appends one simulated player (distinct `Tester N` name + `sim-N` peer id, all 7 stats randomized) to a purely local display list appended after the real finalized broadcast when cards are built — so one developer can verify multi-card layout, hover, and the 5+-card carousel (edge peek / one-step / clamping) alone. Gated on the game's own `GameManager.DeveloperMode` (verified: public **static** bool property on `PerfectRandom.Sulfur.Core.GameManager`, the F3 dev-tools switch) — no mod config, dead key for normal players. Key read via `Keyboard.current.endKey` (legacy `Input` throws in this build, see RS-2); `System.Random` so the key can never perturb seeded gameplay RNG; usable mid-run (cards appear at the next Hub return) or over the live cards (immediate); cleared on every overlay hide, never broadcast or tallied. `RunStatsDevInjector`. | verified in-game (user) · committed `026e28e` |
| **RS-6** | **Cross-player best-stat highlight.** Each of the 7 stats is compared across the Run's displayed list; the best value(s) render in bright ember orange (`RunStatsBestMarks` + `BestValueColor`) — a third accent deliberately apart from plain near-white values and the sulfur-gold "own card" markers (gold = whose card, ember = best number in the Run). Times downed and damage taken count lowest-as-best; every tie highlights (never order-broken); a stat identical across all players gets no mark at all (covers all-zero rows and a solo Run). No network message: computed locally as a pure order-independent function of the finalized broadcast, so every end derives identical highlights; recomputed on every card rebuild (new Run / dev-injected card) so nothing lingers; the mark lives only in the value text's color set at bind time — the hover animator never touches text color, so hover/tilt/carousel can't obscure or change it. Completes the Run Stats Cards track (RS-1…RS-6). | verified in-game (user) · committed `a69f6d1` |
| **RS-1** | **Run Stats Cards — Phase 1+2: host-authoritative live tracking + Run-end broadcast (no UI yet).** A "Run" spans leaving the safe Hub until the Host's authoritative level-transition target (`GM_SwitchLevelRoutine_Pre`) is next the Hub. The Host tracks all 7 per-player stats (shots fired, damage dealt, kills, times downed, rescues, damage taken, destructibles destroyed) live throughout, piggybacking on chokepoints that already exist for other reasons (weapon-fire relay, client-hit-request damage authority, boss-hit authority, revive/downed state machine, breakable-break relay) rather than collecting from clients at Run-end; one new Client→Host report (`NetPlayerLifeStateKind.DamageTakenReport`) covers the one number only a client can compute (its own real post-mitigation damage taken). On Hub arrival the Host finalizes + broadcasts an identical, Slot-ordered snapshot (`RunStatsFinalized`, msg 68) to every client. **Two correctness bugs found and fixed during in-game verification:** (1) kill attribution was keyed by `NetGameplayEntitySnapshot.SpawnIndex` at damage-time but `NetRunStatsManager.EntityKey(instance)` at death-time — two different numbering spaces that essentially never matched, so every kill fell back to the Host regardless of who landed it; fixed by keying both sides off the same per-instance identity (`RunStatsEntityKey`, via the snapshot's tracked weak runtime-object reference). (2) the Host's own `dmgTaken` stayed 0 even when genuinely hit: the gate required `GetUnitCategory(instance) == "Player"` (a narrow, exact-type-name match from an unrelated older probe helper) AND'd with the canonical `IsLocalPlayerUnit` check — for the Host's own player instance the two disagreed, silently skipping the gate before it ever reached the HP read; fixed by dropping the redundant narrower check and keying solely on `IsLocalPlayerUnit` (confirmed by `[HP-NativeAPI]` log evidence: `GetCurrentHealth()` had never once been invoked with a `Player`-typed instance despite an in-progress Run). A related latent bug (the lethal hit that converts into the co-op downed state has its HP healed back up by `StabilizeDownedLocalPlayer` before `Unit.ReceiveDamage`'s postfix ever reads "after HP") is also handled: `EnterLocalDownedState` resolves the pending pre-hit HP directly as the damage taken before the heal can mask it. `NetRunStats(List)(Codec)`, `NetRunStatsManager`, `NetRunStatsClientCache`. Cards/UI are Phase 3+. | verified LogOutput372 (host+client kills/dmgDealt/dmgTaken/downed/rescues/broken all correct, non-zero where expected) · committed `f38173b` |
| **UI-Kofi** | **About-section Ko-fi button** (finishes the UI-3c `Ko-fi` placeholder — was `Coming soon`, no URL). New `AddSmallButton("Support on Ko-fi", …, 260f)` → `OpenUrl("https://ko-fi.com/ryukadev")`, mirroring the existing open-source-repo button; both given an explicit `260f` width. Loc row 16 updated (Ko-fi now a button, not a readonly value). **Dependency fix in SULFUR Native UI Lib (0.10.1, lib commit `5a48673`):** the small-button width argument had never actually applied — the button rows use `HorizontalLayoutGroup.childControlWidth = false`, so Unity sizes each child from its `RectTransform.sizeDelta` and ignores `LayoutElement.min/preferredWidth`; every small button stayed at the 120px `sizeDelta` baked in `CreateSmallButton`, ellipsising labels >~10 chars (`Open-source repo` → `Open-sourc…`). Lib now writes the width to `sizeDelta` (`ApplySmallButtonWidth`, both call sites), so explicit `minWidth` **and** label auto-size take effect. Lib behaviour change: all small buttons now size to their label (auto 96–180px) instead of a fixed 120px — this also un-truncates the connect page's own `AddButtonRow` buttons (Create/Join/Close/Steam) which already passed explicit widths. `CoopConnectPage`. | verified in-game (user — About buttons full-width + Ko-fi opens; Create/Join/Close row fits) · lib 0.10.1 committed `5a48673` · committed `366cbdf` |
| **STEAM-1** | Steam session plumbing: compile-time `Reference` to the game's own `com.rlabrecque.steamworks.net.dll` (`Private=false`, same convention as `PerfectRandom.Sulfur.Core.dll` — nothing bundled, real SULFUR AppID 2124120). New `SteamNetworkingSupport` — `SteamManager.Initialized` soft-dependency check (reflection, matches `SteamIdentity`), local `CSteamID`, raw `SteamNetworkingMessages` send/receive/accept wrappers on a private channel. No behavior change; logs availability + local SteamID at startup. | verified (two real Steam accounts) · committed |
| **STEAM-2** | Steam P2P as a second connection method — **a loopback relay under the existing LiteNetLib socket, not a rewrite**: `SteamRelayBridge` bridges Steam↔`127.0.0.1` datagrams so `NetService`/every codec/`NetHandshake` stay untouched and see ordinary LiteNetLib remote endpoints. Host: one loopback `UdpClient` per Steam peer, "connected" to `127.0.0.1:HostPort` (additive — Direct-IP hosting keeps working, unchanged). Client: `NetService.SetConnectTarget` points `ConnectToHost()` at a local relay port instead of the configured HostAddress/HostPort. `CoopConnection.ApplySteamClient`/`EnableSteamHosting`/`DisableSteamHosting` are the new entry points. Connection key still gates the handshake, same as Direct IP. **Three bugs found and fixed during real two-account verification:** (1) `SendRaw` used `k_nSteamNetworkingSend_UnreliableNoDelay` — Steam's `SendMessageToUser` only queues-and-negotiates a session for *reliable* sends, so the very first (session-opening) datagram was silently dropped pre-session and the host never saw a `SessionRequested` callback at all; fixed to `Reliable | NoNagle | AutoRestartBrokenSession` (the last flag also auto-recovers a mid-game session drop that previously left every further send failing `k_EResultConnectFailed` forever). (2) `CoopConnection.Apply()` unconditionally called `SteamRelayBridge.StopJoining()` on every invocation to clean up stale bridges from a *previous* session — but `ApplySteamClient()` opens the fresh bridge and sets `_pendingSteamJoinTarget` moments before calling `Apply()`, so that same line tore down the brand-new bridge before `NetService.Start()` ever connected through it; fixed by skipping the teardown specifically when this `Apply()` call is the one about to consume a freshly-set pending Steam join target. (3) the host's `_hostPeers` dictionary never removed an entry once a peer's session ended (success or failure) — so any retry from the same SteamID hit the `ContainsKey` "already bridged" short-circuit and was never re-accepted, even though the old bridge was long dead; fixed by subscribing to the new `SteamNetworkingSupport.SessionFailed` event (backed by `SteamNetworkingMessagesSessionFailed_t`, which also surfaces Steam's reason code + debug string for diagnosis) to drop the stale entry. | verified (two real Steam accounts, repeated join/leave/host-recreate cycles) · committed |
| **STEAM-3** | "Invite Friends" convenience via Steam Rich Presence's `"connect"` key (no Lobby — one less API surface): `SteamRichPresenceJoin.AdvertiseHosting` sets it + pops `ActivateGameOverlayInviteDialogConnectString`; accepting fires `GameRichPresenceJoinRequested_t` on the invitee's side with the host's SteamID64, feeding the exact same STEAM-2 manual-join path. Only our own `"connect"` key is ever touched (never `ClearRichPresence()` — that would also wipe the base game's own status text). Registered unconditionally at startup so an invite is caught even if we never host ourselves. **Three bugs found and fixed during real two-account verification:** (1) the host's inbound-session handler (`SteamRelayBridge.OnHostSessionRequested`) was only subscribed inside `StartHosting()` — a friend's join attempt made before the host ever clicked "Invite Friends" arrived with nobody subscribed at all, and Steam only gave up ~30s later (`SessionFailed` reason `App did not respond; discarding`); fixed by moving the subscription to a permanent `SteamRelayBridge.Initialize()` called once from `Plugin.Awake` (the handler itself still only accepts while hosting is enabled, but now responds immediately instead of leaving Steam hanging). (2) `JoinRequested` had zero subscribers anywhere — accepting an invite only latched `PendingInviteHostId` and auto-filled the connect page's text field; nothing ever actually joined, so every accept-invite attempt showed `join requested` in the log with no follow-up join at all; fixed by having `CoopConnectPage.ApplySteamState()` (background `Plugin.Update` tick, runs regardless of page visibility) call the same join path a manual "Join via Steam" click uses, the instant an invite is seen while free to join (not already hosting/connected, save loaded). (3) `EnableSteamHosting`'s `if (SteamHostingEnabled) return true;` early-return skipped re-invoking `AdvertiseHosting` on every call after the first, so "Invite Friends via Steam" only ever opened the Steam overlay once per room lifetime — closing and recreating the room was the only way to see the dialog again; fixed so the bridge only starts once but `AdvertiseHosting` (idempotent — sets rich presence + pops the overlay) re-fires on every click. | verified (two real Steam accounts, repeated invite cycles) · committed |
| **STEAM-4** | Connect-page UI: a new "Steam" section below the untouched Direct-IP block — availability line, read-only "Your Steam ID", "Invite Friends via Steam" (host, additive to Create, re-clickable per STEAM-3), "Steam ID to join" + "Join via Steam" (client, same mutual-exclusion as Join game), pending-invite banner that auto-joins per STEAM-3 (falls back to "load a save to join automatically" if no save is loaded yet). New `LastSteamIdToJoin` setting in the existing `CoopSettingsStore` JSON (not `.cfg`). Docs updated: `NetworkingArchitecture.md` "Connection methods", `CoopUiPlan.md` §5. | verified (two real Steam accounts) · committed |
| **FF-1** | **Friendly fire as a host-authoritative session setting (default OFF)** — the first real player-vs-player damage path, replacing the connect page's `Coming soon` placeholder (CoopUiPlan §5/§7). Both directions built on the existing damage plumbing: **host→client** was already physically happening (host bullets hit the client's Plan B ghost Unit and `Unit_ReceiveDamage_Pre` forwarded *everything* as "enemy via target proxy") — now the forward classifies the source (`FriendlyFireClassifier`: IDamager == local PlayerUnit, or `DamageSourceData.sourceUnit`/`isPlayer`; fail-open = enemy so real enemy damage can never be blocked) and drops player-sourced damage when FF is off. **Client→host/client** is new: `ClientPlayerHitProxyManager` (client-only, FF-on only) keeps a minimal hittable Unit+Hitmesh at each remote player (stripped ghost recipe — no Player component, no `GameManager.Players`), a local-player hit on it sends `PlayerFriendlyFireHit` (msg 69, fail-closed — only positively classified hits) and the host re-validates (own config gate, stamped sender, damage clamp, victim not downed, scene match) then applies to itself (`ApplyFriendlyFireDamageToLocalHost` → the standard ReceiveDamage apply path) or relays via the existing HostDamageRequest channel. The host's toggle value is broadcast as `SessionSettings` (msg 70, revisioned; on toggle + per handshake) — the first session-settings sync; a client shows it on a read-only line. Setting lives in `coop.json` (`FriendlyFire`, default false); diagnostics gated by `LogFriendlyFire` (`[FF]`). `ProtocolVersion` 1→2. Intentional behavior change: with FF off (the new default) the old unintended always-on host→client player damage is gone. | verified Log386 (all scenarios: FF-off both directions blocked + enemy-damage regression clean; FF-on all three directions, mid-session flip, rejoin push) · committed `d4bfbc4` |
| **FF-1b** | **Session-settings rows visually locked on a client + the FF toggle mirrors the host live** (CoopUiPlan §5 "non-host rule"). While connected as a client the FF row is greyed and input-blocked — `OptionsScreenOption.SetLocked(true)` **plus** the private `checkboxToggle.interactable = false`, because the IL shows `SetLocked` only gates the keyboard/controller `Use()` path while a mouse click lands directly on the Unity `Toggle` graphic. The locked checkbox follows the host's synced session value via `SetIsOnWithoutNotify` (no change callback → the client's own coop.json preference is never overwritten); `OnFriendlyFireToggled` additionally discards any client-side commit that slips through between a role change and the next Tick. Leaving the room unlocks the row and restores the checkbox to the machine's own saved value. No new strings, no protocol change. | mirroring + input block verified in-game; the SetLocked visual was broken → superseded by FF-1c |
| **FF-1c** | **FF row lock visuals redone on a CanvasGroup** — in-game the FF-1b lock made the "Friendly fire" label *vanish* instead of greying: `OptionsScreenOption.SetLocked`'s IL recolors the label with the serialized `lockedTextColor`/`normalTextColor` fields, which a lib-built row never initializes — both default `(0,0,0,0)`, so locking painted the label fully transparent (and a later unlock would have wiped it permanently via the equally-uninitialized `normalTextColor`). `ApplyFfRowLock` now adds a `CanvasGroup` on the row root instead: `alpha 0.45` dims the whole row uniformly (label + checkbox), `interactable=false` gates every child Selectable (incl. the Toggle), `blocksRaycasts=false` makes the row fully inert to the pointer; the private `IsLocked` field is still set so the keyboard/controller `Use()` path early-returns. Text colors are never touched. | verified in-game (dimmed + readable + blocked; unlock restores; user) · committed |
| **SS-Toast** | **Toast on every host-side session-setting change** (CoopUiPlan §5 "change-notification rule") — when the host flips any session setting mid-session, every player is notified through the existing UI-1 toast surface: the host at its commit point (`OnFriendlyFireToggled`, only on a real value change while hosting), each client where the snapshot lands (`NetService.HandleSessionSettings`, gated by `NetSessionSettings.ApplyReceived` now returning true only for a live change — the join-time initial sync and rejoin re-sync stay silent, the locked row already shows the value; `ResetSession`'s revision reset keeps that guard correct across sessions). One shared formatter `CoopToasts.NotifySessionSetting(label, enabled)` → `{label}: On/Off` (Localization row 22) so host and clients render identical text and future settings reuse it — a new-setting checklist now lives in CoopUiPlan §5. No protocol change (msg 70 untouched), no new config (gated by the existing `EnableCoopToasts`); offline toggles notify nobody. | verified in-game (host+client toasts on mid-session flips, silent join sync, silent offline toggle; user) · committed |
| **WS-3-Idle** | **Dedicated standing (idle) sprite for the remote-player billboard body** — the WS-3 body froze on walk frame 0 whenever the player stopped moving. New idle art (`Art/Father.png` front / `Art/Father_Back.png` back, 971×2223 single-frame) converted to the established sheet convention (content-trimmed canvas scaled to height 512 px → 224×512; `PixelsPerUnit` 256 → same 2 m body height, waist anchor unchanged) and embedded as `father_idle_front`/`father_idle_back`. `RemotePlayerSpriteBody` shows the idle sprite for the current facing (front/back) while the synced input-walking flag is off; the 8-frame walk animation is untouched and still restarts on frame 0. Fallback: missing idle resources → old walk-frame-0 behavior. Visual only — no config, no protocol change. | verified in-game (two-instance: idle front/back + walk↔idle transition, no size/anchor jump; user) · committed `4b40c00` |
| **DR-1..DR-5** | **Downed/rescue UI rework (CoopUiPlan UI-4), replacing the last IMGUI surface with a host-authoritative rescue protocol + a real uGUI overlay.** **DR-1:** rescue progress used to be timed entirely on the rescuer's own client (only the hold-*complete* instant ever reached the network, as `ReviveRequest`); reworked so the rescuer's client only reports hold-start/hold-stop edges (`RescueHoldStart`/`RescueHoldStop`, reusing the `PlayerLifeState` DTO/message — 4 new `Kind` values + one `Progress` float field, no new `NetMessageType`/no `ProtocolVersion` bump) and the **Host** owns the actual clock: `NetPlayerLifeManager`'s per-target `ActiveRescue` record re-validates eligibility (target still downed, rescuer still alive, still in range) every tick, broadcasts authoritative progress (`RescueProgress`, ~every 120 ms) so both the rescuer's and the downed player's HUD read the identical value, and completes via the existing `ReviveAccepted` path or cancels (`RescueCancelled`) the instant it invalidates — closing the "two independently-timed local guesses" gap the design spec required. Multiple simultaneous rescues (3+ player sessions) are supported (host keyed by target peer id, first-come-wins the slot); each client's cached display is filtered to only the pair it's personally part of, so an unrelated rescue elsewhere in the session can't stomp its own progress. **DR-2 (`src/UI/DownedRescueOverlay/`):** a real `ScreenSpaceOverlay` uGUI panel replacing `NetPlayerLifeManager.DrawOnGUI`/`DrawCenterPrompt` (now deleted, along with `Plugin.OnGUI` — no IMGUI remains in this mod), built on the RunStatsOverlay lifecycle pattern (built once, hidden by default, topmost sorting reapplied per show, lifecycle/animation kept in separate try/catch failure domains). Native font sampling (`RunStatsOverlayManager.ResolveNativeFont`) promoted to a shared `SULFURTogether.UI.NativeFontSampler` so both overlays read the identical current-language font. **DR-3:** `DownedRescueBorderProgress`, a custom-mesh `MaskableGraphic` that traces the panel's rectangular perimeter clockwise from top-center (not a fill bar, not a radial) — two stacked instances (dim complete base + bright live progress). **DR-4:** `DownedRescueVitalityAnimator` derives a continuous 0..1 "vitality" from progress via smoothstep (never a discrete stage jump) and drives float/breathe/tilt (reusing `RunStatsSpring`, the existing underdamped-oscillator primitive) plus color/alpha lerps toward RunStatsCardView's existing ember/gold accents (no new hue invented). **DR-5:** on completion the panel holds a "Rescue complete"/"Restored" text for ~0.6 s then fades; a cancelled rescue just fades quickly with no text change — simplified from an earlier draft that also had a completion scale-punch (dropped per user edit to the plan). Docs updated: `Localization.md` rows 2/3/23-25, `CoopUiPlan.md` UI-4, `PlayerLifeAndDownedInput.md` §1. **Verification bug found + fixed (Log387):** after the first rescue in a session completed, every *later* downed/rescuer-hint display opened already at full brightness/motion instead of dim/idle — `NetPlayerLifeManager.CurrentRescueDisplay.Progress` is deliberately left at `1.0` after a completion (so the completion hold/fade above can show a closed border), but `DownedRescueOverlayManager`'s vitality animation read that raw cache directly instead of the same "what's actually on screen" value the border stroke used; pressing the revive key once (hold-start-then-cancel) reset it to `0` and masked the bug. Fixed by tracking one `_currentProgressForAnimation` value set alongside every `SetBorderProgress` call, so the border and the vitality animation can never disagree again. | **verified Log387** (host+client, repeated downed/rescue cycles incl. the stale-vitality regression test; user confirmed fix) · built 0 error/0 new warning |
| **WS-4-Downed** | **Remote-player billboard body lies flat while the peer is downed.** While a peer's replicated life state is `Downed` (new `NetPlayerLifeManager.IsPeerDowned`, deliberately excluding `NativeDeathCommit`), `RemotePlayerSpriteBody` switches to a lying-flat pose: the sprite plane goes parallel to the ground (hovering ~0.20 m above the feet/proxy origin to avoid clipping, never tilting with the viewer or the player's pitch), the synced look **yaw** rotates the whole sprite (feet along the horizontal look direction, head opposite — a body seen through its own head-mounted camera), and the synced look **pitch** only selects the sprite — looking up → front idle, looking down → back idle — with a ±5° hysteresis band around the horizon so it can't flicker; no walk animation while downed. The held weapon stops aiming independently (pitch dropped) and rides the flat pose as one unit: lowered near the ground beside the body, spinning with the same yaw (`UpdateWeaponOrientation(downed)`, stateless per-frame writes → leaving downed auto-restores the upright billboard, walk/idle animation, and hip-height yaw+pitch weapon aim). Reuses the existing idle sprites (user decision — the lying feel comes from the flat plane): zero new art/resources, zero protocol change, zero new config. | verified in-game (user, two-instance); hover height (0.10→0.20 m) + body orientation (head→feet toward the look direction) tuned on user feedback after the first pass |
| **Loc-1** | **Localization layer — implemented (flips the long-deferred `Localization.md` rule to done).** Every player-facing string (the 25-row registry) is now looked up by dotted key from per-language `lang/*.json`, with the English literal kept in-code as the fallback so behavior is byte-identical when the lib or a file is absent. **No wheel reinvented:** reuses the soft-dependency SULFUR Native UI Lib's own `SulfurLocalization` (wraps the game's I2 Localization, follows the current language, auto-loads `lang/*.json` by filename code, fallback chain `current→base→en→passed`). All access goes through one project-owned reflection boundary `CoopLoc` (`AccessTools`, wired once from `Plugin.WireCoopUi`) — the lib type never leaks into gameplay and the mod degrades to English without it; `CoopLoc.Format` does `{token}` interpolation, tokens identical across every file. Fonts handled entirely upstream (lib rows/toasts sample the current-language TMP, our uGUI overlays use `NativeFontSampler`) — zero font code. **14 languages, 98 keys each, key-set parity verified:** `en` canonical, `zh-CN`/`ja` hand-translated, `ko ru pl tr fr de es it pt sv ar` machine-translated (want native review; `ar` inert unless the game selects Arabic + has an RTL font). csproj `Deploy` copies `lang/*.json` beside the DLL into both `开发` profiles. `CoopLoc`, `lang/*.json`, `Localization.md` (Key column + status flipped). | verified in-game (user — 简体中文 accepted); +`connect.desc.steam` clarification "create a room first" appended in all 14 langs post-acceptance |
| **HZ-1** | **Ground-hazard DoT double-damage fix (issue #2 — a client's poison/fire downs ~3× too fast).** A synced enemy poisons both the client's own local puppet (a real, correct local status DoT) *and* the host's ghost target-proxy for that client; the proxy's per-tick DoT was being forwarded to the client through the host-authoritative enemy-damage channel (`ReportHostAuthoritativeEnemyDamage`), so the client took the poison twice (Log394: 291 local `[Hazard]` ticks + 82 relayed `type=9 Poison applied=True`, ~11 HP/s vs the host's ~3.7). Fix: the host proxy-damage-forward branch (`Unit_ReceiveDamage_Pre`) now skips periodic damage-over-time ticks — identified by `DamageSourceData.name == "PeriodicModifier"`, the tag the game's `AttributeEffect` stamps on every poison/fire/bleed tick — and lets the client's own local status DoT stand (single-player-equivalent); direct hits (any other source name) still forward unchanged. The client native-enemy-damage suppression only wraps the melee-hit window, so DoT coroutines escaped it and were the leak. Also lands the `[Hazard]` diagnostic probe (`HazardProbePatches`, gated `LogHazardProbe`, default OFF): postfixes on `Unit.OnStatusUpdated` + `RegisterAppliedStatus` log local-player damaging-status edges, the live DoT coroutine count, and the applier's identity — which proved the status *value* is hard-capped at 7 (so the reported "infinite stacking" was disproven) and the runaway was pure double-source. `ReverseProbePatches` (fix + `IsPeriodicDotSource`), `HazardProbePatches`, `CoopConfig` (`LogHazardProbe`), `PatchBootstrap`. | verified Log395 (relayed poison cut ~690→85 = −88%; residual all legit `amount=1.0` direct-hit forwards; client death rate ~11→~4.4 HP/s ≈ host — user "mostly fixed") · built 0 error |
| **HZ-2** | **Thrown-throwable EFFECT sync (issue #2 follow-up — a client couldn't SEE another player's poison/fire/explosion, and unsynced ground hazards were the real bug behind the "invisible poison downs me" report).** When a Breakable-type throwable (poison/fire flask, grenade, molotov, …) breaks on the thrower, broadcast the throwing weapon's `ItemId` + the LOCKED impact pose; every other peer resolves the weapon prefab (`AsyncAssetLoading.itemDatabase[new ItemId(value)].prefab`), reads its `ThrowableWeapon.prefabToThrow`, instantiates that at the spot and runs the game's own init+break (`SetOwner`/`SetStats`/`Spawn`/`Break`) so the on-break effect appears on every screen with zero physics drift. Peer-authoritative EFFECT mirror, same topology as BreakableBreak (thrower is authority; Client→Host→relay; thrower never mirrors its own). **Two identity dead-ends ruled out on real logs first (Log396/398):** the thrown Breakable's `UnitSO.id` is NOT unique (PoisonGrenade/StickGrenade/CryoGrenade all shared id 107) and isn't spawnable via the unit database (`UnitSO.SpawnUnit` returned null, and the retrying async spawns caused a client hitch) — the WEAPON `ItemId` (unique per throwable, resolvable via the item database → prefab) is the correct key. Tagging: `ThrowableWeapon.Throw` is bracketed by an `_inThrow` flag and the thrown Breakable is caught on the virtual `Unit.Spawn` (the obvious `Unit.SetOwner` hook silently never fired — it's a tiny non-virtual method the JIT inlines into `Throw`, confirmed by Log397). `Breakable.Die` routes tagged throwables to this channel instead of the in-scene BreakableBreak one (which would never match on peers). `NetThrowableEffect`(+Codec), `ThrowableEffectManager`, `ThrowablePatches`, `RuntimeSpawnManager` (item-db resolver + GameManager accessor), `NetService`/`NetMessageType`(=71)/`NetGameplaySyncBridge` plumbing, `BreakablePatches` (routing) + `BreakableBreakManager` (mirror guard), `CoopConfig` (`EnableThrowableEffectSync` Fixed-on + `LogThrowableEffectSync`), `PatchBootstrap`, `ReverseProbePatches` (level Clear). | **verified Log399** (host↔client both directions: 19/19 mirror spawn+break, 0 failures, unique itemIds per throwable; user "perfectly synced") · built 0 error. Follow-ups resolved: mirror-effect damage does NOT double (user-verified Log400 — HZ-1 covers the DoT case, explosion enemy-damage stays single); in-flight body handled by HZ-3. |
| **HZ-3** | **Thrown-throwable in-flight BODY sync (so peers see WHERE a grenade/flask was thrown, not just the landing effect).** At release, broadcast the weapon `ItemId` + launch position/velocity; each other peer spawns a VISUAL-ONLY copy of the thrown body — same `prefabToThrow` as HZ-2, but its `Breakable` component disabled so it never breaks, damages, or spawns an effect — gives it the velocity, and auto-destroys it after 2.5 s. The authoritative on-break effect still arrives via HZ-2 at the locked spot. **Two runtime bugs fixed on logs:** (1) Log400 — the launch velocity read 0 because `Rigidbody.AddForce(Impulse)` is applied on the NEXT physics step, not synchronously, so the flight is now captured one `WaitForFixedUpdate` later on the body itself (a one-shot coroutine, no persistent pump); (2) the visual body first passed through walls (collisions were fully off to avoid shoving characters) → now it collides with world geometry (hits walls / rests on the floor) but `Rigidbody.excludeLayers` masks out Player/Entities/MeleeColliders/Triggers so it never pushes a real character. `NetThrowableFlight`(+Codec), `ThrowableEffectManager` (flight capture/mirror + shared `ResolvePrefabToThrow`), `ThrowablePatches` (post-launch coroutine), `NetService`/`NetMessageType`(=72)/`NetGameplaySyncBridge` plumbing. | **verified in-game** (user — flies with a proper arc, stops on walls, no character shove, "perfect") · built 0 error/0 warning |
| **WXP-1** | **Client weapon XP now accrues (issue #4 — a non-host player's weapons never gained XP over a session).** Vanilla credits weapon XP in `Npc.Die() → GiveExperience()`, which adds the enemy's `ExperienceOnKill` to the local player's `EquipmentManager.lastEquippedWeapon` — but only when the enemy's `damagedByPlayer` flag is set (any player-faction hit sets it in `Unit.ReceiveDamage`). On a client, weapon hits are suppressed locally and routed to the host (`ClientHitRequest` → host applies via `Stats.SetStatus`, which never runs `ReceiveDamage`), so the client's enemy puppet never has `damagedByPlayer` set and its `GiveExperience()` early-returns; the host's own credit path is unaffected, and it correctly does **not** get XP for a client-only kill (the SetStatus path leaves its `damagedByPlayer` false). Fix reproduces the vanilla credit on the machine that dealt the damage, with no new protocol: the client tags each puppet it damages (`EnemyPuppetRecord.DamagedByLocalPlayer`, set at the confirmed `TrySendClientHitRequest` forward), and when the host death event applies the real `Npc.Die()` on that puppet, a guarded hook (`TryCreditLocalWeaponXpForKill`) reads the puppet's `ExperienceOnKill` and calls `Weapon.AddExperience` on the local last-equipped weapon (which applies the client's own `Stat_BonusXP` and handles rank-up). The kill stays host-authoritative; only the personal, client-owned XP is applied client-side, and each contributing client's weapon gets full credit. The flag is consumed on credit so a re-applied death event can never double-credit. Reads run in the client's own mutation context, so per-player XP bonuses apply. Known gap: only damage routed through the direct client-hit path is attributed (thrown-explosive/AoE kills are not) — matches the reported "weapons don't gain XP" case. `WeaponXpCreditManager` (new, typed), `NetGameplayProbeManager` (`EnemyPuppetRecord.DamagedByLocalPlayer`, tag on hit-forward, credit hook in `TryApplyMatchedNpcDeath`), `CoopConfig` (`LogWeaponXpCredit`, diagnostic only, default OFF). | **verified Log402** (client: 9 `[WeaponXp] credited exp=N` lines, 0 failures, per-enemy values 2/4/5; host: 0 lines = uses vanilla path unaffected; user "perfectly solved") · built 0 error |
| **TS-1** | **Host-authoritative one-shot trigger spawns (issue #5 — Caves maze skeleton ambush desynced: every player triggered their own local, unsynced skeletons).** Vanilla runs `PlayerTrigger.OnTriggerEnter → Triggerable.Trigger → TriggerSpawner.Spawn` locally and one-shot per machine (`Triggerable.onlyOnce`/`hasBeenTriggered` is a per-machine flag), so each player triggered a separate local spawn and the runtime-spawn mirror ignored them (its `ClassifyOwner` only whitelisted `DevToolsManager`). Fix routes every trigger through the host: a prefix on `Triggerable.Trigger(GameObject)` — only for a `Triggerable` that also has a `TriggerSpawner`; fog/other Triggerables and solo play fall through to vanilla — hands off to `TriggerSpawnSyncManager`. On a **client** it suppresses the local spawn and sends the host a `ClientTriggerSpawn` (msg 73) request keyed by the trigger's seed-deterministic world position. On the **host** it de-duplicates (first-trigger-wins, `_consumed` position set) and lets the real spawn run; a client request makes the host find its matching local `Triggerable` (scene scan, ≤1.5 m) and invoke its real `Trigger` under an `_applyingRemote` guard, passing the host player as the triggering object. Either way the host's `TriggerSpawner` spawn — now classified `TriggerSpawn` in `RuntimeSpawnManager` — reaches every peer through the existing `HostRuntimeSpawn` mirror + EnemyPuppet pipeline (host-authoritative AI/damage/death). Result: one shared spawn per trigger, visible and consistent for everyone. `NetTriggerSpawn`(+codec), `TriggerSpawnSyncManager`, `TriggerSpawnPatches`, `RuntimeSpawnManager` (ClassifyOwner), `NetService`/`NetMessageType`(=73)/`NetGameplaySyncBridge` plumbing, `CoopConfig` (`EnableTriggerSpawnSync` Fixed-on + `LogTriggerSpawnSync`), `PatchBootstrap`, `ReverseProbePatches` (level Clear). Host validation also compares the level **seed**, so a client that raced ahead into its own generated instance of the same level is dropped cleanly (rather than mis-matching a far Triggerable). | **verified Log403** (real shared encounter in `Act_01_Caves:2`: client `→ host request` → host `applying … match=0.00m` → `[RuntimeSpawn] host broadcasting … src=TriggerSpawn` → client `mirroring … src=TriggerSpawn` for unitId=17 BlackGuildSkeleton; repeat trigger `dedup`; user "同步成功"). Known limit: when the two players are in desynced level instances the ambush is skipped (safer than a foreign-maze spawn). · built 0 error |
| **CIV-1** | **Goblin civilians no longer flicker into spearmen on clients (issue #9 — every civilian briefly shows the spearman attack pose on a client, everywhere).** SULFUR goblins are billboard sprites whose visible pose is driven by an `Animator`; the `GoblinCivilian` controller reuses the spearman's attack clip (`GoblinSpearmanAttack`), and the civilian role (`AiAgent.AssignStartRole`) is picked with the global `Random`, so a civilian can independently roll "Offensive" on the host. When it does, the host briefly enters that attack and broadcasts a combat action; the client's enemy-puppet animation mirror then (a) replays it via `Npc.TriggerWeaponManually` and (b) faithfully plays the host attack **state hash** through `animator.Play` — both driving the civilian puppet's animator into `GoblinSpearmanAttack` (Log410–414: on a client, one civilian logged 88 replays / 58 spearman frames; the host showed 0). A civilian is non-combat and never needs an attack pose, so both paths are suppressed **on client puppets only**: `ApplyClientEnemyAnimationMirror` skips all host combat-state playback + hash Play for a civilian (it stays on the host-driven idle/walk/cower the Moving/Cowering bools already carry), and `ShouldBlockClientEnemyPuppetNpcCombat` blocks any attack-initiating call (`TriggerWeaponManually`/`TriggerAttackAnimation`/`TriggerShoot`/`MeleeAttack`) for a tracked civilian puppet. Both are gated on client-puppet membership (`ActiveEnemyPuppetsByNpcId`), so the host's own civilian keeps its native behaviour and host-authoritative damage is untouched. `NetGameplayProbeManager` (`IsCivilianNpc` reading `unitSO.isCivilian`, `IsSpontaneousAttackInitiatorSource`, the civilian block in `ShouldBlockClientEnemyPuppetNpcCombat`, the civilian skip in `ApplyClientEnemyAnimationMirror`). | **verified Log415** (client civilian plays only `GoblinCivilian*` clips — Idle/Walk/Cowering/Death, 0 `GoblinSpearmanAttack`; walks/cowers/follows normally; host unaffected; user "终于修好了，很完美") · built 0 error |
| **WID-1** | **Dropped items no longer become permanently un-collectable for everyone (issue #11).** A synced world pickup's vanilla `ExecutePickup` is blocked and the take is host-arbitrated (`WorldPickupManager.TryBeginTake`); before requesting/granting it pre-checks the local backpack via `HasRoomFor`. That pre-check only queried `ItemGrid.GetPossibleSpace(item)` in the item's **default orientation**, but the real `ItemGrid.AddItem(isPickup:true)` succeeds far more often — it accepts the item auto-consumed, fitting in **either** orientation, or dropping into a free **equipment slot** of its type. A false negative made `TryBeginTake` block the vanilla pickup *and* issue no take request, leaving the drop in the world with no resolution path — permanently un-collectable on that peer, and on everyone if both bags were in that shape (rotatable / equippable / consumable drop into a near-full bag). Not packet loss: msgs 48/49/50 are all `ReliableOrdered`. Fix rewrites `HasRoomFor` to faithfully mirror `AddItem`'s success predicate — auto-consume short-circuit, both-orientation grid check (`FitsAnyOrientation`), and `equipmentManager.GetFreeSlotOfType(slotType) != InventorySlot.None`. Genuinely-full bags still correctly leave the item in the world (vanilla-equivalent; resolves on retry once space frees). `WorldPickupManager` (`HasRoomFor`, `FitsAnyOrientation`), `Docs/WorldItemDrop.md`. | **verified in-game** (user — rotatable/equippable drops into a near-full bag are now collectable; "修复了") · built 0 error |
| **WID-2** | **World-drop rest-position sync + anti-tower separation.** `WorldPickupSpawn` only carries the initial drop position; from there each peer runs the pickup's `Rigidbody` physics independently and co-op's no-pause bag dumps let drops collide/scatter locally, so the same item visibly rests a short distance apart per screen (binding/take stayed correct — one peer taking it removes it everywhere — but the position desynced, and items pile into an indistinguishable tower). Two additions: **(A) owner-authoritative settle** — the dropping peer owns the `{peer,seq}` id and runs the authoritative body; `WorldPickupManager.Tick` watches its own drops and once one comes to rest (game's own criterion `linearVelocity² < 0.0025` held 0.4s, plus an 8s hard cap) broadcasts `WorldPickupSettle` **once** (`owner → Host → relay`, ReliableOrdered, new msg 75); mirrors snap to that position and freeze it (`isKinematic`, as the game does to a landed pickup). Re-sent only if a settled drop is later shoved > 0.3m and re-settles. One tiny message per drop, no streaming. **(B) anti-tower separation** — on each real local spawn (drops + own loot; mirrors excluded, they get the settled position) a drop landing amid other recent drops gets a gentle horizontal shove away from them (golden-angle fan when perfectly stacked) so a bag dump fans out. Local/cosmetic; the owner's settle re-syncs the resulting rest position so peers converge. Single-player untouched (gated on an active session). `ProtocolVersion 2→3`. `NetMessageType` (75), `NetWorldPickup`/`NetWorldPickupCodec` (settle DTO+codec), `WorldPickupManager` (settle tracking, `ApplySettle`, `ApplySpawnSeparation`, `Tick`/`Clear`), `PickupPatches` (separation on spawn postfix), `NetService`/`NetGameplaySyncBridge` (broadcast/relay/dispatch), `NetHandshake`, `Docs/WorldItemDrop.md`. | **verified in-game** (user — bag dumps fan out on the ground, positions match across peers, take/removal still correct; "完美") · built 0 error |
| **DEV-1** | **Developer mode as a host-authoritative, vote/entitlement-gated session flag (issue #8 — stop a lone `-dev true` player griefing others).** The mod now owns `GameManager.DeveloperMode`. Two concepts are separated (CLAUDE.md §3): **DevEntitlement** (`CoopDevEntitlement.Local` — did this instance launch with `-dev true` or the persisted `DevToolsEnabled` pref; re-read live so a solo secret-combo unlock still works) vs the **effective flag** driven by `CoopDevAuthority`. Solo → the flag follows local entitlement (applied once on the session→solo transition, then left alone so vanilla dev unlocks keep working). In a session → the flag is uniform and host-authoritative: ON only when **every** connected participant is entitled (auto) or a unanimous vote passed (VOTE-1); any membership change voids that consensus and **fails closed** (dev off + an explanatory toast so a player whose F3 tools stopped knows why). The value syncs on the existing FF-1 `SessionSettings` channel (new `DeveloperMode` field; host truth = the authority, not a saved config — it is transient, never written to the `DevToolsEnabled` pref). Handshake carries each peer's entitlement (host aggregates). While in a session `CoopDevAuthority.TickReassert` re-pins the flag every 0.5s so a client's local secret combo or the game's own `LocalizationManager.Reset` can't bypass the decision. `ProtocolVersion 3→4`. `CoopDevAuthority` (new), `NetHandshake`, `NetSessionSettings`(+`DeveloperMode`, per-field `ApplyReceived` change flags)/`Codec`, `NetPeerSession`, `NetService` (transitions + reassert), `CoopToasts.NotifyDeveloperMode`. | **verified in-game** (user — vote-gated dev mode works; Log422 shows the passed vote applying host-side: `session settings broadcast: … reason=dev mode: vote result devEnabled=True`) · built 0 error |
| **VOTE-1** | **Generic host-authoritative session-vote subsystem (`CoopVoteManager`), first consumer = `EnableDevMode`.** Modelled on the DR rescue protocol — the host owns the tally + a single 15s clock, clients only forward a cast and display the snapshot. Two rules: **Unanimous** (dev mode — any explicit decline fails at once; at the 15s deadline players who never voted count as agreeing; the proposer auto-agrees) and **Majority** (future consumers — a strict >50% either way ends it early, else the deadline decides with non-voters agreeing, ties fail). Anyone may propose (connect-page "Session events" button; host starts locally, a client asks the host which validates); one vote at a time; a failed vote puts its kind on a 10s cooldown; a membership change cancels a live vote. On a passed dev vote the host calls `CoopDevAuthority.HostApplyVoteResult(true)`. Input: **Y = agree, N = decline** (`Keyboard.current`). New msgs `ClientVoteStart`(76)/`ClientVoteCast`(77)/`HostVoteState`(78) + `NetVoteCodec`; `VoteTypes` (enums + snapshot). `NetService` (send/broadcast/handlers/dispatch/tick/reset/membership), `CoopConnectPage` (propose button + status). | **verified in-game** (user — propose → cast → tally → pass, dev mode applied on the result) · built 0 error · note: only the **Unanimous** rule has a consumer so far; **Majority**, the failed-vote cooldown and the membership-change cancel are exercised by no shipped caller yet |
| **EM-0** | **Endless Mode seed-parity probe (first step of the Endless co-op sync track — see [EndlessModeSyncPlan.md](EndlessModeSyncPlan.md)).** The planned host-authoritative Endless sync assumes both ends load the **same arena from the same seed**: vanilla `EndlessModeManager.Awake` captures `seed = (uint)GameManager.currentSeed` then picks the arena via `gameplayRandom.NextInt(0, arenaPrefabs.Count)` — a pure function of that seed, so if `currentSeed` matches (client applies `GlobalSettings.ForceLevelSeed` on follow-in) the arena is identical for free. A postfix on `EndlessModeManager.Awake` logs, at arena entry, the net role + captured `seed` + `GlobalSettings.ForceLevelSeed` + arena-pool size + chosen arena name (`[Endless] EM-0 arena entry: …`), so host and client lines can be compared to confirm (or refute) the parity assumption before any behaviour is built on it. **Pure diagnostic, log-only, no behaviour change.** `EndlessModeProbePatches`, `CoopConfig` (`LogEndlessSync`, `EndlessMode` section, default ON), `PatchBootstrap`. | **verified Log439** (both ends entered `EndlessArena_01(Clone)` with the same `seed=3348393749` — client's `ForceLevelSeed=-946573547`, `(uint)(-946573547)=3348393749` — arena parity holds → Option A confirmed viable, no arena-choice broadcast needed). Note: `EndlessModeManager` is NOT under `PerfectRandom.Sulfur.Core` (bare-name type lookup). · built 0 error | 
| **EM-1 / EM-2** | **Endless Mode host-authoritative world layer (client slave + host enemy mirror).** Vanilla runs a full independent `EndlessModeManager` on every peer (own arena/waves/enemies) — EM-0 proved the arena+seed already match, so the fix keeps the client's arena and makes the rest host-driven. **EM-2 (mirror):** `EndlessModeManager` added to `RuntimeSpawnManager.ClassifyOwner` → `"Endless"` (config-gated on `EnableEndlessSync`); the host's `SetEnemy → SpawnUnitAsync(this,…)` wave enemies now flow through the existing runtime-spawn broadcast → client mirror-spawns + binds them as host-authoritative puppets (same pipeline as DevTools/TriggerSpawn/boss-adds). **EM-1 (slave):** a linked client (`BossMode==Client && NetLinkState.ClientLinked`) prefix-skips `EndlessModeManager.StartEnemySpawning` (no local enemies / no spurious Stage-Wave alerts / no burst-index churn — hands `StartCoroutine` an empty enumerator) and prefix-skips `EndlessModeManager.Update` (its `spawnedUnits` stays empty since mirrored puppets are GameManager-owned, which would otherwise read as 100% killed and drive divergent wave/stage/arena transitions). Host + single-player untouched. `EndlessSyncPatches`, `RuntimeSpawnManager.ClassifyOwner`, `CoopConfig` (`EnableEndlessSync` Fixed-on), `PatchBootstrap`. **Known gaps (later phases):** client wave/XP/stage UI is stale (host-driven state = EM-3); when the host levels up its card selection does `SetTimeScale(0)` on the host only while the client keeps running (shared pause = EM-6 / independent no-freeze = EM-5); loop-scaling parity + world-card rewards + Endless bosses deferred (EM-7). | **verified Log440** (client mirrors the host's wave enemies — `bound=True` for each — and kills/deaths sync both directions; user "同步击杀是正确的，符合预期". Enemy loot/coins correctly shared via the existing SL/WID path; no double-drop). · built 0 error | 
| **EM-3** | **Endless Mode wave/XP/stage state sync (host-authoritative HUD).** The client's `EndlessModeManager` is a slave (EM-1) so its XP bar + stage/wave/level labels were stale/frozen. The host now broadcasts a `NetEndlessWaveState` snapshot (msg **86**, host→all, ReliableOrdered, `ProtocolVersion 10→11`) carrying `currentStage/currentWave/currentBurstIndex/loopCount/transitionState/currentXP/nextCardThresholdXP/currentCardLevel` + a monotonic revision — sent immediately on any discrete change and throttled (~6-7 Hz) for continuous XP, plus a 1 s keepalive for late joiners. The client applies it to its local manager fields and its `Update` prefix flips from "frozen" (EM-1) to **driven slave**: it renders the vanilla HUD via the game's own `UpdateUI` from the synced fields (skipping card-selection/arena-transition frames like vanilla) but still never advances waves, rolls XP, or triggers card selection. Host + single-player untouched. `EndlessSyncManager`, `NetEndlessWaveState`(+codec), `EndlessSyncPatches` (Update prefix), `NetService`/`NetGameplaySyncBridge`/`NetMessageType`(86)/`NetHandshake`(v11) plumbing, `ReverseProbePatches` (level-clear reset). **Known gaps (later phases):** XP orbs still spawn only on the host (the shared pool rises from host collection) — per-player orb spawn/collection = EM-5 (independent) / EM-6 (shared reconcile); card selection still freezes the host only (shared pause = EM-6). | **verified Log441** (client XP bar + stage/wave/level track the host; host `[Endless] EM-3 host state rev=…` stream; client drops the host's early ChurchHub-context snapshots correctly; 0 exceptions both ends; user "符合预期"). · built 0 error | 
| **EM-4** | **Endless progression-mode session toggle (`SharedEndlessProgress`).** A host-authoritative session setting — same plumbing as SL-4 `ShareAllLoot` / FF-1 `FriendlyFire` — selecting **Shared** (default) vs **Independent** Endless progression. Persisted in `coop.json` (`Setting<bool>`, default true), broadcast in the `SessionSettings` snapshot (new `SharedEndlessProgress` field, `ProtocolVersion 11→12`), mirrored on clients via `NetSessionSettings.SharedEndlessProgressEnabled` (the single read point the EM-5/EM-6 progression layer will gate on), toasted on live change, and exposed as a connect-page toggle (client row locked + mirrors the host, like the loot/FF rows). **Infrastructure only — the setting has no behavioural effect yet** (both branches still behave like the current EM-3 shared-ish world; EM-5 independent + EM-6 shared give it meaning). `CoopSettingsStore`(+`sharedEndlessProgress`), `CoopConfig`(`SharedEndlessProgress`), `NetSessionSettings`(+field/property/apply/reset), `NetSessionSettingsCodec`, `NetService`(build+toast), `NetHandshake`(v12), `CoopConnectPage`(toggle+lock), loc rows 30 (en/zh/ja + English fallback). | built 0 error · pending real-machine test (toggle + toast + client mirror) | 
| **EM-5** | **Endless Independent progression mode (per-player XP + non-freezing card selection).** Active when the host sets `SharedEndlessProgress` OFF. **XP:** a host postfix on the canonical `EndlessModeManager.OnEnemyDied` broadcasts the enemy's base XP drop (`NetEndlessXpDrop`, msg **87**, `ProtocolVersion 12→13`, position + `ExperienceOnKill` orb count); each client spawns its own orbs via its local `XPOrbManager`, which collects them toward its own camera into its own `currentXP` with no per-orb traffic (the melee-XP-bonus card is host-personal and deliberately not doubled into the broadcast). **Client progression:** in Independent mode the client's `Update` prefix returns to running vanilla `Update` (own XP threshold / leveling / card picks) instead of the EM-3 driven-slave — the world stays host-authoritative for free because the wave-advance guard (`kill% ≥ 100 && allBurstsSpawnedForCurrentWave`) can never fire on the client (`StartEnemySpawning`, the only setter of that flag, is suppressed). EM-3 `ApplyHostWaveState` now syncs world fields only (stage/wave/burst/loop) in Independent mode, leaving the client's own XP/level/transitionState untouched. **No freeze:** vanilla card selection does `SetTimeScale(0)` which would freeze the shared co-op world; a postfix on `StartTransition(CardSelection)` overrides it straight back to 1 and gives the selecting player a brief local invulnerability bubble instead (cleared on `CardSpinComplete`, with host-tick + Update-prefix + level-clear safety clears so a player can never get stuck invulnerable). Both ends. `EndlessSyncManager`(+xp-drop/invuln/timescale reflection), `NetEndlessXpDrop`(+codec), `EndlessSyncPatches`(OnEnemyDied/StartTransition/CardSpinComplete + mode-aware Update), `NetService`/`NetGameplaySyncBridge`/`NetMessageType`(87)/`NetHandshake`(v13). **Known gap:** world-spawning card rewards (companions/shop) still resolve locally in Independent mode → deferred to EM-7. | built 0 error · pending real-machine test | 
| **EM-5b** | **Endless XP orbs reworked into host-authoritative shared world pickups (like WID for XP).** Supersedes EM-5's initial per-player-orb model. Per the design decision, **both modes** sync-spawn orbs and sync-remove them (collected by anyone → gone for everyone); only the reward differs — Independent = the collector's own pool, Shared = the host's single pool (mirrored via EM-3). Granularity is **per-death-group** (each enemy's whole XP is one pickup). On the host, `OnEnemyDied`'s vanilla orb spawn is suppressed (a bracketed flag + an `XPOrbManager.SpawnOrb` prefix swallow, preserving the corpse-sink) and replaced by one broadcast pickup (`NetEndlessXpDrop` extended with `DropId`/`TotalXp`/`OrbCount`, codec v2). Both ends spawn cosmetic orbs (`xpValue 0` → vanilla collection never credits) + register a pending pickup; a per-frame `PickupTick` fires a collect request when the local player is within `pickupRadius + 1.5` of a pickup (client → host msg **88**, host resolves directly); the host does first-collector-wins, applies the Shared-mode pool add, then broadcasts `EndlessXpCollected` (msg **89**) — every end removes the pickup's orbs (`RemoveOrbsNear`, reflection over `activeOrbs` by position) and, in Independent mode, the collector's own `currentXP` gains `TotalXp`. `ProtocolVersion 13→14`. Also folds a **controller lock** into the independent card-select bubble (`ModifyControllerLock`, input-only) so the picking player stands still. `EndlessSyncManager` (XP-pickup rework + pickupRadius/activeOrbs reflection), `NetEndlessXpDrop` (rewritten) + `NetEndlessXpCollect` (new, +codec), `EndlessSyncPatches` (OnEnemyDied bracket + SpawnOrb swallow), `NetService`/`NetGameplaySyncBridge`/`NetMessageType`(87-89)/`NetHandshake`(v14). **Known gaps:** world-spawning card rewards still local (EM-7); one host round-trip of latency on the authoritative XP credit (visual orbs fly in immediately); two deaths at the same spot can share orb removal (rare, XP still correct). | built 0 error · pending real-machine test | 
| **EM-Bug2** | **Endless enemies now target the client, not only the host.** Root cause: Endless enemies acquire targets from `AiAgent.overridetargets` (rebuilt every `EndlessModeManager.RefreshTargets` from a host-player-only list), so the co-op ghost players (which drive normal enemies via `hostilesInLOS`) were invisible to them — and `RemotePlayerTargetProxyManager` deliberately skips enemies with a native override. Fix: a host postfix on `RefreshTargets` re-adds the client ghost units (`RemotePlayerRegistryManager.GhostUnitsByPeer`, newly exposed) to each Endless enemy's `overridetargets`, so `GrabHostileUnit` can pick whichever player is nearest. `EndlessSyncPatches` (RefreshTargets postfix + OverrideTarget reflection), `RemotePlayerRegistryManager` (static ghost-unit registry). **Known limit:** the vanilla `GetTarget` 10 m host-preference still makes an enemy within 10 m of the host prefer the host, so the split is fully fair only when players are apart. | built 0 error · pending real-machine test | 
| **EM-5c** | **Independent-mode Endless XP → deterministic kill attribution (first-damage / last-hit), with a host toggle.** Replaces the pickup model for Independent mode (Shared keeps EM-5b pickup). The host tracks, per Endless enemy, who first-damaged it and who last-hit it — client hits via `ProcessClientHitRequest` (direct record + a `HostApplyingHitPeer` flag so the `Npc.ReceiveDamage` host hook attributes the forwarded damage to that client), host hits via the same hook. On death (`OnEnemyDied`), the host resolves the attributed peer per the new **`EndlessXpFirstDamage`** session setting (default **last-hit**) and broadcasts an award (the reused `NetEndlessXpDrop` gains `AwardPeerId`, codec v3). **Only the awardee's screen** spawns the (real-value) orbs; they are force-collected — flipped to the vanilla `Collecting` state regardless of distance (so snipes work) — and vanilla credits the XP as each orb is absorbed (not instantly). No orbs on non-earner screens → no "orbs fly to me but vanish" illusion. New host-authoritative session setting `EndlessXpFirstDamage` (coop.json, connect-page toggle, `SessionSettings` sync + toast, `ProtocolVersion 14→15`). **bug1 fix:** a downed player's XP threshold no longer opens the card panel (prefix on `CheckXPThreshold` skips while downed; held XP fires on revive) — kills the soft-lock. `EndlessSyncManager` (damager maps + award + force-collect + XPOrb reflection), `EndlessSyncPatches` (OnEnemyDied branch + CheckXPThreshold guard), `NetGameplayProbeManager` (ProcessClientHitRequest record+flag), `ReverseProbePatches` (ReceiveDamage record), `NetEndlessXpDrop`(+AwardPeerId), session-settings plumbing + `CoopConnectPage` toggle + loc rows 31. | built 0 error · pending real-machine test | 
| **EM-5c-fix** | **Two follow-up fixes on the EM-5c award orbs + card select.** (1) **Far/sniped kills showed no XP + no credit:** force-collect stamped `XPOrb.collectStartTime` with `Time.realtimeSinceStartup`, but vanilla's Collecting attract-speed ramps on `Time.time - collectStartTime`; the two clocks diverge by accumulated load/menu time, so the term went large-negative → the orb accelerated *backwards*, shrank into the distance, and was never captured (`activeOrbs` grew, XP stayed 0). Fixed by stamping `Time.time`. (2) **Host soft-locked after leveling up:** the independent card-select "movement lock" used `ModifyControllerLock(Cinematic)`, but any padlock routes through `LockPlayerController`, which locks **Camera + PlayerMovement + Weapon + Interaction** and `InputReader.LockInput` — and card selection needs look (to hover a card) + Fire/Interact (to confirm), so the pick could never complete. Swapped to `AddLock(PlayerLocks.PlayerMovement)` (zeroes locomotion only; look + card-pick stay free). `EndlessSyncManager` (ForceCollectTick clock, card-lock swap). | **verified in-game** (user "完美": far/sniped kills burst-then-fly + credit; host levels up, picks a card, regains full control) · built 0 error |
| **EM-Target** | **Endless co-op targeting: distance-based (drop the 10 m host bias) + the card-selecting player leaves everyone's aggro.** Supersedes the EM-Bug2 "known limit". (Req 1) Vanilla `AiAgent.GetTarget` forces any override-target enemy within 10 m of the host's local `PlayerUnit` onto the host, ignoring the override list (which RefreshTargets_Post fills with every player's ghost). A host-only prefix, scoped strictly to a live Endless run (`EndlessSyncManager.EndlessActive`), bypasses the bias and returns the game's own distance pick `OverrideTarget.GrabHostileUnit` (TargetType.Closest) directly — nearest player, host & client symmetric. The existing `BalanceCoopEnemyTargeting` hostilesInLOS re-pick is skipped for Endless (it doesn't exclude invulnerable players and would undo Req 2). (Req 2) A player picking a card is invulnerable; `GrabHostileUnit` already excludes invulnerable units, so the **host** selector drops off aggro for free, and its sticky Closest cache is invalidated when the cached target is now-invuln so enemies redirect. For a **client** selector, its ghost lives on the host: new `EndlessCardSelect` (msg **90**, client→host) marks/unmarks that client's ghost unit invulnerable (`SetPeerCardSelectInvuln`) around its card-select bubble, so the same exclusion applies. `ProtocolVersion 15→16`. `ReverseProbePatches` (GetTarget endless prefix + override-target reflection + balance skip), `EndlessSyncManager` (`EndlessActive`, `SetPeerCardSelectInvuln`, client send in card-select open/clear), `NetEndlessCardSelect`(+Codec), `NetMessageType`(=90)/`NetService`/`NetGameplaySyncBridge`/`NetHandshake` plumbing. | **verified in-game** (user "独立模式已完全完工 完全符合预期") · built 0 error · committed `2c32530` |
| **EM-6a** | **Shared-mode card-select pause window — client mirror (base for EM-6, no new protocol).** First slice of Shared progression. When the host's shared XP pool crosses the threshold it enters the vanilla card panel with `SetTimeScale(0)` (host frozen). The host's `transitionState` is already synced to the client every EM-3 wave-state snapshot (discrete changes send immediately, 1s keepalive re-asserts), so the client freezes/resumes off that single authoritative edge — a `ApplySharedPauseEdge` on the applied `TransitionState` calls `SetClientTimeScale(0/1)` when it enters/leaves `CardSelection`. **No new message.** Compatible with Phase 5.7-NP by construction: NP only blocks pause *padlocks* (Inventory/Paused/DevTools/Dialog); the card-select freeze is a `SetTimeScale` change (`gameState` stays Running), which NP never touches — so both ends pause together and boss timelines stay aligned. `Reset` clears any mid-freeze (`ClearSharedPause`) so a run boundary never leaves the client stuck. This is an intermediate: the host still picks a card solo (client waits frozen); the shared 1-of-N **card roll broadcast + vote** is EM-6b and shared **card-effect application** is EM-6c. `EndlessSyncManager` (ApplyHostWaveState shared-pause edge + client time-scale helper + Reset). | **verified in-game** (user "两端一起冻结一起恢复，符合预期") · built 0 error · committed `9c4556b` |
| **EM-6b-1** | **Shared-mode card manifest capture + broadcast (data spine for the shared card vote).** The host owns the card RNG (`EndlessModeManager.gameplayRandom`); the client's Endless manager is a slave with a diverged stream, so it can never roll the same cards — the host must broadcast the authoritative set. `FloatingCardManager.cardsSetup` is true exactly while the panel is up, so its rising edge (polled each service tick in Shared mode on the host) is the canonical "cards ready" signal: the host reads `spawnedCards[]` / `cardRewards[]` and builds a manifest (per card: `cardKey`, static-flag for the Skip/Reroll tail cards, host-rendered title + description) and broadcasts it once per event (`NetEndlessCardManifest`, msg **91**, ReliableOrdered, `CardEventId` monotonic). This first slice logs the manifest on the client — validating the capture point + wire before the mirror panel + vote (EM-6b-2/3) are built on it; the pick flow is unchanged. `ProtocolVersion 16→17`. `EndlessCardManager` (new: capture/broadcast/apply + FloatingCard reflection), `EndlessSyncManager` (`ResolveEndlessInstance` shared accessor), `NetEndlessCardManifest`(+Codec), `NetMessageType`(=91)/`NetService`(broadcast/handle/dispatch/tick)/`NetGameplaySyncBridge`/`NetHandshake` plumbing, `ReverseProbePatches` (Reset). | **verified in-game** (user — host & client log identical card key lists across events) · built 0 error · committed `c5bd44d` |
| **EM-6b-2** | **Shared-mode 3D card mirror via roll-state replay (plan A — perfect fidelity, full vanilla reuse).** User chose real 3D floating cards on the client over a 2D list. The client's card RNG (`gameplayRandom`) + card-manager selection state are diverged, so instead of re-rendering, the host captures its exact pre-roll state and the client sets that state and runs the **unmodified** vanilla roll — reproducing byte-identical cards (keys, preselected items, artwork). A `FloatingCardManager.SpawnCards` **prefix** on the host captures (before `SetupCards` consumes RNG — the card window suppresses enemy spawns, the only other `gameplayRandom` consumer): `Unity.Mathematics.Random.state`, `ChoiceDrawAmount`, rerolls/banishes remaining, and the selection lists (`selectedOneTimeUsed`/`banishedCardKeys`/`forcedPreselectCardKeys`/`removedCardItems`/`banishesUsedThisSession`), broadcast as `NetEndlessCardRoll` (msg **92**). The client writes that state (RNG via box-mutate-unbox of the struct field), forces card-count parity with a `ChoiceDrawAmount` getter postfix, and invokes `SpawnCards`. The client's own pick is blocked (prefix on `OnPlayerFired`/`OnPlayerInteracted` while replaying — the pick becomes a vote in EM-6b-3); the panel is torn down (`ExitCardSelection` + destroy cards) when the host leaves CardSelection (the EM-6a resume edge). A client tick cross-checks the replayed keys against the EM-6b-1 manifest (`match=True`). `ProtocolVersion 17→18`. `EndlessCardManager` (roll capture/apply/teardown/verify + RNG/selection-state reflection), `EndlessSyncPatches` (SpawnCards prefix, ChoiceDrawAmount postfix, card-pick block), `EndlessSyncManager` (teardown on resume edge), `NetEndlessCardRoll`(+Codec), `NetMessageType`(=92)/`NetService`/`NetGameplaySyncBridge`/`NetHandshake` plumbing. Independent mode untouched (all gates require Shared + a client replay). | built 0 error · pending real-machine test |
| **EM-6b-3a** | **Shared-mode 1-of-N card vote + apply + on-card voter stamps.** Builds on 6b-2 (both ends show the same 3D cards): the vanilla "whoever fires first picks" becomes a host-authoritative group vote. When the host's card panel opens, `EndlessCardManager.HostTick` opens a vote for the ordinary reward cards (`EndlessCardVoteManager.HostOpenVote`, votable = the non-static cards; Skip/Reroll + banish are deferred to 6b-3b/c). Both ends cast by aiming at a card and firing — the existing card-pick prefix (`CardPick_Pre`, now given `__instance`) routes the pick into a vote cast on **both** roles (host records locally, client sends `EndlessCardVoteCast` msg **94**) and blocks the vanilla pick; re-castable. The host owns the tally + clock and broadcasts `EndlessCardVoteState` (msg **93**, tally + phase + resolved index, monotonic revision). Resolution: strict majority; tie → host roll; nobody voted → host uniform roll; resolve early when everyone has voted. **The countdown starts only after the first cast** (`TimeoutActive`) — early deliberation is unbounded (user decision). On resolve both ends run vanilla `SpinAndDismissCard(resolvedIndex)` (`EndlessCardManager.ApplyResolvedPick`, guarded once per event): personal card effects land on each `PlayerUnit`; **world-card duplication (companion/shop) is the known EM-7 gap.** Feedback = **on-card stamps** (`EndlessCardVoteOverlay`): each voter's name is cloned from the card's own subtitle TMP and stamped on the card they voted for — bold, per-voter random tilt + rect-relative jitter, gold ink for the local player / red for teammates — plus a bottom-centre status hint (prompt → "waiting n/m — Xs" → resolved). Stamp font size = the subtitle's **auto-size floor** (`fontSizeMin`), not `fontSize*1.5` — the card subtitle auto-sizes, so `fontSize` returns its large max and a short name ballooned ~10×. A **tie/no-vote roll** plays a short decelerating **raffle sweep** across the cards (both ends, `RaffleActive`/`RaffleCursorIndex` → the overlay pulses the cursor card's scale, `FrameTick` applies the winner when the sweep lands) so a rolled pick never snaps in instantly (`ResolvedByRoll` on the snapshot; a clear majority still applies at once). 0-ordinary-card panels open no vote (host falls back to a vanilla Skip/Reroll pick; client follows via the EM-6a teardown edge), so no soft-lock. **Deterministic client cards:** the 6b-2 roll replay diverged when a card's item pool depended on per-save state — chiefly `CardReward.IsStorageUnlocked` (storage/service cards) plus stale pools — because an empty pool skips a `SelectAvailableItem` RNG draw and desyncs the stream. Before every shared roll, both ends now rebuild all card pools (`FloatingCardManager.InitializeSession`) with the storage/service unlock filter forced open (`IsStorageUnlocked` prefix gated by `ForceStorageUnlocked`), so the pools are identical, the roll is deterministic, and the client renders each card **natively** (its own language + correct artwork). A manifest-reconcile (correct `CardReward`/title/desc/artwork by key) remains as a safety net if anything still diverges. Trade-off: shared-Endless storage/service cards offer all storages regardless of unlock. Feedback = **on-card voter stamps** (name cloned from the card's subtitle TMP, height-matched via `bounds`, gold/red ink, black SDF outline) + a bottom-centre status hint. A tie/no-vote roll plays a **raffle sweep** reusing the native card highlight (`FloatingCard.SetSelected` border+enlarge) across only the tied cards, landing on the winner. `ProtocolVersion 18→19`. Localization row 31 (`endless.cardvote.*`). `EndlessCardVoteManager` (new), `NetEndlessCardVote`(state+cast +Codecs, new), `EndlessCardVoteOverlay` (new), `EndlessCardManager` (open hook + selection/panel/spin/raffle-highlight/pool-reset/reconcile helpers + Reset), `EndlessSyncPatches` (`CardPick_Pre` → vote, `FloatingCardManager.Update` raffle-highlight postfix, `IsStorageUnlocked` prefix), `NetMessageType`(=93/94)/`NetService`(send/broadcast/handle/dispatch/tick + FrameTick)/`NetGameplaySyncBridge`/`NetHandshake` plumbing, `Plugin` (overlay tick). Independent mode + single-player untouched (all gates require Shared + an open card vote). | **verified in-game** (Log457 — votes / re-casts / tie-roll resolve identically; deterministic roll `match=True` on unlock-gated cards; stamps + outline + raffle correct) · built 0 error |
| **EM-6b-3b** | **Shared-mode card vote: Skip + Reroll are votable options (host-authoritative reroll).** Extends the 6b-3a group vote from ordinary reward cards to the whole panel — the votable set is now every **interactable** card (ordinary + Skip + Reroll), computed by `EndlessCardManager.GetVotableIndices` from `FloatingCard.Interactable`, so an exhausted **0-reroll card is excluded** (`ConsumeReroll` has no floor, and a dead reroll must never be a tie/no-vote roll target). `HostOpenVote` now takes the full card count plus that votable-index array: casts and the no-vote uniform roll are both restricted to it (`IsVotable`). `TryReadAimedOrdinaryCard` → `TryReadAimedVotableCard` drops the static-card rejection and instead requires `Interactable` (the DismissButton banish is still deferred to 6b-3c). **Resolution by outcome:** a **Skip** win runs the vanilla `SpinAndDismissCard(skipIndex)` on both ends — its `PassForStamps` reward is personal, so each player gets their own stamps and the panel closes. A **Reroll** win is **host-authoritative**: the host runs the vanilla spin, whose terminal `SpawnCards` re-rolls the panel and — through the existing 6b-2/6b-1 chain (`SpawnCards` prefix → `HostCaptureRoll` broadcast, `HostTick` manifest + new `HostOpenVote`) — issues a fresh roll/manifest/vote under a new `CardEventId`. The **client does not** run that spin (its terminal `SpawnCards` would roll a divergent local panel and could collide with the incoming host roll mid-coroutine); instead `ApplyResolvedPick` spins the reroll card for feedback and keeps `ClientRollActive` set so the host's new roll tears the panel down (`TeardownClientCards`) and swaps in the authoritative one via `ApplyCardRoll`. A `FloatingCardManager.SpawnCards` **client prefix** now suppresses any non-replay client card spawn in Shared mode (only our host-driven replay, which sets `ClientRollActive` first, is allowed) as defence in depth. Status HUD names the Skip/Reroll outcome (`DescribeResolvedCard` → `endless.cardvote.resolved_skip`/`_reroll`, loc row 31). `ProtocolVersion 19→20` (vote semantics changed even though the wire is unchanged). **Reroll panel-capture fix:** the manifest capture originally polled the `cardsSetup` rising edge, but a reroll's terminal `SpawnCards` flips `cardsSetup` false→true inside one frame — the per-frame poll never observed the gap, so the new panel was never captured and no vote opened (cards appeared, un-votable, overlay stuck on "Rerolling…"). Fixed by keying capture on the `spawnedCards` **array reference identity** (a fresh array per panel, chained reroll included) instead of the edge. `EndlessCardManager` (votable-index/interactable/reroll/spin/describe helpers + reroll-aware `ApplyResolvedPick` + array-identity capture), `EndlessCardVoteManager` (votable-set gating), `EndlessSyncPatches` (`SpawnCards_Pre` client suppression), `EndlessCardVoteOverlay` (resolved status), loc rows (en/zh/ja), `NetHandshake` (v20). Independent mode + single-player untouched. | **verified in-game** (Log459 — Skip/Reroll votable; consecutive rerolls each open a fresh vote; all 11 client events `match=True`, no reconcile, no exceptions; user "完美通过") · committed `5a6a282` |
| **EM-6b-3c** | **Shared-mode card vote unified: pick / Skip / Reroll / banish are one vote, plus per-option retract.** Adds banishing a card as a competing option inside the existing Endless card-vote channel (not `CoopVoteManager`, per the reviewed decision: that subsystem is payload-less, Y/N-keyboard, single-vote, and its Majority path is untested; the Endless channel already has a host tally, participants, a realtime clock that survives the frozen card-select world, and the on-card overlay). Per the user's revision, **every player casts exactly one vote** — pick a card (ordinary/Skip/Reroll) OR banish a card — and **any option is a toggle** (re-cast to retract). Aiming at a card's **dismiss button** and firing is a banish vote; the game only offers that button on a non-static, non-`TravelBackToChurch` card while shared banishes remain (natural gate; host re-validates). Resolution counts every cast option (a pick and a banish of the same card are distinct), takes the most-voted, and breaks a tie by a single host roll across **all** tied options (so a 4-way pick/Skip/Reroll/banish tie is one uniform draw). The tie-break **draw is host-authoritative and reused for every rolled outcome and every banish** (a clear pick still applies at once): the host plays a short draw (a multi-card sweep, or a single-card blink for a clear banish / a same-card pick-vs-banish tie) with the winner hidden, broadcasts it via a new snapshot `RaffleActive` flag + `TiedIndices`, and both ends render it (the on-card stamps stay visible through the draw, which is what lets the *last* banish vote's ✕ show before its card leaves). When the host timer elapses it applies the winner. A **pick/Skip/Reroll** winner resolves the panel (both ends run `SpinAndDismissCard`); a **banish** winner runs the vanilla `FloatingCardManager.DismissCard` (consuming one shared banish, updating the pools that feed future rolls via the roll-state replay, animating the card out; clients mirror via `ClientMirrorBanish` = StartDismissal + vanilla `RemoveCardAfterDismissal` + local banish decrement + text refresh, retried if the panel isn't ready) and **re-opens the same vote on the remaining cards** (all votes cleared; Skip/Reroll are never banishable so the panel can't empty). The winning card's ✕ stamp is released (not destroyed) so it rides the dismissing card out. The **countdown runs only while ≥1 vote is cast** — retracting to zero stops it and the next cast restarts it. Protocol: `NetEndlessCardVoteCast` gains a `Kind` byte (0 pick / 1 banish); `NetEndlessCardVoteState` gains a per-participant `BanishIndex`, a cumulative `BanishedIndices[]`, and a `RaffleActive` flag (codecs → v3); no new message type. Feedback: a violet **✕ NAME** stamp on the card each player is voting to banish (distinct from the gold/red pick stamps). `ProtocolVersion 20→22`. `NetEndlessCardVote` (DTO/codec), `EndlessCardVoteManager` (unified tally/resolve + toggle + votes-gated countdown + host-driven draw + banish continue + client mirror), `EndlessCardManager` (banish read/execute/mirror helpers + reflection: `DismissCard`/`RemoveCardAfterDismissal`/`StartDismissal`/`UpdateBanishText`/`TravelBackToChurch`), `EndlessCardVoteOverlay` (banish stamps + ride-out release + vote count), `NetService`/`NetGameplaySyncBridge` (cast `Kind`), `NetHandshake` (v22). Independent mode + single-player untouched. Follow-up polish (client-only, no protocol change): the draw now decelerates onto and **holds** the winning card raised (it was only mapping to the winner on the final frame → looked like it rested on the wrong card); a reconciled card renders its text from the corrected reward's own **client-localized** `displayName`/`description` where possible (was showing host-language strings). | **verified in-game** (user — unified vote / banish / draw / retract / votes-gated countdown, then the draw-rest + localized-fallback polish) · committed `e42fe21` + `c7a45e7` |
| **EM-7a** | **Endless world-card LOOT routed to a single authority (Shared mode) — fixes doubled card loot.** A resolved card runs `FloatingCardManager.ExecuteReward` on both ends (the shared vote applies the pick on each), so a world reward that spawns an object duplicates it. This first EM-7 slice dedups the loot-table reward's plain-Pickup path: `SpawnFromLootTable` with `containerPrefab == null` goes through `InteractionManager.SpawnPickup`, which the WorldPickup (WID) pipeline already mirrors — so the **client** suppresses its own copy (an `ExecuteReward` prefix gated on linked-client + Shared mode + the loot-Pickup reward; `ExecuteReward` is fire-and-forget so the prefix returns a completed `Task`) and the **host** tags its spawn (`WorldPickupManager.EndlessSharedLootContext`, cleared in the postfix after the synchronous loot branch) so the pickup mirrors to the client **regardless of the SharedLoot session toggle** — card loot has no `inventoryData`, so it otherwise fails the Independent-mode WID filter and the client would see nothing. Container-path loot, companions, shop NPCs, and interactables remain per-end (later EM-7 slices). No protocol change (reuses the WorldPickup mirror). `WorldPickupManager` (`EndlessSharedLootContext` + `Qualifies`), `EndlessSyncPatches` (`ExecuteReward` prefix/postfix + `CardReward.rewardType`/`containerPrefab` reflection). Independent mode + single-player untouched. | **verified in-game** (user — card loot no longer doubles; host & client show one shared pickup at the same position) · built 0 error · committed `597306d` |
| **EM-7b** | **Endless card-spawn locator beam synced (Shared mode).** The vanilla `FloatingCardManager.lootLightEffect` is a single shared light pillar that marks where a card spawned loot / chests / NPCs. It is now host-authoritative and mirrored so the client sees the same locator: the client suppresses its own local `SpawnLootLightEffect` (a linked-client prefix, Shared mode) and the beam's active state + position ride the existing `NetEndlessWaveState` (86) snapshot (codec v1→v2, reliable host→all) — the host detects a beam change and sends immediately, the client applies position + active in `ApplyHostWaveState`, so it appears/moves/disappears in sync. Independent mode keeps each player's local beam (world-card spawns are separate there). `ProtocolVersion 22→23`. `NetEndlessWaveState`(+beam fields)/`NetEndlessWaveStateCodec`(v2), `EndlessSyncManager` (beam reflection + host read/change-detect + client apply, gated diagnostics), `EndlessSyncPatches` (`SpawnLootLightEffect` client prefix), `NetHandshake` (v23). | **verified in-game** (Log470 — client beam ON at the host's position, persists; user "两个物品都是同步的，可以过了") · built 0 error · committed `1b1dd0e` |
| **EM-7c** | **Endless companion cards mirrored as a single host-authoritative ally (Shared mode) + charmed presentation on the client.** A `SpawnRandomAllies` card spawns allies via `FloatingCardManager.SpawnCompanion → SpawnUnitAsync(this,…)`; that owner is now classified in `RuntimeSpawnManager.ClassifyOwner` so the host's companion is broadcast and the client mirrors it as a puppet instead of each end spawning its own. Because `FloatingCardManager` also spawns shop NPCs (`SpawnNPC`), the host brackets `SpawnCompanion` with a depth counter (`HostCompanionSpawnDepth`) so **only companion spawns** are classified (`"EndlessCompanion"`) — shop NPCs are left per-end for EM-7d. The client suppresses its own companion spawn (`ExecuteReward` prefix, extending the EM-7a world-reward classifier). A companion is a **charmed** ally on the host (`ApplyForcedCharmed` = heart symbol + allied faction), which the position/animation puppet mirror doesn't carry; since `NetRuntimeSpawn.Source` already travels on the wire, the client re-applies that presentation on the mirrored puppet (the puppet's AI is suppressed, so `forcedCharmingOwner` is inert — visual only). No protocol change for companions. `RuntimeSpawnManager` (FloatingCardManager classify + `MirrorSpawnAsync` source param + `ApplyEndlessCompanionCharm`), `EndlessSyncPatches` (`SpawnCompanion` bracket + companion in `ClassifyWorldReward`/`ExecuteReward_Pre`). | **verified in-game** (Log468/470 — one companion mirrored `bound=True`, `src=EndlessCompanion`; user "友方单位正常"; heart-charm reflection fixed after Log470's arity mismatch, pending visual re-confirm) · built 0 error · committed `1b1dd0e` |
| **EM-7d-1** | **Endless shop-NPC card mirrored as one host-authoritative vendor + set up on the client (Shared mode).** A `SpawnNPC` card spawns a shop vendor via `ExecuteReward`; it is now classified in `RuntimeSpawnManager.ClassifyOwner` so the host's shop is broadcast and the client mirrors it as a puppet instead of each end spawning its own. `SpawnNPC` has no dedicated method (it is inline in `ExecuteReward`), so the host brackets `ExecuteReward` with a depth counter (`HostShopSpawnDepth`) — its first `await` is the shop's `SpawnUnitAsync`, which `NotePendingSpawn` observes synchronously — so only shop spawns are classified (`"EndlessShop"`, distinct from companion; a multi-NPC card mirrors only its first, accepted). The client suppresses its own shop spawn (the EM-7a world-reward `ExecuteReward` prefix, kind 3). Because the client suppresses the host's post-spawn setup, the mirrored puppet has the shop components but isn't wired up; on mirror (`ApplyEndlessShopSetup`) the client registers the `UnitInteractable` with the `InteractionManager` and runs each `ServiceStation.DoSetup` (virtual → the concrete vendor), so the shop is openable + usable. Purchases are local (per the design decision — one host-authoritative NPC, independent buying). No protocol change (reuses RuntimeSpawn + the existing `Source` field). `EndlessSyncPatches` (SpawnNPC in `ClassifyWorldReward`/`ExecuteReward` bracket), `RuntimeSpawnManager` (shop classify + `ApplyEndlessShopSetup`). **Open:** vendor stock is rolled per-end by `DoSetup` (`AddItems(VendorTable)` → `UnityEngine.Random`); it matches only for deterministic vendors — a random `ShopKeeper` would need a host-authoritative stock broadcast (EM-7d-2), added only if a divergent vendor shows up. | **verified in-game** (Log472 — `Unit_Trader_Fex` mirrored `bound=True`, `src=EndlessShop`, `applied shop setup (2 stations)`, usable both ends; user "完美过了") · built 0 error · committed `7a1a846` |
| **EM card fixes (#2a/#2c)** | **Two Endless card-correctness fixes (no protocol change).** **#2a — infinite-ammo / indestructible expiry on the client:** their expiry check lives in `StartEnemySpawning`, which a linked client suppresses (EM-1), so the flags (read per-player from `EndlessModeManager.Instance` when firing / taking durability loss) never cleared — the effect lasted forever on the client. The client now expires them off the host-synced wave counter (`ApplyClientFlagExpiry` in `ApplyHostWaveState`, both modes); the grant wave matches on both ends (the counter is host-synced), so the thresholds agree. **#2c — IncreaseXPAmount multiplier:** vanilla applies it as `orbValue * xpMultiplier`, but our XP path awards base `ExperienceOnKill` and skips it, so the card was inert in co-op — the host now scales the **shared-mode** award by the shared multiplier (`HostXpMultiplier`). `IncreaseXPRange` (pickup radius) already worked in Shared mode (the EM-5b `PickupTick` collect reach); it's inert in Independent by design (force-collect ignores distance, same both ends). Independent-mode per-killer XP multiplier is a documented gap (the host doesn't know a client's multiplier). `EndlessSyncManager` (expiry reflection + `ApplyClientFlagExpiry` + `HostXpMultiplier`), `EndlessSyncPatches` (`OnEnemyDied_Post` shared-award scaling). | **verified in-game** (Log473 — `#2a client infinite-ammo expired` fires on the client, no errors; user "完全没问题") · built 0 error · committed `aed9378` |
| **EM-7e** | **Endless card-spawned non-unit interactables (chests / storage / service stations) mirrored (Shared mode) — fixes "the client chest is at the wrong position and can't be opened".** These are plain `Interactable`s created with `Object.Instantiate` (not Units) at a player-motion-dependent `GetRandomSpawnPositionInView`, so the RuntimeSpawn pipeline can't carry them and each end spawned its own at a diverging spot; the locator beam (host-authoritative EM-7b) was already correct, so the chest sat away from its beam and the SL-2 `ChestSyncManager` logged `no chest near`. Two paths: the actual card **chest** is `SpawnFromLootTable` + `containerPrefab` (`ExecuteReward` kind 5, a `Container` with `StaticLoot`), and `SpawnInteractable` (kind 4, storage/stations) rides the same mirror. Prefab identity travels **without RNG parity**: the prefab is a serialized reward field (`containerPrefab` / an `itemPool` entry), so the host broadcasts the spawned root's `Object.name` (`(Clone)` stripped) + `cardKey` + the Container's `StaticLoot` ItemId, and the client resolves `CardReward(cardKey)` and matches by name (`NetEndlessInteractable`, msg 95). The **host** runs the vanilla branch (correct spawn + wiring + arena-transition cleanup), bracketed by `EndlessInteractableManager.HostArmCapture`/`HostCaptureAndBroadcast` (synchronous branch → the postfix reads a fully-populated `spawnedInteractables`); only roots (parent == the Endless manager transform) are broadcast. The **client** suppresses its own spawn (the EM-7a world-reward `ExecuteReward` prefix, kinds 4 & 5), instantiates the resolved prefab at the host position, restores `Container.StaticLoot` from the ItemId, and re-runs the vanilla setup. **Chest open/loot then works for free:** the mirrored `Container` auto-registers with `ChestSyncManager` in `Start`, so at the host position SL-2's position-keyed open matches it. The client's slave manager never runs `ArenaTransitionRoutine`, so `EndlessInteractableManager` tracks mirrors and destroys them on the host-synced `ArenaTransition` edge + run reset. `ProtocolVersion 23→24`. `NetEndlessInteractable`(+Codec), `EndlessInteractableManager` (new), `EndlessCardManager` (`ResolveInteractablePrefab` + `itemPool`/`containerPrefab` reflection), `RuntimeSpawnManager` (`ResolveItemDefinition`), `EndlessSyncPatches` (kinds 4 & 5 in `ClassifyWorldReward` + host arm/capture), `EndlessSyncManager` (transition-edge cleanup + Reset), `NetService`/`NetGameplaySyncBridge`/`NetMessageType`(=95)/`NetHandshake`(v24). Independent mode + single-player untouched. | **verified in-game** (Log475 — kind 4 `BulkAmmoBox` + kind 5 `Chest (Armor)` mirrored at the host position, `[ChestSync]` open now matches: `mirror peer=host OPENED … near (62.6,-0.1,-9.9)`; user "成功同步位置并且同步打开了") · built 0 error |
| **EM corpse cleanup** | **Client sinks Endless wave-enemy corpses (both modes) — fixes corpses piling up on the client.** Vanilla Endless sinks each `SetEnemy` wave enemy's corpse into the ground after death (`OnEnemyDied` → `Npc.SinkIntoGroundEndless`) to avoid perf-killing corpse accumulation; companions / shop NPCs / bosses do not. On the client the wave enemies are host-mirrored puppets whose slave manager never wired that callback, so their corpses leaked. The runtime-spawn mirror tags puppets with source `"Endless"` (= exactly the wave enemies) via `EndlessSyncManager.NoteEndlessWavePuppet`; when such a puppet dies (from the runtime-spawn death replay in `NetGameplayProbeManager.TryApplyMatchedNpcDeath`), `OnClientPuppetDied` runs the same vanilla `SinkIntoGroundEndless` on it (removed from the set = at-most-once). Local per-end presentation/perf, never authoritative; applies in both progression modes; cleared on run reset. No protocol change. `EndlessSyncManager` (puppet-id set + sink reflection), `RuntimeSpawnManager` (`MirrorSpawnAsync` source "Endless" tag), `NetGameplayProbeManager` (death-replay hook). | built 0 error · ran alongside Log475 with no errors; corpse sink not separately observed |
| **IND-1** | **Independent-mode client companion routed to the host (#3, companion-only slice).** In Independent mode each player picks its own cards and spawns world objects locally; a per-type analysis found only the **companion** is functionally broken (a client-local companion can't damage the host-authoritative enemy puppets, so it can't fight — shop/chest/station/plain-loot work locally as personal objects, and routing them would make Independent-mode rewards shared, contradicting the mode). The client's `SpawnCompanion` prefix (Independent + linked client) reads the resolved `UnitSO` id, sends `NetEndlessWorldCard` (msg 96, client→host), and suppresses the local spawn. The host spawns the companion via the game's own `SpawnCompanion` (reusing the EM-7c `HostCompanionSpawnDepth` RuntimeSpawn bracket + all vanilla stat mods/onDeath) with the charm target redirected to the **requesting peer's ghost unit** so it follows the picker — a consume-once flag read by an `Npc.ApplyForcedCharmed(Unit,bool)` prefix that swaps arg 0 via `__args` (survives `SpawnCompanion`'s await as a static field; no-op for every other charm; falls back to the host's charm if the ghost isn't resolved). The companion mirrors back as a puppet (`"EndlessCompanion"`) and the picker's client re-charms it to its own player (EM-7c). `ProtocolVersion 24→25`. `NetEndlessWorldCard`(+Codec), `EndlessWorldCardManager` (new), `EndlessSyncPatches` (`SpawnCompanion_Pre` client route + `ApplyForcedCharmed_Pre` charm redirect), `RuntimeSpawnManager` (`ResolveUnitSOPublic`/`ReadUnitIdValuePublic`), `NetService`/`NetGameplaySyncBridge`/`NetMessageType`(=96)/`NetHandshake`(v25). Also fixed a pre-existing EM-7c cosmetic bug the IND-1 log surfaced: the client's mirrored-companion heart visual (`RuntimeSpawnManager.ApplyEndlessCompanionCharm`) looked for a 1-arg `ApplyForcedCharmed` that doesn't exist (only `ApplyForcedCharmed(Unit,bool)` does), so the heart never applied — now resolves the 2-arg overload with `applyHeartSymbol: true`. **Known:** companion spawns at the host and walks to the picker; 3+ players charm the non-picker mirror to its own player (visual). Shared mode + host picks + single-player untouched. | **verified in-game** (Log477 — client `IND-1 client routed companion unitId=15`, host `IND-1 host spawned companion for peer=client-1 … charmTo=picker` + `RuntimeSpawn host broadcasting … src=EndlessCompanion`, no errors; user "貌似没问题") · heart-visual fix built, not separately re-tested · built 0 error |
| **CZ-DeadIntroLock** | **Fix the 1.1.1 fixed-repro "every non-host player frozen after the Cousin dies" (field reports ×3).** Field-log proof: on the client the cousin fights as an enemy puppet, so the fight-start `Trigger()`'s `triggeredByPlayer=true` is never consumed by its (suppressed) behavior tree; the unit's death releases the puppet, the tree resumes, sees `triggeredByPlayer && !introPlayed` and runs `Introduction()` — taking the **Cinematic controller-lock + invulnerability** — in the ~1s window before the host's `CousinDeath` broadcast lands (`terminal` not yet set, so the faithful-intro gate let it through); the paired release lives in `StartFight()`, which the terminal gate then blocks → the player can't move/shoot/open inventory until the host changes scene (locks are scene-scoped). The host is immune (`introPlayed=true`). Three-layer fix, no protocol change: ① the terminal gate treats a **dead local health unit** as terminal (`IsHealthUnitDead` — the local death is canonical; no need to wait for the broadcast), closing the race window for every boss family; ② blocking a start source on a terminal/dead encounter now calls a new adapter hook `TryReleaseStalePreFightLocks` (Cousin: release Cinematic lock+invuln iff `introPlayed`, mirroring native `StartFight`'s release — `ModifyControllerLock` is set-semantics so a no-lock release is a no-op); ③ the client's `CousinDeath` apply inoculates the resumed tree (`triggeredByPlayer=false`, `introPlayed=true`) and releases any lock the tree already took. `IBossEncounterAdapter`/`BossAdapterBase` (+hook), `CousinHelperAdapter`, `NetBossEncounterManager` (terminal gate). | built 0 error · pending in-game verify |
| **HTG-DupComplete** | **Suppress a duplicate host `CompleteLevel` while a transition is already in flight — the same-seed different-layout caves desync root (field: "uncrossable path / seemingly different level").** Field-log proof (Caves:2, seed −70890021): a client relay led the transition (host `GoToLevel` running, guard active since `host-GoToLevel`), then the host player walked into the exit during the fade window → its native `CompleteLevel` started a SECOND `SwitchLevelRoutine`; two generations interleaved (two `LevelStepTrace` chains, double stalagmite spawns, two seed rolls), the host's level was built from mixed generator state, and the clients' clean load of the final seed produced a **different layout** (host `rooms=1` vs client `rooms=2`, different `genHash`, same seed). All matching levels that session were single-path; both diverging ones were double-path. Fix: `GM_CompleteLevel_Pre` blocks a host `CompleteLevel` when `NetHostTransitionGuard.IsActive` **or** `gameState==Loading` (guard covers [transition start → finalize]; Loading covers [finalize → level running]) — the in-flight transition already saves + advances, so the duplicate has no side effect worth keeping. `ReverseProbePatches.cs`. | built 0 error · pending in-game verify |
| **Relay-CatchUp** | **Reload-in-place now requires the relaying client to BE in the host's scene; a lagging client's catch-up relay is answered with the current scene request instead of a re-generation.** The F3-Reload host rule assumed "a client merely catching up never relays" — the field log disproves it: a client still on Caves:1 walks into the exit after the host already moved to Caves:2, its gate relays `target=Caves:2`, and `SameScene(host, target)` alone classified that as a reload-in-place → the host would re-generate, with a fresh seed, the level everyone else is already playing (this session it was only saved by the loading-defer racing first). Fix: `reloadInPlace = hostAtTarget && clientInHostScene`; the new `hostAtTarget && !clientInHostScene` branch logs `catch-up` and re-sends the current finalized `HostSceneRequest` to that peer (`SendCurrentHostSceneRequestToPeer`). The misleading `reload-in-place lead` log line also moved after the defer checks (it used to print before them, logging leads that never executed). True F3 same-scene reloads (client==host==target) unchanged. `NetService.HandleClientTransitionRequest`. | built 0 error · pending in-game verify |
| **TOAST-JoinFlow** | **Join toasts tell the truth: "Connecting to host…" on every join route, "Linked to host" no longer fired by the button press.** The join buttons link synchronously before the handshake (by design — Log185), and `NetLinkState.SetClientLinked` toasted unconditionally, so "Linked to host" popped the moment ANY join button was pressed, success or not (and the invite-accept route showed nothing at all while connecting). Now: `NetConnectFeedback.BeginAttempt` (the single convergence point of Direct-IP, Steam-ID and invite joins) toasts `toast.connectingToHost` on the false→true transition (the Steam path calls it twice); `SetClientLinked` suppresses the link toast while an attempt is in flight — a successful join is announced by the existing "Connected to {host}" (handshake-accept), and "Linked to host"/"Playing solo" remain for the mid-session PageDown/PageUp toggles where they are accurate. New string ×14 (Localization.md row 33). `NetConnectFeedback.cs`, `NetLinkState.cs`, `lang/*.json`. | built 0 error · pending in-game verify |
| **STEAM-2b** | **Smooth Steam joins: relay bridge starts with the room, stray session requests fail fast, Steam connect window widened, SDR pre-warmed (Log464).** Log464 exposed three compounding causes of "Steam ID join says connection failed / invite join takes ~10s / it magically works after the host presses Invite": ① the relay bridge only started when "Invite Friends" was pressed — a session request before that was **ignored** (`Steam hosting not enabled`), so an ID-join could never succeed until the button was pressed (the button's side effect, not the invite, is what "fixed" it); ② an ignored request was left pending, so the requester hung until Steam's ~20s "App did not respond" timeout; ③ a Steam join's first datagram triggers the P2P session negotiation (NAT punch / SDR relay, seconds when cold), which regularly outlived LiteNetLib's default 5s connect window (10×0.5s) → first attempt `ConnectionFailed` → our 5s backoff → attempt 2 connects instantly = the observed ~10s. Fixes: `CoopConnection.Apply` starts `SteamRelayBridge.StartHosting` right after a successful Host start (acceptance only — rich presence + the overlay stay strictly on the invite button, and every session still lands in the key+version-gated LiteNetLib handshake, so the deliberate "don't advertise unless asked" stance is preserved while the deliberate opt-in for *acceptance* is dropped as the Log464 UX trap); `SteamRelayBridge.OnHostSessionRequested` now `CloseSession`s an ignored request (fast, deterministic failure for a joiner hitting a non-hosting peer); `NetService.SetConnectTarget` gains a `steamRelay` flag and `ConnectToHost` sets `MaxConnectAttempts` 30 (15s window) for Steam targets so the first connect rides through the negotiation (direct IP keeps 10); `SteamNetworkingSupport` calls `SteamNetworkingUtils.InitRelayNetworkAccess()` at startup (Valve-recommended async warm-up). `CoopConnection.cs`, `SteamRelayBridge.cs`, `NetService.cs`, `SteamNetworkingSupport.cs`. | **verified in-game** (user: 测试成功 符合预期) · committed `c0080c5` |
| **GEN-SnapLifetime** | **Keep the generation snapshot across networking sessions — fixes "joined the room but never teleported to the host's hub" (Log469, intermittent).** `NetService.Stop()` cleared `NetGenerationInputCapture`, but that state is derived purely from local generation events (GoToLevel/SwitchLevelRoutine/ResetContext) and describes the level that is STILL loaded — its lifecycle follows the level, not the networking session. A host who had started+stopped any networking (a join attempt, a re-created room) while sitting in the hub lost the boot-time hub finalized snapshot and could never re-finalize without a level load → every hub scene request took the `defer hub return until finalized` branch (`missing-finalized-hub-seed`, `autoLoad=False`, graph name showing the leaked `service-start-prime` diagnostic tag) **forever** → the joining client (properly linked via ui-join) skipped every request `reason=not-authorized` and was never pulled in; a client reconnect changed nothing. Log471 = identical flow without the prior stop → boot snapshot intact → `autoLoad=True` finalized request → instant follow. The intermittency criterion is exactly "did the host stop any networking after the hub loaded, before creating the room". Distinct from the LK-Hint/ONI unlinked case (that skip is client-side `not-linked`; this one is host-side `autoLoad=False` and LK-Hint's toast does not fire on `not-authorized`). Fix: drop the `Stop()`-time clear and delete the now-unused `Clear()` — each new generation supersedes the snapshot, and with no level loaded the request builder classifies the target as menu and never reads it. Known leftover (out of scope): `service-start-prime` still leaks into the run state's `LevelGenerator` via `ReportLevelSeed(seed, generatorName)` — cosmetic once the snapshot survives, tracked separately. `NetService.cs`, `NetGenerationInputCapture.cs`. | **verified in-game** (user: 成功无问题) · committed `990a2f1` |
| **LK-Hint** | **Unlinked-client join hint toast (field: "friend failed to join my church instance").** Field-log proof: the friend connected but never linked (`ClientLinked` default OFF by design — no solo-run hijack), so every hub auto-follow skipped silently (`not-joined-hub-request-waiting` / `not-linked`), leaving them invisible in their own same-named `ChurchHub:0` (different seed hides remote visuals) with no in-game clue the link key exists; they only got in after a full reconnect. The deliberate skip stays; it just becomes visible: `CoopToasts.NotifyLinkHint` (throttled 90s, respects the toast preference) fires from both skip sites showing the configured `ManualClientSceneFollowKey` — `link.pressToJoin`, localized ×14 (Localization.md row 32). `CoopToasts`, `NetClientLoadGate`, `NetClientJoinFlow`, `lang/*.json`. | built 0 error · pending in-game verify |
| **UI-VOTE** | **Vote HUD overlay + shared overlay widgets factored out of DR.** The DR overlay's two reusable primitives were promoted to `src/UI/Shared/`: `PerimeterProgressGraphic` (the rect-perimeter stroke, now with a `Clockwise` flag) and `VitalityAnimator` (float/breathe/tilt motion, now targeting a new `IVitalityPanel` interface); DR migrated onto them with identical behaviour. New `src/UI/VoteOverlay/` (top-center, below the boss health bar): `VoteOverlayPanelView` renders three stacked strokes from the single snapshot — yellow agree fraction clockwise; red = a full ring under the yellow on a Unanimous fail, or the decline fraction counter-clockwise in Majority — plus a `VoteTallyStrip` (one white/green/red square per player, local one outlined) and title/prompt/result text with the remaining-seconds countdown. Every end (pass/fail/cancel) is held 5s (host keeps the vote in its Resolved phase) then the overlay fades. Border and animation read one progress value (the DR single-source lesson). Ticked from `Plugin.Update`. Localization rows 26–28. | **verified in-game** (user — overlay renders and the vote reads correctly through it; DR migrated onto the shared widgets with no regression) · built 0 error |
| **GU183-1** | **Re-bind the damage patches to the game's real `ReceiveDamage` after 0.17.4 → 0.18.3 (Qiosk's Plenty).** The update moved the real damage implementation off the overload carrying `DamageTypes damageType` — `DamageSourceData.damageType` now holds it — leaving `Unit.ReceiveDamage(float, DamageSourceData, Hitmesh.Data, Vector3?)` as the implementation and `(float, DamageTypes, IDamager, Hitmesh.Data, Vector3?)` as a thin validating wrapper; `Npc` overrides only the implementation. Both prefixes bound `damageType` by name, so Harmony refused them with `IL Compile Error`: the `Unit` prefix survived only on the wrapper (native callers that build their own `DamageSourceData` bypassed it — reflect `Unit.cs:1899`, melee `Npc.cs:1725`), and `Npc_ReceiveDamage_Pre` never ran at all, cutting the sole call site of `TryClientBossHit` / `TrySendClientHitRequest` — the whole client→host hit chain. Fix binds **only** the real implementation (`FindRealReceiveDamage`: argument 1 is `DamageSourceData`), never the wrapper: one prefix run per hit (binding both would double-fire wrapper-routed damage), wrapper-skipping callers included, and a single known `__args` shape. `damageType` now comes from `DamageSourceData.damageType` via cached reflection (`ReadDamageType`), matching the existing `_dsdNameField` reader. **The argument-index trap**: the source sat at `__args[2]` in both old overloads, but in the new one index 2 is `Hitmesh.Data` and the source is index 1 (`DamageSourceOf`) — binding without moving it would have fed a hitbox to the fail-open `FriendlyFireClassifier`, forwarding friendly fire as enemy damage with FF off. A missing implementation is now `Log.Error` naming the dead capability, not a silent no-op. `ReverseProbePatches`. | **verified in-game** (both peers, zero `[DamageForward]` errors; damage lands; `type=7` reaches the victim, confirming the `DamageSourceData` read) · built 0 error · full rebuild 411 warnings = baseline |
| **GU183-2** | **Keep host ghost players out of the game's new manual player update (0.18.3 enemy-AI freeze).** Phase 5.7-B registers a headless `Player` per remote player into `GameManager.Players` so vanilla enemy detection sees them, and relied on `enabled = false` to keep vanilla per-player logic off a stand-in that carries only the fields detection reads. 0.18.3's manual-update refactor deleted `Player.Update` and replaced it with `Player.ManualUpdate`, which the new central driver `ProjectileSystem.Update` calls **directly** (`for (int i = 0; i < players.Count; i++) players[i].ManualUpdate();`) while its Burst job runs — and a direct call ignores `enabled`. Every frame a ghost reached `ManualUpdate` and threw an NRE on `inputReader` / `cameraRootAnimator`, aborting `ProjectileSystem.Update` mid-loop so `RunWhileWritingInstanceData()` — where `BehaviourTreeCode_Gameplay.ManualUpdate` (the enemy behaviour-tree driver), `PlayerHUD` and `GooEyeManager` register — never ran, and `_lastJobHandle.Complete()` never completed. Host enemy AI froze; clients saw it through their puppets. Host-only: the client injects no ghosts (client `Player.log`: 0 exceptions). Fix makes the skip explicit — a `Player.ManualUpdate` prefix returns false for a ghost, identified by `RemotePlayerRegistryManager.IsGhostPlayer` reusing the `_ghostGoIds` set that already backs `IsGhostCollider`. Real players are untouched and the check fails open, so a misidentification can never swallow a real update. Suppressing ghosts during load (`IsHostLoading`, the `ShowLevelNode` camera NRE) does not cover this — it fires in ordinary gameplay, exactly when the ghost must exist. `GhostPlayerPatches` (new), `RemotePlayerRegistryManager`, `PatchBootstrap`. | **verified in-game** (host `Player.log` NRE 12485 → **0**; enemy AI moves again; `[GhostPlayer] patched` + 3 skips) · built 0 error |
| **K-1** | **Projectile-path throwable (ThrowingKnives) flight visual sync (issue #10 — a thrown knife was invisible to other players).** ThrowingKnives take `ThrowableWeapon.Throw`'s `prefabToThrow == null` branch — a real `ProjectileSystem` projectile of type `Custom`, NOT a Breakable — so HZ-2/HZ-3 don't apply; the closer cousin is the bullet channel (WS-1). A throw-time probe (removed on landing) confirmed the knife's only visible body is the weapon's serialized `customVisuals` (`drawDefaultBullet == false`), that it carries **no** `lootOnDestroy` (native replay can't dupe loot) and is AutoPool-registered, and that vanilla knives aren't retrievable (so no pickup sync is needed). Capture at the canonical source — a `ProjectileSystem.StartProjectile` prefix gated by the existing `_inThrow` flag (the Breakable branch never reaches `StartProjectile`, so this isolates exactly the knife dispatch) — snapshots the real ray and broadcasts weapon `ItemId` + launch pose/velocity/mass/drag/stick/caliber/damageType. Receivers resolve that weapon's `customVisuals` (item-db prefab → `ThrowableWeapon.customVisuals`, same resolver family as HZ-2) and replay ONE damage-stripped `Custom` projectile (empty `damageComps` + `explicitDamage 0` + `isPlayer false`) through the real projectile system, so flight + stick match natively. VISUAL ONLY — knife damage stays on the existing host-authoritative hit route. Same topology as PlayerWeaponFire (thrower never replays its own; Client→Host→relay; receivers skip their own PeerId). `ProtocolVersion 5→6`. `NetThrowableProjectile`(+Codec), `ThrowableProjectileManager`, `ThrowablePatches` (StartProjectile prefix), `NetMessageType`(=80)/`NetService`/`NetGameplaySyncBridge` plumbing, `NetHandshake`, `CoopConfig` (`EnablePlayerThrowableProjectileSync` Fixed-on + `LogPlayerThrowableProjectileSync`). | **verified in-game** (user — "完美 过了": knife flight + stick visible on both peers, no damage leak) · built 0 error · committed `a214bc8` |
| **SL-1** | **Shared-loot client loot-roll suppression (Model A: host-authoritative rolling).** First piece of the "shared world loot" feature (the connect-page "Loot mode: Independent (Shared coming soon)" placeholder). When shared loot is active a client must never roll its own loot — the host is the sole roller and every rolled pickup mirrors down through the existing world-drop channel (`WorldPickupManager` + the `ShareAllLoot` capture filter WID already shipped). `LootRollSuppressionPatches` prefix-blocks the whole `LootManager` roll surface on clients — `SpawnGlobalLoot` (enemy/money/ammo) + `SpawnLootFrom` ×3 (override tables, breakables, projectile `lootOnDestroy`); all are `void` and their callers ignore the return, so blocking is safe (the `SpawnPickup` chokepoint can't be blocked — callers dereference its result, e.g. `SpawnLootFrom(...).body.AddForce`). Not touched: chests (direct `SpawnPickup` → SL-2), player drops (`inventoryData`, the WID base), the mirror path, the host, and solo. The client's enemy-puppet death already avoids `SpawnLoot` (visual-only), so this mainly catches client-broken crates. Gated by `ShareAllLoot`; behavior-neutral by default. `LootRollSuppressionPatches`, `PatchBootstrap`. | **verified in-game** (combined SL-1/SL-2 test, `ShareAllLoot` on — enemy + crate loot a single shared set, host-rolled, first-picker-wins; user "完美无问题") · built 0 error · committed `e24251f` |
| **SL-2** | **Shared-loot host-authoritative chest (`Container`) open + state sync (Model A).** The loot chest rolls its item with local RNG in `Container.SetContainedItem`, so only the host may roll it. **Host interacts** → vanilla open runs (roll + `SpawnPickup`, mirrored via the shared-loot world-drop channel, + the open animation) and an `OnInteract` postfix broadcasts `ChestOpened`. **Client interacts** → the local open is blocked (no roll) and a `ChestOpenRequest` — keyed by the container's deterministic world position (same keying as the DB-1 inter-chunk doors) — goes to the host, which opens its matching chest (→ roll + spawn + the same broadcast); its `looted` flag is the first-come arbiter (a second request no-ops). Every peer mirrors `ChestOpened` **visual-only** (mark `looted` + `SetTrigger("Open")`, never re-rolling — a re-roll would diverge). Loot itself mirrors through the one `InteractionManager.SpawnPickup` overload that both the chest and WID share. Only active while `ShareAllLoot` is on (Independent mode keeps chests per-peer). `NetChestOpen`(+Codec, one DTO both directions), `ChestSyncManager` (position registry + request/host-open/broadcast/mirror), `ChestPatches` (`Container.Start` register + `OnInteract` prefix/postfix), `NetMessageType`(81/82)/`NetService`/`NetGameplaySyncBridge` plumbing, `NetHandshake` (`ProtocolVersion 6→7`), `CoopConfig` (`EnableChestSync` Fixed-on + `LogChestSync`, runtime-gated by `ShareAllLoot`), `ReverseProbePatches` (level Clear), `PatchBootstrap`. Known minor v1 gaps: the displayed-item "PickItem" take animation isn't mirrored (the mirror pickup isn't container-linked) and the per-item quality glow color isn't reproduced on clients. Model A adds a host round-trip before a client-opened chest visibly opens (intended). | **verified in-game** (combined SL-1/SL-2 test — chest opens on both screens whether host- or client-initiated, single host-rolled item, first-picker-wins, no re-open/dupes; user "完美无问题") · built 0 error · committed `ce0f1d3` |
| **SL-4** | **Shared world loot as a host-authoritative session toggle (completes SL-1..SL-4; replaces the connect-page "Loot mode: Independent (Shared coming soon)" placeholder).** Adds a `SharedLoot` field to the FF-1 `SessionSettings` snapshot (msg 70) + `NetSessionSettings.SharedLootEnabled` (host → `ShareAllLoot` config, client → received value, solo → off) as the single read point that loot-roll suppression (SL-1), chest sync (SL-2) and the world-drop capture filter now gate on — re-pointed off the raw `ShareAllLoot.Value`, so a client is driven entirely by the host's broadcast. The readonly Loot-mode row becomes an interactive "Shared loot" toggle with the exact FF lock/mirror machinery: the host edits + broadcasts + both ends toast; a client's row is locked (dimmed, input-blocked) and mirrors the host's synced value via `SetIsOnWithoutNotify`; a read-only line shows the session value. Independence held — the toggle gates only SL-1/SL-2; the WID player-drop base (`inventoryData != null`) and the K-1 knife sync are separate paths. The host writes its choice to config (remembered); a client never writes config. `ProtocolVersion 7→8`. `NetSessionSettings`(+field/accessor/apply/reset)/`Codec`, `NetService` (state build + client apply toast), `NetHandshake`, `LootRollSuppressionPatches`/`ChestSyncManager`/`WorldPickupManager` (gate re-point), `CoopConnectPage` (toggle + lock/mirror + tick/dispose), lang en/zh-CN/ja + `Localization.md` row 29. | **verified in-game** (user "这部分无问题" — the toggle drives shared loot on/off, client row locked + mirrors, toasts both ends) · built 0 error · committed `565d9c2`. Follow-up **SL-2b**: the chest sync so far covers only the `Container` class — food / material / cash-register loot sources are a different class, being identified next. |
| **SL-2b** | **Shared-loot sync for `LootableObject` (food / material / scavenge hatboxes + the cash register — a non-`Container` loot class the SL-2 chest sync missed).** These use `PerfectRandom.Sulfur.Gameplay.LootableObject` (an `Interactable` whose trigger event calls `LootableObject.Trigger()` → an RNG item count → `LootManager.SpawnLootFrom` per item). Since that loot goes through `SpawnLootFrom`, SL-1 already suppresses a client's roll; the missing piece was that the host must roll its own copy when a client loots one. `LootableSyncManager` (reflection — the mod doesn't reference the Gameplay assembly; covers `ChurchCollectionLootable` too) registers each object by deterministic world position and, from a `Trigger` postfix, a client asks the host (`LootableTriggerRequest`, position-keyed) which rolls the loot **directly** via the public `TriggerLootFromAnimation()` — NOT the animator event, since the host isn't looking at a client-opened object so its animator is culled and the event never fires (Log430: hatboxes triggered but dropped nothing) — then broadcasts (`LootableTriggered`) so peers replay the open. Reuses the `NetChestOpen` DTO/codec. `ProtocolVersion 8→9`. `LootableSyncManager`, `LootablePatches`, `NetMessageType`(83/84)/`NetService`/`NetGameplaySyncBridge`, `NetHandshake`, `CoopConfig` (`EnableLootableSync` Fixed-on + `LogLootableSync`), `ReverseProbePatches` (level Clear), `PatchBootstrap`. | **verified in-game** (user — food/material/register loot now shares host-authoritatively, first-picker-wins, no double-roll) · built 0 error · **known limitation (issue #14):** the hatbox *open animation* doesn't replay on remote peers (loot is correct; the register animates, the hatboxes don't — a `HatboxController` quirk not reproducible from a programmatic trigger). · committed `757ef2e` |
| **GU183-3** | **Re-bind the two remaining hooks 0.18.3 renamed/re-signed, and stop a missing hook from taking an unrelated one with it.** Both were silent: `AccessTools` returns null on a miss, so neither had a functional symptom to chase. **(a)** The NPC list sweep bound `BatchedNPCRaycasts.Update`, which the manual-update refactor renamed to `ManualUpdate` (the class now runs from `ProjectileSystem.Update` alongside `EffectsManager`/`InteractionManager`/every `Player` — same position in the frame, before the LOS/ground job is scheduled, so only the entry point moved). Destroyed units stopped being swept from `GameManager.units`/`aliveNpcs`. **(b)** Worse, that miss `return`ed early and so also skipped the **Phase 5.7-DS2 `LateUpdate` finalizer**, an unrelated protection that swallows the transient `ArgumentOutOfRangeException` thrown when the roster/`Players` count moves between job-schedule and result-decode (Log118) — dead since the update, and nothing would have said so. The two are now installed independently. **(c)** `CousinHelper.DoneAppearing` was resolved with `Type.EmptyTypes`; the game did not remove it but gave it a parameter (`DoneAppearing(int disableInvulnerable)`), so the exact-signature lookup returned null and the boss's pre-commit invuln-hold stopped applying. Binds by name now — `CousinHelper` declares exactly one, and the postfix takes only `__instance`, so the parameter list was never ours to pin. (`HellShrewDigga.DoneAppearing()` is still parameterless — a different type, easy to confuse.) All three misses now `Log.Error` naming the dead capability. `ReverseProbePatches`, `BossEncounterPatches`. | built 0 error · full rebuild 411 warnings = baseline · **pending in-game verify** (low risk: sweep + a crash-swallow + Cousin pre-fight invuln) |
| **WID-3** | **Mirror-side settle glide (cosmetic) — replace the world-drop position-sync teleport with a local eased animation.** When a synced pickup's owner broadcasts its authoritative rest position (WID-2 `WorldPickupSettle`, msg 75), the mirror used to snap there instantly — a visible teleport, worst for host-rolled shared loot whose mirror drops straight down from the spawn point while the host's copy was flung elsewhere. `WorldPickupManager.ApplySettle` now freezes the body up front (kinematic + no collisions, the game's own landed-pickup end-state, so physics can't fight the move) and starts a short local glide that `Tick` (`AdvanceSettleAnimations`) drives to the target over a distance-scaled window (`distance / 6 m/s`, clamped `0.12–0.5 s`) with an **ease-out** horizontal path + a small sine **hop** (peaks mid-glide, zero at the target, capped `0.4 m`) — deliberately not uniform/linear. A correction under `5 cm` still snaps. Purely cosmetic + local: no protocol/authority/wire change (the owner still settles one authoritative position; a re-settle retargets the glide from the current spot), no new config (constants only). Single-player untouched (no session → no settle → no glide). `WorldPickupManager` only. | **verified in-game** (user "完美 过了" — mirror pickups glide into place instead of teleporting) · built 0 error · committed `102d97b` |
| **TD-1** | **Shared damage numbers on the 0.18 unlockable target dummy** (`Docs/TargetDummySync.md`). The dummy's display is `DamageTracker` (Gameplay assembly): `onDamageRecieved → ShowDamage` flies a per-hit head number and `AddToTotal`s a foot total (5 s idle reset). It is not a host-roster puppet, so every shooter damages its own local copy (`TrySendClientHitRequest` skips non-puppets) and only that shooter saw the numbers — the same reason the white hit flash already worked. **Model = peer-broadcast, each peer accumulates locally** (no host authority forced onto a non-puppet object; the shooter keeps zero-latency vanilla feedback): a `ShowDamage` postfix captures each real local hit, the peer broadcasts it on the `BreakableBreak` topology (`Client→Host→relay`, host stamps PeerId + scene, ReliableOrdered, source never echoes to itself), and every other peer replays the **effect only** via the same private `ShowDamage` (reflection, mirror-guarded) — flying number + total, **never touching the dummy's health**. Each peer's total = own hits + everyone else's relayed hits = the same exact shared sum. **Coalescing** (the user's perf concern — high-RPM guns): a shooter sums its hits per dummy over a 75 ms window and ships ONE message whose amount is both added to the total and shown as one flying number → total stays exact, ≤~13 msg/s per dummy per shooter at any RPM, local shooter keeps full per-hit fidelity (only remote numbers are chunked). **Unlock gating falls out of the lifecycle**: a locked dummy's subtree is `SetActive(false)` (`UnlockableStorage` → per-player `PlayerProgress` church checkpoint), so `DamageTracker.Start` never runs there, nothing registers, and a relayed hit finds no local tracker and is ignored — the sync never spawns or reveals anything. Identity = the tracker's authored world position (1.0 m epsilon, same keying as chest/door/breakable); hit point travels separately. `ProtocolVersion 9→10`. `NetTargetDummyDamage`(+Codec), `TargetDummySyncManager`, `TargetDummyPatches`, `NetMessageType`(85)/`NetService`(broadcast/relay/dispatch/Tick)/`NetGameplaySyncBridge`, `NetHandshake`, `CoopConfig` (`EnableTargetDummySync` Fixed-on + `LogTargetDummySync` off), `PatchBootstrap`, `ReverseProbePatches` (level Clear). | **verified in-game host→client** (user "完美符合预期" — the host's hits show their flying numbers + shared total on the client) · built 0 error · full rebuild 437 warnings = baseline (0 new) · committed. **client→host direction**: turned out the client DOES damage the dummy (host-roster Npc, hit routed to host), but the host applies it via `SetStatus` which bypasses `onDamageRecieved` so no number showed — fixed in **TD-1b** (host drives its own dummy's `ShowDamage` after applying a client hit → the existing TD-1 broadcast carries it). |
| **TD-1b** | **Show shared numbers for a CLIENT's hits on the target dummy** (TD-1 follow-up; the host→client direction was verified, this closes client→host). The dummy is a host-roster `Npc`, so a client's hit IS routed to the host — but the host applies it (like every enemy) via the health-only `EntityStats.SetStatus` write, which **bypasses `onDamageRecieved`**, so `DamageTracker.ShowDamage` never ran for a client's hit and no number showed anywhere (the dummy's ~1M HP ticked down invisibly; the white flash still worked because it rides a separate `HostHitVisual` channel — which is why it *looked* like the client dealt no damage). The client's own `ReceiveDamage` is suppressed when the hit is routed out, so it made no local number either. Fix: right after `TryApplyHostHitDamage` applies a client hit, the host drives the number on its **own** dummy via `TargetDummySyncManager.ShowHostNumberForClientHit(unit, actualApplied, …)` — a direct `ShowDamage` call (NOT mirror-guarded), so the existing TD-1 postfix captures it and broadcasts to every peer (the shooter included), exactly as the host's own hits already sync. No-op for any unit without a `DamageTracker` (cached `GetComponentInChildren` — every ordinary enemy), and only client-originated hits reach `TryApplyHostHitDamage` (the host's own hits go through vanilla `ReceiveDamage`), so exactly one host-side `ShowDamage` per hit — no double-count. Damage application (SetStatus), health/death/flash sync all unchanged; only the number is added. Known limitation: `NetClientHitRequest` carries no damage type → client-dealt numbers use `Normal`(7) colour (value + total exact; colour generic) — adding the type is a wider shared-hit-path change, deferred. `TargetDummySyncManager` (+`ShowHostNumberForClientHit`/`GetTrackerOnUnit` cache), `NetGameplayProbeManager` (one call in `TryApplyHostHitDamage`). No protocol change. | **verified in-game** (user — a client's hits now show flying numbers + shared total on both ends) · built 0 error · full rebuild 437 warnings = baseline (0 new) · committed. Separately confirmed during this test: the ChurchHub spawns **two overlapping dummies per station in the base game** (reproduces in vanilla with the mod uninstalled) — two foot-totals / doubled appearance are that base-game artifact, not the sync; documented in `Docs/TargetDummySync.md`, no workaround by design. · committed `a1a70d4` |
| **LK-UnknownFollow** | **A client now follows the host into chapters that match no combat keyword** (the DevTools debug scene + the DLC). The co-op level-follow system buckets every destination Hub/Menu/Combat/Unknown by keyword on the chapter name (`WorldEnvironmentIds.ToString()`). The keyword lists only know the base-game `Act_xx`/`Caves`/hub names, so five whole environment families fall through to **Unknown**: `DebugChapter`, `DLC_01_RobotHeaven_Lower/Middle/Upper/Tower`, `EndlessMode`, `ChallengeEnvironments`, `Onboarding(_LoopAround)`. The **client-led** direction already handled Unknown (5.6-LK-P2), but the **host-led** direction did not: host send (`SendHostSceneRequest`) set `AutoLoadAllowed = autoFollowCfg && (combatTarget||hubTarget)` — false for Unknown, and took the seedless menu/unknown branch; client follow (`TryAutoFollow`) computed `combat = !hub && IsCombatTarget` — false for Unknown → `AutoFollowSkippedHub`/`non-combat-non-hub`, returned without following. So when the host entered a debug/DLC/endless level the client never auto-followed and the two ended up on different maps (the reported "can't sync into debug scene" + the mid-run map desync). Fix reuses the existing combat pipeline with no new keywords: a target that is **finalized + seeded + non-hub + non-menu** is treated as combat-like on both sides. Host: `SendHostSceneRequest` promotes such a target to `combatTarget` (guarded by `snapMatchesTarget` = the finalized snapshot's `SameScene` as the request, so a stale combat snapshot can't hijack an Unknown target) → it flows through the combat seed+used-sets copy and gets `AutoLoadAllowed`. Client: `TryAutoFollow` computes `combat = !hub && (IsCombatTarget || (req.HasLevelSeed && !menu))`, `menu = IsHubOrMenuTarget && !hub` isolating true menus (no seed → excluded). No protocol/`ProtocolVersion` change (reuses `HostSceneRequest` fields), no new config. Base `Act_xx`/hub/menu paths untouched; single-player untouched. `NetService`, `NetClientLoadGate`. | **verified in-game** (user — "正常进debug关卡了", client now follows the host into the debug scene) · built 0 error · committed |
| **DR-1b** | **Downed-rescue feel: engage radius alignment + release grace** (Log438 follow-up to Phase DR). User report: "can't rescue a teammate, had to mash E." Logs showed the mechanism was fine (two successful revives, zero host rejections, input read working — BepInEx `KeyboardShortcut` bindings DO work in this game, unlike raw `Input.*`; see `RunStatsInputReader.cs`). Real causes were three small things stacked: (1) the client's engage radius was the bare `PlayerReviveDistance` (2.5 m) while the host validated at +1 m (3.5 m), so a rescuer 2.5–3.5 m from the body got **no `Hold [E]` prompt and no hold-start** — a genuine long-press read as "nothing happened" (the user's "first hold did nothing"); (2) every key release ran `CancelHostActiveRescue`, resetting progress to 0, so tapping never accumulated; (3) the border-fill progress wasn't prominent enough to notice. Fix (host-authoritative, `NetPlayerLifeManager` only, no protocol/config change): (1) new `ReviveEngageRadius() = PlayerReviveDistance + 1` shared by the client's `TryFindNearestDownedPeer` (prompt + hold-start) and the host's `TryValidateRescueEligibility` — one radius, no dead band, earlier prompt; (2) `ActiveRescueRecord` gains `Released`/`ReleasedAt` + a `RescueReleaseGraceSeconds = 0.3` window: a release **freezes** progress instead of cancelling, a re-hold by the same rescuer within 0.3 s **resumes** from the frozen value (`StartedAt += now − ReleasedAt`, so the paused span isn't counted), and only a lapsed window cancels for real — so rapid tapping and one-frame input gaps accumulate. Hold duration unchanged (5 s). | **verified in-game** (user "很完美") · built 0 error · committed |
| **SS-Toast-Bypass** | **Session-setting-change toasts now reach a client that has join/leave toasts off.** Log388: the host toggled friendly fire and only the host toasted — the client showed nothing (not one `[CoopToast]`, not even its own connect toast). Root cause: `CoopToasts.NotifySessionSetting` routed through the gated `Notify`, which early-returns (before even logging) when the local `EnableCoopToasts` preference is off — but that toggle is the personal "show player join/leave notifications" switch, and this client had it off. A host changing a session **rule** (friendly fire is gameplay-affecting) is a host-authoritative announcement every player must see regardless of their join/leave choice (CoopUiPlan §5 change-notification rule "every player is told") — otherwise a client silently never learns FF was turned on. Fix: `NotifySessionSetting` calls a new private `Notify(title, message, respectPreference:false)` that bypasses the `EnableCoopToasts` gate; the join/leave/link/connect toasts stay gated by it. No protocol/sync/config change. `CoopToasts`, CoopUiPlan §5. | verified in-game (user — client with join/leave toasts off now sees FF-change toasts) · committed |
| **EM-Boss** | **Make Endless bosses fightable in co-op: stop the client from killing them during their intro, and fix the duplicate/residual boss health bar.** Endless bosses are the story bosses' `Endless` controller variants (`CousinEndlessHelper`/`LuciaBossEndlessHelper`/… : `BossEndlessHelper` : `BossFightHelper`), spawned as a normal wave burst so their spawn/HP/death already ride the EM-2 puppet mirror. Two co-op problems surfaced once they were actually reached. **(1) Intro instant-kill:** a boss spawns invulnerable (`Awake → SetInvulnerable(true)`) and only clears it at its animation-event fight start (`Anim_OnStartBossFight → TriggerFight`). Vanilla `Unit.ReceiveDamage` blocks damage while `isInvulnerable`, so the host's own shots do nothing during the ~5 s intro — but the client-hit path (`ProcessClientHitRequest`) writes health via `Stats.SetStatus` and never calls `ReceiveDamage`, bypassing that guard, so a strong client weapon melted the boss to 0 during its intro and it died before its fight started (Cousin/Lucia "instant-die / skip", Log482–483). Fix: `ProcessClientHitRequest` drops the hit when the target is an Endless boss still in pre-fight — `IsBossInPreFightInvulnerability` = unit `isInvulnerable` **and** it carries a `BossEndlessHelper` whose `fightStarted == false`. Narrow by design: ordinary enemies (no helper) and a boss whose fight has started (Terrorbaum's permanent body-invuln + eye-hitbox damage, `fightStarted == true`) are unaffected. **(2) Two bars + residual:** the boss showed a second health bar that lingered across stages. Roster probe proved the crypt segment bar (`EndlessModeManager.spawnedBosses`) was clean (0→1→0) on both ends; the extra bar was the **standard single boss bar** (`UIManager.bossUI`, `AttachToBossUI`). Our story boss-encounter system was sweeping the Endless boss in via the generic `BossFightHelper` adapter and calling `TryAttachBossBar → AttachToBossUI(true)` — redundant (Endless uses the crypt bar) and never detached for the Endless variant, so it lingered (Log485: `stdBossBar` tracked the endless Cousin, never released). Fix: `BossAdapterBase.TryAttachBossBar` skips the standard bar for Endless bosses (`IsEndlessBoss`). Also fixed a latent crypt-bar hide bug: `CryptProgressBar.Update`'s show/hide slide used scaled `Time.deltaTime`, so the stage-transition freeze (`timeScale 0`) stalled the hide-slide and the bar never reached `SetActive(false)`; a transpiler swaps that one read to `Time.unscaledDeltaTime`. No protocol/`ProtocolVersion` change (host-side damage/UI + adapter logic), no config. `NetGameplayProbeManager` (`IsBossInPreFightInvulnerability`), `BossAdapterBase` (`IsEndlessBoss` guard), `EndlessBossBarFixPatches` (new, crypt-bar transpiler), `EndlessBossDiagnostics` (new, observe-only probe gated on `LogEndlessSync`). **Deferred (not done):** full phase authority (block the client controller + mirror the host's phase) — Log484/486 showed the client boss is already functionally synced (adds bound via the existing RT3 manifest, HP mirrored), the only divergence being the "Choose Random Phase" RNG pick, which causes no real harm; a full block+mirror is a high-risk rework of a working fight, so it waits until a real problem appears. Emperor Endless (bespoke, not `BossEndlessHelper`) is out of the guard's scope. | **verified in-game** (Log486 — `stdBossBar=<none>` throughout on both ends, single crypt bar, `cryptBar` hides after death, no residual; Lucia this run confirmed the guard generalizes past Cousin; Step-1 `ABSORB pre-fight-invuln` active; user "可以收工了") · built 0 error · Step 1 (Log483 — Cousin fully fought through phases) → residual root-caused via roster probe (Log484/485) → standard-bar skip + crypt transpiler |
| **EM-Arena** | **Sync the Endless arena (stage) transition — fixes the reported "level desync in endless".** The Endless "level" is an in-place arena swap (`EndlessModeManager.ArenaTransitionRoutine`): on a stage clear the host picks the next arena via `gameplayRandom.NextInt(0, arenaPrefabs.Count)` (avoiding a repeat), destroys the old arena, `InstantiateArena`s the new prefab and `TeleportTo`s the player — it never goes through GoToLevel/scene-load, so the LK/transition-relay pipeline never sees it. The client's slave manager never runs the routine (Update suppressed in Shared, and in Independent the wave-advance guard can't fire), and `gameplayRandom` has long diverged, so from the 2nd stage on the two ends were in different arenas. Fix makes the pick host-authoritative and reuses the vanilla routine on both ends: the host **arms the swap from `HostTick` on the `transitionState → ArenaTransition` edge** (`HostPickAndArmArena`): the instant the manager enters ArenaTransition — i.e. when the routine starts, ~3 s before it reaches its own pick — the host chooses the next arena (`UnityEngine.Random`, avoiding `lastUsedArenaIndex`), forces it via `debugOverrideArena` so its own routine uses it, and bumps a monotonic `ArenaEventId`+`ArenaIndex` carried on the EM-3 `NetEndlessWaveState` snapshot (codec v2→v3). The client, on seeing a new id (`ApplyHostWaveState`, **both modes** — arena is world-authoritative in each), forces the same prefab via `debugOverrideArena` and runs the SAME vanilla routine (its cleanup / navmesh bake / player teleport / per-player "continue" gate), first setting `transitionState = ArenaTransition` so an Independent-mode client's vanilla Update parks on the no-op case. An `InstantiateArena` postfix clears the forced override on both ends. Idempotent to the latest arena for a late joiner. **Why the transition edge (HostTick), not the routine:** a Harmony prefix on the `ArenaTransitionRoutine` iterator's kickoff does NOT fire in this game (Log478); hooking `InstantiateArena` instead fired too late (client swap trailed badly) and missed some transitions (Log479); the host-owned transitionState edge fires exactly once per transition, early, for every transition. `ProtocolVersion 25→26`. `NetEndlessWaveState`(+`ArenaEventId`/`ArenaIndex`), `NetEndlessWaveStateCodec`(v3), `EndlessSyncManager`(`HostPickAndArmArena`/`ClearForcedArenaIfSet`/`ClientRunArenaTransition`+arena reflection+HostTick edge-detect/ApplyHostWaveState/Reset), `EndlessSyncPatches`(`InstantiateArena_Post`), `NetHandshake`(v26). Single-player untouched (`BossMode != Host`). **Known limits (to watch in real-test):** spawn point is each end's own `GetRandomPlayerSpawnPoint` (same arena, possibly different points); a narrow client-cleanup-vs-next-wave-puppet race. **Separate (NOT arena) — endless boss skip:** Log479 showed the endless boss (`GoblinCousinEndless`) spawn + `Npc.Die` in 0.4 s with `damageCount=0` on both ends → the wave instantly counts clear → the arena advances, so the boss appears to be "skipped". That is unsynced/unhandled endless-boss behaviour (the deferred EM-Boss work, `AddBossUnit` path), not the arena sync. | **verified in-game** (Log480 — host armed event 1/2 idx 8/2, client swapped both to the matching arena, 0 errors, low latency; user "关卡切换貌似没问题了") · built 0 error · kickoff prefix didn't fire (Log478) → InstantiateArena too late/missed (Log479) → re-hooked to the HostTick transition edge, which fixed it · committed `f6d62a0` |
| **EM-TargetAppend** | **Fix "Endless enemies target only the client" — a regression in EM-Target's targeting fix.** `RefreshTargets_Post` (host) adds the client ghost units to each Endless enemy's `AiAgent.overridetargets` so `GrabHostileUnit(Closest)` can pick whichever player is nearest. It did so by calling `OverrideTarget.AddUnits(ghostList, Closest)` — but decompiled `AddUnits` **replaces** the internal list wholesale (`unitsToTarget = targetUnits`), so it wiped the list vanilla `EndlessModeManager.RefreshTargets` had just built (`{ host PlayerUnit } + companions + other enemies`, minus self) and left `unitsToTarget = ghosts only`. With the host player gone from every enemy's candidate set, `GrabHostileUnit` could only ever return the client ghost → enemies glued to the client; when the client went down its ghost was filtered (`Dead`/`isInvulnerable`) → empty list → aggro fell back to the host (matching the report exactly). Fix: `AddGhostsToEnemies` now reflects `OverrideTarget.unitsToTarget` and **appends** each ghost to the live list vanilla just populated (with a `Contains` dedup), preserving the host player + companions + enemy-vs-enemy entries; the replace-semantics `AddUnits` is kept only as a graceful fallback if the private field can't be resolved (degrades to client-only aggro, not to inert enemies). Host-side targeting logic only — no wire/protocol change (no `ProtocolVersion` bump), no config, Independent + Shared both fixed, single-player untouched. `EndlessSyncPatches` only. | **verified in-game** (user "完美" — enemies now split by nearest player instead of only chasing the client) · built 0 error · committed `e6b0ca3` |
| **EM-5b-flyin** | **Shared-mode distant XP orbs now fly into the collector instead of vanishing (cosmetic).** In Shared Endless mode the enemy-death XP is one host-authoritative shared pickup that spawns `xpValue 0` cosmetic orbs on both ends (EM-5b); the numeric XP was always correct. `PickupTick` fires a collect request when the local player is within `pickupRadius + 1.5` — **outside** vanilla's own collect range — so on the collector's screen the resulting `ApplyCollected → RemoveOrbsNear` deleted a distant orb the instant the player crossed the outer reach, before vanilla's own fly-in (which only starts inside `pickupRadius`) could play: the orb blinked out and XP jumped. Near orbs looked right because vanilla self-collects them while the player is already in range. Fix: on the collector's **own** end `ApplyCollected` now reuses the EM-5c force-collect machinery (`RegisterForceCollect`) to flip the pickup's orbs to the vanilla `Collecting` state so they home in and vanilla absorbs+removes them (value 0 → no XP double-count); the non-collector ends keep the instant `RemoveOrbsNear` (a remote player took them). `PendingDrop` carries the orb count and `CanForceCollect()` falls back to instant removal if the orb reflection isn't resolved. Client-side visual only, no protocol/authority change; Independent mode untouched (its pickup path is unused — EM-5c awards deterministically). `EndlessSyncManager` (`ApplyCollected`/`ApplyDrop`/`PendingDrop`/`CanForceCollect`). | **verified in-game** (user "完美") · built 0 error · committed |

## 5. Rule going forward

- Every behaviour-changing commit names its phase label in the subject, and that label appears in this registry
  (or in `DevelopmentPlan.md` / memory for older tracks) with a one-line meaning + status.
- When unsure what a label means, this file (§4) and the agent memory index are the lookup.
| **MP-Cap** | **Remove the four-player cap so a fifth (and beyond) peer can join** (reported: "the 5th player is stuck in joining forever, on both Steam and LAN, reproduced on five hosts"). Origin was an early-days debt: `MaxPlayers` defaulted to 4 and its setter clamped to `[2,4]` (`CoopConfig`), while the key had been retired from the `.cfg` and was never surfaced in the in-game UI — so it was unreachable and permanently 4. The gate lived in `NetService.OnConnectionRequest` (`_clients.Count >= MaxPlayers - 1`, host holds slot 0 → 3 clients max) and fired **before** the handshake, at the LiteNetLib connection-request layer. **Why it hung instead of failing:** `request.Reject()` carries no reason payload, and the client's `OnPeerDisconnected` unconditionally schedules `_nextClientReconnectTime = Now() + 5f` — so the rejected peer retried every 5 s forever with nothing to display, never leaving "joining". **Why both transports:** Steam is not a separate path — `SteamRelayBridge` bridges each Steam peer (per-SteamID dict, no cap of its own) into the same loopback LiteNetLib socket, so it hit the identical gate. Fix per the `CoopUiPlan` §"Player cap" decision (**no artificial cap** — this is a private session over direct IP or the Steam relay, and the real ceiling is per-client bandwidth/CPU): the connection-request gate is deleted, `MaxPlayers` is removed from `CoopConfig` + the `CoopSettingsStore` JSON (the key stays in `RetiredCfgKeys` so stale `.cfg` entries are still pruned; `JsonUtility` ignores the now-unknown `maxPlayers` in an existing `coop.json`, so no other setting is disturbed), `AllocateClientSlot` takes no bound and returns the lowest free slot from 1 upward (it can no longer return `-1`, the second latent cap), and the trailing `maxPlayers` field is dropped from `HandshakeAccepted`. Slot was audited first and is **display/ordering identity only** (session status text, run-stats ordering) with no gameplay dependency, so widening the peer count touches no authority logic. `ProtocolVersion 26→27`. `CoopConfig`, `CoopSettingsStore`, `NetService` (gate + two call sites + log), `NetSessionManager`, `NetHandshake`, `CoopConnection` (connection signature), `Docs/NetworkPlan.md`, `Docs/CoopUiPlan.md`. | built 0 error · 451 warnings, none in the touched files (all pre-existing nullable analysis) · **in-game verify waived** (needs 5+ machines; scope judged small enough to ship without it) · committed `f13875e`; the per-client bandwidth/CPU cost at high player counts is untested and is the real remaining limit |
| **PK-1** | **Desert pikes: host-authoritative jumps for the LEVEL ambush pikes, and a carrier-owned transform for their riders** (reported: "in the desert it teleports my friends to the start of the floor or outside the map's boundaries, always near pike riders, only for whoever isn't hosting"; separately "sudden 1-2 FPS with `Rotation quaternions must be unit length` spam"). The desert level's ambush sandworms are `DesertPikeCarrier` with `isLevelSpawning` — the same component as the boss's pike, but outside any boss arena. Two authority defects met there. **(1) Split ownership of a rider.** `DesertPikeCarrier.Update` writes every attached unit's `localPosition = zero; localRotation = identity` EVERY frame, while our generic enemy mirror was simultaneously driving those same riders to the host's world positions (they are ordinary level `Npc`s; the existing `IsDesertEnemyPikeSnapshot` exemption covers only the pike itself). Two writers, ~60 Hz: the mirror hard-snaps the rider to the host's pike, the local carrier yanks it back onto the client's pike. A live collider crossing metres per frame next to a player is resolved by PhysX by ejecting the player — and the native `Player.ManualUpdate` rescue (`linearVelocity.y < -100` → `TeleportTo(Room.FindClosestRoom(lastValidGroundPosition).GetRespawnPoint())`) then files them back at the floor's start, which is exactly the reported pair of symptoms. The same rotation writes (`LookRotation` onto a rider parented under the carrier's **mirrored, negative-x-scale** transform) are the likely source of the non-unit-quaternion spam, whose per-frame logging is what drops the frame rate. Fix: while `Npc.isAttachedToUnit` is true the carrier is the single position authority and the mirror skips the transform/animator apply entirely (`IsMountedOnCarrier`, joining the existing Cousin-arm / Emperor-section / mounted-boss-body exemptions); on dismount the flag clears natively and the ordinary puppet drive resumes. **(2) The pike itself was never synced outside a boss fight.** The F4-ADDS jump replay required a *started Desert boss encounter* to resolve an encounter key, so a level pike got no `PikeJump` event, its local jumps were not blocked (they are level entities, not RT3 mirrors), and each end pounced on its own local player — the divergence that fed defect (1). Fix: the addressed event `PikeJump:<hostIdx>` no longer needs an encounter (empty key), the client dispatches it **before** the encounter lookup and resolves the target through the general host-binding map (`TryGetHostBoundRuntimeObject`; RT3 mirrors still resolve first), and the client blocks local jumps for any pike bound to a host SpawnIndex — **fail-open** on unbound pikes, since an unbound pike receives no replay and blocking would bury it for the level. The replay now also carries `shouldJumpOff`/`inverted`, which `FindPositionToJumpTo` arms on the deciding end only and `JumpTowards` does not set: without `shouldJumpOff` a replayed arc never drops its riders (`UpdateJump` gates `JumpOff` on it) and the client's riders would stay welded forever. **(3) Latent host crash guard.** `OnTriggerEnter` accepts any Unit with `isPlayer`, which includes the host's headless remote-player ghosts — but the coroutine it starts opens with `player.GetComponentInChildren<ExtendedCameraController>().gameObject.transform`, and a ghost has no camera rig; the NRE kills the coroutine *after* `isTriggeredByPlayer` latched true, permanently disabling that pike and leaving its riders vulnerable. A prefix drops ghost-collider triggers (`RemotePlayerRegistryManager.IsGhostCollider`). **Intentional behaviour change:** a level pike is now armed by the HOST player only — a client walking past one alone gets no ambush (buried consistently on every end) instead of a divergent local one; arming it from a remote player needs a look direction the ghost does not carry and is deferred. `ProtocolVersion 27→28` (DiscreteEvent codec v2→v3). `NetGameplayProbeManager` (`IsMountedOnCarrier` + exemption, `TryGetHostBoundRuntimeObject`, `IsHostBoundEntity`), `NetBossEncounterManager` (host broadcast/level pikes, client block, addressed replay + flags, ghost-trigger guard), `NetBossEncounterMessages` (2 fields, codec v3), `BossReflect` (`TrySetMember`), `BossEncounterPatches` (`OnTriggerEnter` prefix), `NetHandshake` (v28). Boss-arena pike behaviour, single-player and every other enemy path unchanged. | built 0 error · 451 warnings = baseline (0 new) · **NOT yet verified in-game** — needs a host+client desert run: walk the host into an ambush pike and confirm the client sees the same emergence/arc, riders dismount on both ends, no player launch, no quaternion spam. Runtime risks that only a real run can settle: whether a dismounting rider's `AiAgent.JumpTo` behaves on a suppressed client puppet, and whether both ends still roll the same rider unit types (`spawningEnemies[Random.Range(...)]` per seat is pre-existing and unsynced) | · committed `69979b3`
| **PK-2** | **Stop the client from hanging on a poisoned enemy transform, and let a client actually trigger a desert pike** (Log487: the client froze hard in a normal desert level; both BepInEx logs were error-free because BepInEx does not capture Unity's own output — the evidence was in `Player.log`). **(1) The hang.** Unity was emitting `Rotation quaternions must be unit length` with a full native stack trace from `BatchedEvents.FixedUpdate → AIBase.OnUpdate → FinalizeMovement → FinalizeRotation → Rigidbody.MoveRotation`, first occurrence landing immediately after two **unbound** puppets were released (`stale for 3.02s (unbound)`), then degenerating into a continuous flood until the process stopped responding. Decompiling `AstarPathfindingProject.dll` settles the mechanism with no guesswork: `RichAI.MovementUpdateInternal` does `if (updateRotation) simulatedRotation = tr.rotation;` and `AIBase.FinalizeRotation` feeds that straight to `Rigidbody.MoveRotation` — **A\* re-reads whatever we wrote onto the transform, every physics step**, so a single bad rotation becomes a self-feeding loop that can never clear itself (Unity rejects the write, so the poisoned value stays). Our own two rotation writes were the way in: both guarded with `delta.sqrMagnitude < 0.001f`, and **every comparison against NaN is false**, so a NaN (or infinite) delta sailed through into `LookRotation` and onto the enemy. Three gates, all in the client mirror: `TryFaceEnemyTowards` is now the single rotation-write path and refuses anything non-finite (position, delta, direction, or resulting quaternion); the per-frame apply **repairs** a poisoned puppet transform (identity rotation + the host's position) instead of `continue`-ing past it and leaving it to rot; and `ReleaseEnemyPuppet` refuses to hand a broken unit back to native AI — release re-enables A\* (`SetNavMeshAgentState` → `NavMeshEnabledTarget=true`), which is exactly what detonated in Log487, so an unrepairable unit now stays suppressed (frozen and harmless) and says so in the log. Counters `nonFiniteFacingsRejected` / `transformsRepaired` / `releasesKeptSuppressed` ride the existing Phase5.2-diag line. **(2) The disarmed ambush.** PK-1's ghost-trigger guard prevented a real NRE but left nobody able to fire a level pike when the client is the one exploring: Log487 has 5 client-side blocked local jumps and **0** host broadcasts, i.e. the ambush happened on neither end. The host genuinely cannot derive the landing point (the native search opens with `player.GetComponentInChildren<ExtendedCameraController>()` and a headless ghost has no camera rig), but the client already computed a valid navmesh-snapped one before PK-1 blocked it — the blocked `JumpTowards` call carries it as an argument. So the client relays that point (`ClientPikeJump`, msg 97) and the **host** validates and runs the real `JumpTowards`, which fires the existing PK-1 postfix so every end replays the one host arc. No A\* dependency added and no reimplementation of the native search. Host-side validation treats it as untrusted input: run (chapter/level/seed) match, finite target, SpawnIndex resolves to a live unit whose `UnitIdentifier` really starts with `HellshrewPike`, a carrier exists, it is not already jumping, the target is within 60 m of the pike, and a 2 s per-pike cooldown. On accept the host also reproduces the rider-arming half of the native `OnTriggerEnter` (`SetInvulnerable`/`SetHitboxesInvulnerable` false + latch `isTriggeredByPlayer`) — without it the host's riders stay invulnerable and every client hit on them would be rejected. `ProtocolVersion 28→29`. `NetGameplayProbeManager` (`TryFaceEnemyTowards`, `TryRepairPoisonedTransform`, `IsFinite(Quaternion)`, apply-loop repair, release guard, `TryGetRuntimeObjectForSpawnIndex`, diag counters), `NetBossEncounterManager` (request send + host validation/accept, `FindPikeCarrier`, `ArmPikeRidersForAmbush`), `NetBossEncounterMessages`(+`NetClientPikeJump`+codec), `NetMessageType`(97)/`NetService`(send/handle/dispatch)/`NetGameplaySyncBridge`, `BossEncounterPatches` (prefix now takes `targetPosition`), `NetHandshake` (v29). **Deliberately NOT changed:** the 76–119 per-session `Setting linear/angular velocity of a kinematic body is not supported` messages — decompiled `AIBase` contains no velocity write at all, so blaming them on our kinematic override is unproven, and the previous session logged 119 of them and exited cleanly. Also untouched: the runtime-spawn divergence the same log shows (host 41 vs client 44 units, `firstDivergence idx=29 host=HellshrewShoota client=HellshrewDigga`, 3 host-only / 6 client-only), which is what produced the unbound puppets in the first place — a separate, larger piece of work. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **NOT yet verified in-game.** Needs a two-player desert run where the **client** walks into an ambush pike: expect `[PikeJumpSync] client requested pike ambush` on the client and `host accepted pike ambush` on the host, the pike surfacing identically on both ends, riders dismounting and being damageable, and `Player.log` free of `Rotation quaternions`. The hang fix is a guard, so a clean run only shows the counters staying at 0 — proving it required reproducing Log487's exact state, which was not attempted | · committed `69979b3`
| **PK-3** | **A puppet correction may no longer launch the player out of the world** (Log488 reproduced the long-standing "client gets teleported off the map" report: the client's view became skybox and came back a few seconds later). Evidence chain, all from one session: the host's proxy samples put the client at `(-56.3, 3.0, 205.4)` → `(110.6, -55.9, 51.9)` → `(-101.0, 3.0, 171.7)`, i.e. **~234 m of displacement** followed by the vanilla rescue (`Player.ManualUpdate`: `Rigidbody.linearVelocity.y < -100` → `TeleportTo(Room.FindClosestRoom(lastValidGroundPosition).GetRespawnPoint())` — which is the "teleported back to the start of the floor" half of the report); and in the same seconds the client's own drift diagnosis reports `[idx=43 HellshrewDigga maxErr=219.58 snaps=1] [idx=44 HellshrewShoota maxErr=219.03 snaps=2]` — **two puppets hard-snapped ~219 m**. A puppet correction is a teleport, and the collider goes with it: a kinematic collider materialising inside the player's dynamic body makes PhysX resolve the penetration with an enormous impulse. No NaN and no pike required — pikes and their riders were simply the units that move and rebind the most, which is why every reporter blamed them. Fix is deliberately narrow: `ApplyPuppetPosition` performs any correction over 3 m with the unit's **main collider disabled** (`Unit.mainCollider.enabled`, recording the collider's real previous value rather than assuming it was on — a mounted rider or a culled unit must not be handed back solid by us), then `Physics.SyncTransforms()` so PhysX sees the move as a placement instead of a swept overlap; `ProcessPuppetColliderHolds` (every frame, before the apply, so a stalled feed can't strand anything) restores the collider once the unit is clear of the local player, with a 2 s ceiling, and `ReleaseEnemyPuppet`/`ClearLevelScoped` drop holds too. NetService pushes the local player's live position each Tick for the clearance test. **Binding is untouched on purpose.** The upstream cause of the 219 m snap is the runtime-spawn divergence (client spawns a Shoota where the host spawned a Digga), and the obvious-looking fix — capping bind distance — was evaluated and **rejected**: an unbound client-only combat enemy is quarantined (`TryQuarantineClientOnlyEnemy` disables BT/navmesh/RVO and suppresses its combat and death claims), so refusing the bind would trade one violent teleport for a permanently missing enemy plus a frozen statue, i.e. worse sync. Accepting the far bind is right — the enemy ends up exactly where the host says — it just must not happen through the player. Sync behaviour is therefore bit-for-bit unchanged; only the physics of the teleport differs. Identity safety was checked while evaluating this: cross-type binds are already rejected (`UnitIdentifier` comparison in the roster reconcile), and the manifest binder groups candidates per host unit type, so the 219 m pair was same-type. Counters `safeTeleports` / `safeTeleportHoldsExpired` / `safeTeleportHoldsActive` ride the Phase5.2-diag line. No protocol change, no config. `NetGameplayProbeManager`, `NetService` (one position push per Tick). | built 0 error · full rebuild 451 warnings = baseline (0 new) · **NOT yet verified in-game** — needs a desert run where a client fights near burrowing/riding enemies: expect `safeTeleports` to climb while the player is never thrown out of the world, and no drop in enemy position accuracy (the drift diagnosis should look as it does today). Same-session results that DID verify earlier work: PK-2's relay ran end to end (`client requested` → `host accepted` → `host broadcast PikeJump:14` → `client replayed`, identical start/target, plus the cooldown and already-jumping rejections firing), and the client logged **zero** `Rotation quaternions must be unit length` with all three PK-2 guard counters at 0 (no NaN state was entered this session, so the guards are still unproven rather than exercised) | · committed `69979b3`
| **PK-4** | **An airborne desert pike can no longer shove the local player on a client** — the actual mechanism behind "the client gets teleported off the map", corrected after the user's own observation and a single-player control test overturned the earlier hypothesis. What the field data established: Log489 caught three launches, **every one during the pike's DESCENT**, two of them with the player standing 2.1 m and 2.8 m from that pike's landing point (`PikeJump:32 target=(198.8,-0.9,-334.2)` vs the player at `(196.9,-0.8,-335.1)`); the user could feel the flight capsule as an "air wall" and reported the launch as reliable on descent only. That geometry is by design — the native ambush search aims the landing point a few metres in front of the player who triggered it — so with PK-2 relaying a client-computed point, the pike now comes down next to the *client* every time. The user then tested single-player: the identical contact merely blocks or pushes the player, so **this is not vanilla behaviour**. The one provable structural difference is that on a client the pike runs as a PUPPET (`Activated Client puppet mode idx=5/15/17/20/32 movement=none`): puppet mode writes `isKinematic` directly, calls `StopOnCurrentPosition` and shuts down RichAI/RVO/BT, bypassing the vanilla `physicsEnabledTarget` state machine (`Unit.SetPhysicsEnabled` only sets a target; Core:78544 is what actually flips `isKinematic`/`useGravity`). **Which part of that state turns PhysX's depenetration of a descending kinematic capsule into a launch instead of a shove is still unidentified** — frame-time spikes (no ≥50 ms hitch logged at any of the three moments), a rigidbody kinematic-state difference (decompile shows both ends kinematic during a jump) and our own velocity writes (`TryZeroRigidbodyVelocity` already early-outs on kinematic bodies; the 379 session messages are the game's own) were each checked and ruled out. So this fixes the interaction rather than the unknown: while `isJumping`, a client calls `Physics.IgnoreCollision(capsuleCollider, <local player colliders>, true)` and hands the collision back when the flight ends, per carrier, driven from the existing `DesertPikeCarrier.Update` postfix (idempotent, null-safe, cleared on level reset). **Intentional behaviour change, client-only:** a client can now walk through an airborne pike (it loses the air wall) and in exchange cannot be launched out of the world by one; host and single-player are untouched. Nothing else about the pike changes — `IgnoreCollision` does not affect raycasts, so bullets still hit it, riders still dismount, the arc is identical. A player-velocity clamp was considered as a category-level net for "player thrown out of the world" from any source and **rejected by the user**: this game has many legitimate speed boosts, so a clamp would break real play. `NetBossEncounterManager` (guard + local-player collider resolution + reset), `BossEncounterPatches` (`Desert_PikeUpdate_Post` now takes `__instance`). No protocol change, no config. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **NOT yet verified in-game** — the test is exactly the case that reproduced three times: as a **client**, trigger an ambush pike and stand on its landing point through the descent. Expect no launch, and `[PikeCollision] client ignoring pike↔player collision` per flight with `LogBossEncounterSync` on. Still open and worth a separate test the user was asked for: whether a HOST standing on its own pike's landing point in co-op is launched too — if it is, the cause is co-op-wide rather than client-puppet-specific and this guard only covers half of it | · committed `69979b3`
| **PK-4b** | **Cover every solid collider on the pike, not just the carrier's flight capsule** (PK-4 follow-up, Log491). PK-4's guard did fire — `[PikeCollision] client ignoring pike↔player collision` on all five flights — and it visibly changed the interaction: the user reported that what they now run into "isn't a wall any more, it's a capsule shape", but a launch could still happen (and only sometimes). So the pike carries more than the one collider PK-4 addressed: PK-4 passed only the carrier's private `capsuleCollider` field, while the pike Unit has its own `mainCollider` (and potentially others on the object). Instead of guessing which one remains, the guard now collects **all non-trigger colliders** under the pike (`GetComponentsInChildren<Collider>(true)`) and ignores each against the local player's set — triggers are deliberately excluded so the ambush trigger keeps working. Two robustness changes came with it: the ignore is **re-asserted every frame** while `isJumping` (Unity drops an ignore pair when either collider is disabled and re-enabled, which the pike does during the arc — the flight capsule switches on at `JumpTowards`, riders dismount mid-air), and both collider sets are **logged by type and name** on the first frame of each flight so the next log says exactly what was covered on each side rather than a bare count. The player side is unchanged (`mainCollider` + whatever sits on the Rigidbody's GameObject, one collider in Log491) — the evidence says it is working, and if it turns out to be incomplete the new log line names it immediately. Same intentional, client-only trade as PK-4. `NetBossEncounterManager` only. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **NOT yet verified in-game** — as a client, stand on an ambush pike's landing point through the descent; expect no launch and one `[PikeCollision] … pike[…] player[…]` line per flight naming both sets. If a launch still happens, the remaining suspect is the **riders**: they dismount at the arc's midpoint (`UpdateJump` calls `JumpOff` at `num > 0.5`, i.e. exactly when the descent starts, which is the phase the user reports) and `JumpOff`'s landing callback re-enables each rider's main collider right where the player is standing | · committed `69979b3`
| **PK-4c + PK-5** | **Finish the pike-collision coverage, and stop touching a mounted rider's AI at all** (Log492: three ambushes, **zero launches** — PK-4b's coverage worked — but the player still ran into something, and eleven `Rotation quaternions must be unit length` appeared on the client with **zero** on the host). **PK-4c:** the per-flight collider dump PK-4b added showed exactly what was missing — `pike[BoxCollider:Sprite, CapsuleCollider:…Digga(Clone), …, CapsuleCollider:Collider]` is all riders plus the flight capsule, with the pike Unit's own body capsule absent. The carrier component sits on a CHILD object, so `GetComponentsInChildren` could not see a collider on its parent. Collection now also starts from the carrier's `owner` Unit so the whole pike is covered — and deliberately not from `transform.root`, because units are parented under GameManager's shared unitRoot and a root sweep would pull in every enemy in the level. (The same dump also confirmed the player side was right all along: one `CapsuleCollider:Unit_Player(Clone) 0`.) **PK-5:** the quaternion error was not ours — the PK-2 guard counters stayed at 0, so nothing wrote a bad rotation through our facing path; the stack is A*'s `AIBase.FinalizeRotation → Rigidbody.MoveRotation` as before. The mechanism: `JumpTowards` mirrors the carrier (`localScale.x = |x| * (inverted ? 1f : -1f)`, i.e. a **negative** x scale whenever `inverted` is false — both of this session's replays were `inv=False`), and a rider parented under a mirrored transform has a non-orthonormal world matrix, so reading `transform.rotation` there yields a non-unit quaternion. A* reads precisely that every physics step (`RichAI.MovementUpdateInternal`: `if (updateRotation) simulatedRotation = tr.rotation`) — but only while its AIBase is enabled, which vanilla makes sure it is not: `DesertPikeCarrier.SetMovement` disables the rider's RichAI when it attaches. On a client our puppet suppression put it back (`SetCanMove`/`SetNavMeshAgentState`/`ToggleRVO` in the intent-driven branch), which is why the host never logs this. So `ApplyClientEnemyPuppetMode` now returns immediately for a unit with `isAttachedToUnit` — the same single-ownership rule PK-1 applied to a mounted unit's transform, extended to its AI state: while the carrier holds it, the carrier owns it completely. The record simply stays un-applied (`record.Applied` is set after the guard), so the ordinary puppet drive picks the rider up on the frame after vanilla clears the flag on dismount. **The negative-scale chain is a hypothesis** — it explains the client/host asymmetry and every observation, but the log cannot name which mounted rider produced the quaternion; the guard is correct on ownership grounds regardless of whether that chain is the whole story. Counter `mountedSuppressionSkipped` rides the Phase5.2-diag line. No protocol change, no config. `NetGameplayProbeManager` (PK-5 guard + counter), `NetBossEncounterManager` (`CollectPikeColliders` now owner-rooted). | built 0 error · full rebuild 451 warnings = baseline (0 new) · **NOT yet verified in-game** — as a client, take an ambush pike's descent standing on its landing point: expect the `pike[…]` list to now include the pike Unit's own capsule, no "air wall" left from the pike itself, no launch, and **no `Rotation quaternions` in the client's Player.log** (the decisive signal for PK-5, since the counters only prove our own writes were clean). `mountedSuppressionSkipped` should be non-zero whenever pikes carry riders | · committed `69979b3`
| **PK-5b** | **Stop re-enabling A* on the pike itself** — the actual source of the "Rotation quaternions must be unit length" flood and the client freeze (Log493: the client hung twice, both times on touching a pike; `FrameHitch dt=8581ms` and `dt=31941ms`, 27 errors in `Player.log`, and the BepInEx log simply ends mid-line). PK-5 was aimed at the mounted riders and it does fire — `mountedSuppressionSkipped=23135` — but the flood survived it, which rules the riders out and leaves the pike. The chain: `DesertPikeCarrier.AttachUnit → SetMovement` shuts the pike's A* down when the carrier takes ownership (RichAI component disabled, `SetNavMeshAgentState(false)`), while our client puppet suppression turns it straight back on in the intent-driven branch (`SetCanMove`/`SetNavMeshAgentState`/`ToggleRVO`); and `JumpTowards` mirrors the pike's transform — `localScale.x = |x| * (inverted ? 1f : -1f)`, so **any `inv=False` jump leaves a negative x scale that nothing ever flips back**. A mirrored transform has a non-orthonormal world matrix, so `tr.rotation` is a non-unit quaternion, and A* reads exactly that (`RichAI.MovementUpdateInternal`) and hands it to `Rigidbody.MoveRotation` every physics step. That explains all three observations at once: client-only (the host never puppet-izes, and logged 0 errors), triggered by touching the pike (the contact is what gets the agent updating again), and escalating to multi-second frames because Unity prints a full native stack per occurrence per step. Fix mirrors PK-5 one level up: a unit carrying a `DesertPikeCarrier` is owned by that carrier, so `ApplyClientEnemyPuppetMode` returns for it — and, because an earlier frame may already have enabled it, actively puts the navmesh/RVO back off first rather than merely skipping. This is the AI-state twin of the position exemption pikes already had (`IsDesertEnemyPikeSnapshot`). Counter `carrierDrivenSuppressionSkipped` joins `mountedSuppressionSkipped` on the Phase5.2-diag line. Also confirmed by the same log: no launches (PK-4/4b/4c holding), and the `pike[…]` collider dump is unchanged in shape, so `CapsuleCollider:Collider` is the pike's own body collider and PK-4c added nothing new — harmless, and it removes the doubt. No protocol change, no config. `NetGameplayProbeManager` only. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **NOT yet verified in-game** — the decisive test is to walk a client into a pike (ideally one that has already made an `inv=False` jump) and watch for `Rotation quaternions` in the client's `Player.log`: it must stay at 0, with `carrierDrivenSuppressionSkipped` non-zero. If the flood still appears, the mirrored transform is being read by something other than A* and the negative-scale chain needs to be attacked at the source instead (e.g. restoring the pike's scale sign after each flight) | · committed `69979b3`
| **SEED-1** | **Release `GlobalSettings.ForceLevelSeed` once its generation has consumed it.** It is written when a client follows the host into a level so the client reproduces the host's instance, but nothing ever wrote it back to 0 — and `StartLevelRoutineGraph` only rolls a fresh random seed when the field reads 0 (`long num2 = GlobalSettings.ForceLevelSeed; if (num2 == 0L) num2 = new System.Random().Next(...)`). So after a single follow, **every later LOCAL generation on that client was pinned to the last host seed**. Two consequences, one observed and one structural: a client whose relayed level-complete timed out and fell back to generating on its own produced the *previous* level's seed (Log516: client `Act_01_Caves:1` on `386868468`, the level-0 seed, against the host's `-1839793949`), which turns divergence from a risk into a certainty; and an unlinked client playing solo would regenerate the same map every level. Released at the generation-input finalize point (`FinalizeFromMakerContext`, i.e. the first graph node), where the graph has already consumed it and `GameManager.currentSeed` is fixed. `TryApplyForceLevelSeed` now records what it wrote and `ReleaseAppliedForceLevelSeed` clears **only that exact value**, so a seed set by the game's dev tools is never taken out from under it. No protocol change, no config. `NetLevelSeed`, `NetGenerationInputCapture`. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **verified in-game (Log517)** — `[LevelSeed] released ForceLevelSeed=… (consumed by generation) source=generation-finalize` fires once per level load on both ends, and no level after the first reuses a previous seed. Not separately exercised: the unlinked-solo-client case (same code path, but nobody played a solo run to confirm each level now rolls a fresh map) · committed `790cd02` |
| **AWAIT-1** | **Model the press-to-continue window as its own state.** `LevelGeneration.ShowLevelNode` (the last generation node, decompiled from `PerfectRandom.Sulfur.LevelGeneration.dll` — not previously kept in `D:\SULFUR_backup/decompiled`) parks on `while (gameManager.awaitingStartLevel) yield return null;` and only *afterwards* flips the world live: `Physics.simulationMode`, `SetState(Running)`, `SetTimeScale(1f)`, `LoadingFade(false)`, and `npcs[j].enabled = true; aliveNpcs.Add(...)` for every NPC. A peer parked there therefore has a **fully generated map** while reporting `GameState=Loading`, running at `timeScale 0`, with no active enemies — indefinitely, because it is waiting on a keypress. The scene-authority code modelled loading as one boolean off `GameState`, which made this indistinguishable from "still generating"; that conflation is the shared root of AWAIT-2 and AWAIT-3. `NetAwaitStartLevel.IsLocalAwaitingStartLevel` is a **read-only view of the game's own flag** — the game stays the sole owner, nothing is mirrored — and deliberately **local-only**, since every consumer asks about the peer it runs on, so there is no wire format and no protocol bump. Observation only; a `SetAwaitBeforeStartLevel` postfix logs `[AwaitStart] entered/left` so transition logs show the window explicitly. `NetAwaitStartLevel` (new), `ReverseProbePatches`. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **verified in-game (Log517)** — `[AwaitStart] entered/left press-to-continue scene=… role=Host/Client` brackets every level load on both ends, including the parked windows AWAIT-2/3 act on. No behaviour change by design · committed `4cdf06d` |
| **AWAIT-2** | **A client relay may lead a host parked at press-to-continue** (reported: "if someone is still on the loading screen and the other walks ahead, players/map/enemies all desync; it fixes itself when someone reaches the next level, and it doesn't seem to matter who is hosting"). `HandleClientTransitionRequest` deferred whenever `host.GameState.Contains("load")`, which lumped a host that is genuinely mid-generation together with one parked on the press-to-continue screen. The parked case has no exit — the host is waiting on a keypress — so every relay was refused for as long as it stood there, the client retried to its 15s `ClientInitiatedLoadTimeoutSeconds`, and `DoClientInitiatedLocalFallback` advanced it locally into its own instance. Reproduced exactly in Log516: eight consecutive `[TransitionRelay] defer …: host is loading (state=Loading)` on the host against `attempt=1…8` on the client, then `timeout waiting host lead for client level-complete; advancing locally`, ending with the two ends on different seeds for the same level; the client was then yanked *back* to level 0 by the host's next gen-input broadcast. A parked host has not entered the level (timeScale 0, enemies inactive), so refusing preserves nothing — it is now led away and both ends generate the next level from the same host seed. Genuine mid-generation still defers and `NetHostTransitionGuard` is untouched. **Behaviour choice** (confirmed with the user): the parked host is simply taken along, rather than making the client wait and adding a "the other player wants to advance" prompt — that alternative needed the window on the wire plus a protocol bump and would stall the client on an AFK host. `NetService` only. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **verified in-game (Log517)** — host parked on the press-to-continue screen, client walked into the exit: `[TransitionRelay] host parked at press-to-continue (state=Loading); leading anyway target=Act_01_Caves:1` followed immediately by `host LEADING client-requested transition … attempt=1`. Zero `defer …: host is loading`, zero `advancing locally`, no 15s stall, both ends on one seed — against Log516's eight defers plus the local fallback on the same scenario · committed `2aa2e90` |
| **AWAIT-3** | **Retire the abandoned `ShowLevelNode` when a parked peer is led away** — the other half of the same report, and what made the symptom look like corruption rather than a plain desync. `StartLevelRoutineGraph` opens with `ClearLevel(); SetAwaitBeforeStartLevel(false);`, which **releases the OLD node's wait**; nothing has ever stopped that coroutine (no `StopAllCoroutines` on the transition path), so it resumed during the new level's teardown and ran its tail against the wrong level — `SetState(Running)` mid-generation, the freshly raised black fade torn off while the loading overlay stayed up (the user's screenshot: a half-built level visible behind the "press to continue" art at `17/17`), and the previous level's already-destroyed NPCs re-enabled and pushed back into `GameManager.aliveNpcs`. Log516 shows the whole chain on adjacent lines: `[GM] ClearLevel <<` → `[GM] SetState << Running` ×2 → `[NpcListSweep] removed 6 destroyed Unit(s) from GameManager.units/aliveNpcs`, with the entire next generation (`idx=2/17 … 17/17`) running at `GameState=Running`. That sweep was added for unrelated NRE reasons and had been silently absorbing this defect all along, which is why it never crashed. Fix: `SwitchLevelRoutine` — the authoritative entry every transition route passes through, and which fires ~0.5 s *before* the graph releases the wait — arms a one-shot abandon when this peer is parked, and the stale node's next `MoveNext` returns false, ending the coroutine before its tail runs. Arming is a no-op on a normal transition (the player already pressed the key), and a 10-frame budget drops an arm that is never consumed so it can never reach the incoming level's own `ShowLevelNode`. Nothing is lost by cutting the node short: `Physics.simulationMode`, timeScale, game state and the fade are all re-applied by the incoming node, and `SetAwaitBeforeStartLevel(false)` still restores input and clears the prompt. Applied unconditionally — functional, not diagnostic. `AwaitStartLevelPatches` (new), `PatchBootstrap`, `ReverseProbePatches`, `Docs/SceneTransitionAndLinkState.md` §8b. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **verified in-game (Log517)** — `[AwaitStart] patched LevelGeneration.ShowLevelNode+<Execute>d__0.MoveNext` at startup on both ends; on the parked transition, `transition while parked at press-to-continue — retiring stale ShowLevelNode source=SwitchLevelRoutine` → `retired stale ShowLevelNode … its tail will not run against the new level`. `[GM] ClearLevel <<` is no longer followed by `SetState << Running`, and the `NpcListSweep removed 6 destroyed Unit(s)` line is gone. Zero `arm expired unused` · committed `892ffff` |
| **GH-1** | **Ghost species follows the level seed** (reported after AWAIT-1..3 landed: "there are still enemies the client can't see hitting him"). `LevelGeneration.SpawnEnemiesNode.SpawnGhost` takes *how many* ghosts, *which rooms*, and their scatter offsets from the node's seeded RNG (`base._Random`), then picks the species from the **global** `UnityEngine.Random`: `switch (Random.Range(0, 3)) { Apparition / Poltergeist / Wraith }`. Same seed, so both peers agree a ghost exists and where it stands and disagree on what it is two times out of three. A type mismatch is refused by the roster binder, which leaves the **host's** ghost with no representation on the client while it stays alive and hostile — an enemy the client cannot see but can be killed by. Log517: `[WorldRoster] No local match for hostIdx=1 hostUnit=GhostPoltergeist` against `Client-only non-combat localIdx=1 unit=GhostWraith`, identical positions `(317.7,416.4,-1625.7)`; Log516 shows the same shape with `GhostApparition`/`GhostWraith`, so this predates the AWAIT work. **This is also the real cause behind issue #6 "ghosts desync"**, where the `RandomlyKeepChild` hypothesis was disproven by a probe that never fired — the species roll was never in that code path. Fix: seed the global RNG from the level seed for exactly the `MoveNext` that runs the species pick (resolved through the `<SpawnGhost>d__X` state machine, latch reset per generation at `StartLevelRoutineGraph`), then restore the previous `UnityEngine.Random.state`. **Scoping to that one call is the design point** — the obvious alternative, seeding the whole generation pass, silently requires both peers to issue the same number of `UnityEngine.Random` calls throughout generation, which nothing enforces and whose failure leaves no log trace. A full audit of `LevelGeneration.dll` found only two global-RNG uses in generation: this one and a material `_Seed` (purely visual). **Intentional behaviour change, single player included:** ghost species becomes a function of the level seed instead of being re-rolled per playthrough; distribution unchanged (still uniform over three) and consistent with every other random choice in the same method. Deliberately not gated on multiplayer — that would make one seed mean different things in the same install depending on session state. **Deliberately NOT fixed:** `AiAgent.AssignStartRole` has the identical defect (`UnityEngine.Random.Range(0, rolesAvailable.Count)`, the source of the `Offensive`/`Defensive` modifier mismatches in the same manifest diffs, and the root behind issue #9's civilian/spearman flicker which was only ever fixed at the presentation layer) — client enemies are host-driven puppets, so a role disagreement does not break binding. No protocol change, no config. `GhostSpawnPatches` (new), `PatchBootstrap`, `ReverseProbePatches`. | built 0 error · full rebuild 451 warnings = baseline (0 new) · **verified in-game (Log518)** — `[GhostSpawn] patched … <SpawnGhost>d__7.MoveNext` at startup on both ends; `seeding ghost species from level seed=… derived=…` fired on every generation with **all five shared levels producing an identical derived value on both ends** (`-1830181039`, `-780526`, `-1648928171`, `-714493871`, `1776828609`). Not one `Ghost*` unit mismatch in the whole session, against Log516/517 where every ghost level had one; each level's settled manifest diff reads `mismatch=0 hostOnly=0 clientOnly=0`. Zero errors, no `seed failed` / `RNG state restore failed`. Still present as expected: `Offensive`/`Defensive` modifier mismatches (AssignStartRole, untouched) and a pre-existing **room** divergence (`CaveSideRoomChests` vs `ToiletExplodableRoom(CavesVariant)`) that Log516 already shows, so it is not from this change | committed `6bfa1e5` |
| **ST-1 / ST-2** | **Enemy status effect authority** (feedback: a client's Petrification enchantment shows the petrified material + shatter VFX but the enemy keeps walking; ~always on the labyrinth's undying enemy, intermittently elsewhere). Root cause: applying a status and applying damage are two INDEPENDENT calls — `ProjectileUtilities.ProcessUnitHit` runs `Unit.ApplyHitModifiers` (the enchantment proc, -> `EntityStats.ModifyStatus`) and only then `ReceiveDamage`. The damage half was already host-authoritative, but the host applies a `ClientHitRequest` with a raw `Stats.SetStatus(92)` health write that never runs `ApplyHitModifiers` — so a client's status landed on its **local puppet only**, and `AttributeEffect.UpdateMovementSpeed`'s `SetUnitSpeed(0)` only ever runs on the machine holding the status, while the puppet's position is host-driven. Affected every enchantment status from a client (fire/frost/poison/stun/charm/root/...), not just petrification. **ST-1 (client->host, msg 98):** prefix on the single chokepoint `Unit.ApplyHitModifiers` — on a client, a modifier list aimed at a host-bound puppet is never applied locally; the client rolls each `procChance` (unconditionally, in vanilla order, so the shared global `UnityEngine.Random` stream advances identically) and forwards the passing entries; the host applies them to the real NPC through the vanilla `ModifyStatus`, keeping resistances / diminishing returns / the protected-NPC guard / the per-attribute cap host-owned. Fail-closed forwarding (only a positively identified local-player attacker); a non-player source is still suppressed locally but not forwarded — the host simulates it and the result returns via ST-2. **ST-2 (host->all, msg 99):** postfix on `Unit.OnStatusUpdated` (the same callback the game uses to raise/remove an effect's own presentation) broadcasts only the START/END **edges** of a `NegativeEffect_*` status on a roster-bound NPC — the per-frame decay runs locally on every end. Clients write it with `SetStatus` (absolute, owner callback on) so the vanilla effect plays on the puppet. This makes the host the single authority for what every screen shows: it re-asserts over any locally-applied divergence on the next edge, and it fixes the converse gap (a status the HOST applied was previously invisible on the client — the enemy just stopped moving). Attribute whitelist derived from the enum's own `NegativeEffect_` naming, not a literal id table (the enum is versioned). Host-side validation: scene, spawn-index + UnitIdentifier type guard, alive, whitelist, value range, per-peer arrival budget. Status attribution on the host uses its own `PlayerUnit` (this end has no Unit for a remote player; the one vanilla consumer, frozen-solid's shatter kill, already falls back to exactly that unit). Also guards the new terminal ST-2 makes reachable on a client: a mirrored `Frozen` hitting 100 runs `DelayedFrozenSolid` -> `float.PositiveInfinity` damage, which is now swallowed instead of forwarded as a hit request (the host's own copy kills the NPC authoritatively). **Post-verification hardening (same slice):** (a) `ApplyHitModifiers` can carry ANY `EntityAttributes` (its data is `ItemAttribute.applyAttributeModifier` / an NPC's `modifiersOnHitOverride`), so the first cut's whitelist deleted off-list modifiers outright — suppressed locally *and* not forwarded. Off-whitelist attributes are now applied right there in the prefix exactly as the suppressed vanilla body would (`RegisterAppliedStatus` + `ModifyStatus`). Deliberately not widened into the wire format: letting a client address arbitrary attributes on a host unit would hand it `Status_CurrentHealth` and every stat. (b) Counter split (`clientNoProc` / `clientNotLocalAttacker` / `clientLocalOnly`) — a low forwarded/intercepted ratio was ambiguous between an enchantment's proc chance and a failed attacker identification. (c) The client drop log carries the value + start/end. ProtocolVersion->30. Always on; `LogUnitStatusSync` is diagnostic only. `UnitStatusSyncManager`, `NetUnitStatus`/`NetUnitStatusCodec`, `UnitStatusPatches`. | built 0 error - full rebuild 451 warnings = baseline (0 new) - **verified Log519** (both ends `Patched ApplyHitModifiers(True)/OnStatusUpdated(True)`; ST-1 client sent 25 -> host applied 25, **0 rejected**; ST-2 66 edges = **33 start / 33 end, perfectly paired**; client applied 55; **0 errors both ends**. Full round trip visible in 3 client lines: `client->host seq=1 hostIdx=8` -> `client applied ... =6.0`. The converse fix is visible too: the client mirrored a HOST-applied petrification (client line 2104) before it had fired a single request of its own. User confirms in-game: **a petrified enemy really does stop**. All 11 client drops are the host's `Npc.Die` -> `SetStatus(0)` end-edge landing after the death mirror already released the puppet — harmless, the enemy is dead.) Untested: the off-whitelist local-apply branch (Log519 carried only `NegativeEffect_Petrified`), and the labyrinth's undying enemy (only Fortress dummies + Caves grenadiers were run) |
