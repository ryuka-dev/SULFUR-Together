# Boss Multiplayer Authority

Implementation of host-authoritative boss fights (Phases 5.4-E → 5.4-G7, plus 5.5-RT runtime spawn). The reverse-engineered references this is built on live in **`BossSourceAudit.md`** — read that for exact method/field names and per-boss mechanics. This doc is the *architecture + per-subsystem map*.

> Messages: `ClientBossStartRequest=28`, `HostBossEncounterStart=29`, `ClientBossDialogCommitRequest=30`, `HostBossDialogCommit=31`, `HostBossState=32`, `HostBossDynamicSpawn=33`, `ClientBossHitRequest=34`, `HostBossHitVisual=35`, `HostBossDiscreteEvent=36`, `ClientLuciaEyeReport=37`, `HostLuciaEyeState=38`, `HostLuciaDeath=39`, `HostWitchPhase=40`, `HostWitchP2Manifest=41`, `HostWitchP2Result=42`, `HostRuntimeSpawn=43`. Config: `EnableBossEncounterSync` (+ per-feature flags below).

---

## Core principle

The **host runs the real boss fight**; the client mirrors it. Every mechanic-advancing action goes through the host's real game code:
- Real damage entry is **`Unit.ReceiveDamage`** (fires `onDamageRecieved`). `SetStatus` only sets HP and does **not** advance phase/death mechanics (5.4-E5 audit finding).
- The client never computes boss damage, phase changes, spawns, or death locally — it reports intent and applies host-authoritative results.

### Adapter pattern — `IBossEncounterAdapter`
Each boss family has an adapter (`WitchBossControllerAdapter`, `CousinHelperAdapter`, `LuciaBossFightHelperAdapter`, `EmperorBossAdapter`, generic `BossFightHelperAdapter`). `NetBossEncounterManager` is the message hub; `BossReflect` is the reflection helper (`DeclaredOnly` to handle `new`-hidden members). Adapters resolve roles and host targets; the manager owns the wire protocol.

---

## 1. Encounter start (5.4-E / E2 / E3)

Bosses start via a host-authoritative handshake so both ends begin the fight on the same frame:
- Client triggers a boss → `ClientBossStartRequest(28)` → host runs the real start chain → `HostBossEncounterStart(29)`.
- An **authorized continuation window** keeps a cross-frame start chain from being blocked (5.4-E2). Real start chains per boss; Emperor's real type is `PerfectRandom.EmperorBossFightHelper`.
- **Dialog-gated bosses** (Cousin / Lucia / Desert): the real dialog is closed with `DialogueTree.currentDialogue.Stop(true)`; commit is `ClientBossDialogCommitRequest(30)` → `HostBossDialogCommit(31)`, with duplicate dialog-entry suppression. Lucia uses `new TriggerFight`.
- Presentation (`EnableBossClientPresentation`, **default false**): the client's boss-intro animation chain does not fully run, and `DoneAppearing` (an animation-event that clears invulnerability) never fires → client-side boss owner stays permanently invulnerable. So client presentation is suppressed by default; only safe visuals (e.g. Lucia hit flash) are kept (5.4-F3).

---

## 2. Damage authority — `BossDamageAuthority` (5.4-F)

```
client hits boss main body
  → Npc.ReceiveDamage prefix → TryClientBossHit (boss-claim takes priority over the generic puppet ClientHitRequest)
  → ClientBossHitRequest(34){role}
  → host BossDamageReflect → real Unit.ReceiveDamage(IDamager overload, source = GameManager.Instance.PlayerUnit, NOT Player)
  → boss mechanic advances + ~0.4s later HostBossState(32) syncs health
```
- Adapter `ResolveHitTargetRole` (client) / `ResolveHostTargetForRole` (host) map a hit to the right host Unit. `main` ↔ `GetHealthUnit`.
- Feedback (5.4-F2): `HostBossHitVisual(35)` → client plays the native white flash (visual only, no re-damage).
- **Witch routing (5.4-G):** the player actually shoots each phase controller's `witchUnit` (not `witchMainUnit`); its `onDamageRecieved` routes to `OnDamageMainWitch` (+ Phase4 `RegisterInstance`). The adapter routes a client hit on the phase1/3/4/5/6 witch to the host's corresponding `controller.witchUnit` so the main HP and the phase mechanic advance together, reusing the same `BossDamageAuthority` pipeline.

---

## 3. Phase authority

### Witch phases (5.4-G2) — `HostWitchPhase=40`
Witch phases **cycle** (Phase6 → Phase1) and the client would self-advance and desync. Fix: patch `ChangePhase` — prefix **blocks** the client's self-advance; postfix (host) broadcasts `phase + monotonic revision`. The client applies by revision (`EndCurrentWitchPhase` outside the reentry, `ChangePhase` inside). `HostBossState` then only carries health.

### Witch Phase 2 — true/illusion domes (5.4-G4 / G5 / G6)
The real witch among the domes is the host's two random rolls; the client must learn the **real dome index**:
- Host captures `realDome = IndexOf(realWitchUnit)` at the `ShowWitches` postfix → `HostWitchP2Manifest(41)`.
- Client blocks its local `ShowWitches` and mirrors: real witch into the host's real dome, illusions elsewhere.
- Hits route as `p2dome:N` role (boss-first); host calls `spawnedWitches[N].ReceiveDamage` → real `Real/IllusionTakeDamage` → `HostWitchP2Result(42)` → client hides.
- Lifecycle hardening (5.4-G6): a dedicated `_witchP2Cycle` (per `ShowWitches`) + `_witchP2Active` gate the result broadcast so it fires once per real transition; `HidePhase2Witches` on leaving Phase 2.

---

## 4. Dynamic / runtime spawns

### Boss-owned adds (5.4-E4) — `HostBossDynamicSpawn=33`
Boss sub-units (Terrorbaum/Lucia henchmen/maidens, CousinArm, LuciaEye, Witch illusions) spawn at runtime and the two ends choose differently. Chokepoint: hook **`UnitSO.SpawnUnit`** (its `mono` = the owning boss). The host records `(encounter, addType, seq)` and broadcasts; the client binds `local[seq] ↔ host[seq]` (same-position adds can only be bound by sequence — proven by LuciaEye). Health sync via `SetStatus` (92) + fire `onHealthChange` + attach bar once (E4.2).

### General runtime spawn sync (5.5-RT) — `HostRuntimeSpawn=43`
Architecture for **all** post-stabilization spawns (boss adds + F3 player spawns): host spawns → broadcasts `unitId.value + pos + hostSpawnIndex` → client `unitDatabase[UnitId(value)]` → `UnitSO.SpawnUnitAsync` mirror + `RegisterMirroredRuntimeSpawn` binds the host SpawnIndex into the existing puppet pipeline. Unified hook = `UnitSO.SpawnUnit`; F3 = `DevToolsManager.Spawn`.
- RT3: boss-mono adds reuse the E4 path (`NetBossDynamicSpawn` += `UnitIdValue + HostSpawnIndex`); client `FinishBind` → `RegisterMirroredRuntimeSpawn` reuses the local add as the host puppet (no double-spawn / NRE). Special-exclude set `{LuciaEye, CousinArm}`.
- Config `EnableRuntimeSpawnSync`.

---

## 5. Discrete events & terminal death

### Cousin (5.4-F4) — `HostBossDiscreteEvent=36`
Cousin is a **fixed-pool** event boss; both ends independently pick a pool (Submerge/MoveToNewPool/Reappear) and surface at different pools. Host is authoritative: broadcasts `NetBossDiscreteEvent`; the client prefix **blocks** its own choice and mirrors the host's pool (`MoveToNewPool` → `SetNewPool` + `TeleportTo`, matched by `cousinPosition` world pos since pool indices are unstable). **CousinDeath** reuses msg36 (`EventName=CousinDeath`): client `owner.Die()` runs the real death (elevator/health bar/music/hand cleanup) + marks `_terminalDead` to suppress later hit/state.

### Lucia eyes (5.4-F5 / F6) — `ClientLuciaEyeReport=37`, `HostLuciaEyeState=38`, `HostLuciaDeath=39`
Eye phase (`currentPhase==5`) count/cycle loop: client `EyeDied` prefix intercepts → reports (37), does NOT locally `RestartPhases`; host consumes its first living `LuciaEye` (`owner.Die()`) to trigger the real `EyeDied/RestartPhases`; postfix broadcasts remaining (38). Cycle-complete: client calls the real `RestartPhases` (recenter / leave phase5 / multi-wave re-entry). Terminal death (39): patch-overridden `OnBossDead`, prefix isolates the client's loot/save (`PlaceLoot/SaveCheckpoints/SaveBackup`), client `bossUnit.Die()` real death + presentation subset, reuse `_terminalDead`.

### Witch death (5.4-G7) — `EnableWitchDeathFix`
Client `WitchDeath` crashed because `witchMainUnit` is roster-bound → generic `EnemyDeathMirror` `Die()` → `WitchDeath` → `EquipmentManager.AmuletHoldable` getter → `equippedItems[InventorySlot.Amulet]` `KeyNotFoundException` (the client has no Amulet slot key). Fix (G7b): **prefix blocks the client's `WitchDeath`** + `adapter.TryApplyWitchDeath` runs a reflection-based safe replica (teleport back / health bar off / fire / music / environment / clear adds; **skips amulet + PlayerProgress**; `ChangePhase Dead` handled by G2) + `MarkWitchTerminal`; postfix (host) marks terminal.

---

## 6. Per-boss quick map

| Boss | Real helper type | Damage target role | Phase mechanic | Special spawns | Death |
|------|------------------|--------------------|----------------|----------------|-------|
| Witch | `WitchBossController` (+ per-phase `witchUnit`) | per-phase `controller.witchUnit` → `OnDamageMainWitch` | phases CYCLE; revision-driven (`HostWitchPhase`); Phase2 dome manifest | Phase2 illusions, Phase6 egg (TODO), adds | G7 safe replica (`TryApplyWitchDeath`) |
| Cousin | `CousinHelperAdapter` | main | fixed-pool discrete events | CousinArm | `CousinDeath` via msg36 |
| Lucia | `LuciaBossFightHelperAdapter` | main + eyes | eye count/cycle (`currentPhase==5`) | LuciaEye, henchmen | `HostLuciaDeath` (msg39) |
| Emperor | `PerfectRandom.EmperorBossFightHelper` | main | (diagnostic) two-worm | worms | (extension point) |
| Desert | `DesertClauseBossFightHelper` via `BossFightHelperAdapter` | main (`bossUnit`) | `BossPhase` (health-%), **host-authoritative** (client suppressed) | pikes / mount / diggas / missiles / saddled-pike adds (all synced) | (extension point) |
| Terrorbaum | `TerrorbaumBossFightHelper` via `BossFightHelperAdapter` (`TriggerFight`, non-dialog) | main | client boss is a puppet (host-driven, attacks synced) | craws (`CrawWatcher` flyers) via RT3 | (extension point) |

### Desert (host-authoritative composite)

The Desert boss (`DesertClauseBossFightHelper`) is a composite: an old-man body (`bossUnit`/`bossNPC`) on a pike **mount**, a moving **sandstorm arena** (`DesertClausePerimeter` — a `SphereCollider` danger zone), spawned pikes/diggas, and pre-placed sniper/terminator missile bases. Its fight is **host-authoritative** and the client boss is a **passive puppet**: the client's per-frame phase logic is suppressed so it can't run a divergent local fight, and everything visible is driven from the host. Full breakdown in **§9**.

---

## 7. Files

`src/Networking/Gameplay/Boss/`: `NetBossEncounterManager.cs` (hub), `NetBossEncounterMessages.cs`, `IBossEncounterAdapter.cs` / `BossAdapterBase.cs`, the per-boss adapters, `BossReflect.cs` / `BossDamageReflect.cs` / `BossDialogReflect.cs`, `BossDynamicSpawnManifest.cs`, `BossLifecycleProbe.cs`, `WitchPhase2Probe.cs`, `EmperorWormDiagnostics.cs`, `BossTypeDiscovery.cs`. Patches: `BossEncounterPatches.cs`, `BossSpawnPatches.cs`. Runtime spawn: `RuntimeSpawnManager.cs`, `NetRuntimeSpawn.cs`, `BossSpawnPatches.cs`.

---

## 8. Known gaps

- Witch Phase 6 egg; precise Phase 2 `EightFlying` timing; black-ripple VFX; phase-witch positions (mostly OK, occasionally off).
- Emperor phase/death is diagnostic-only (extension points present).
- Boss adds: RT3 covers boss-mono adds; non-boss-mono adds (e.g. Witch egg whose `mono` is GameManager/phase) go through the general per-unitId-seq hook (RT3b).
- Host hitting a boss while the client watches: a minor "no feedback on client" cosmetic gap was deferred.

---

## 9. Desert boss (LD-Sandstorm / F4)

The Desert boss combines the sandstorm arena keep-in (LD-Sandstorm) with the client-presentation problem (F4). Reverse-engineering lives in `BossSourceAudit.md`; the sandstorm arena in `BossPreFightFlow.md`. **The fight is now fully synced end-to-end** (intro → arena movement → dialogs → pikes/dismount → missiles → adds → downed rescue); the remaining items under "Open / known divergences" are deferred cosmetics/refinements, not blockers.

**Arena (LD-Sandstorm).** Gate-less: the battlefield is ringed by a moving `SphereCollider` "danger zone", not a door, so LD-2's seal/barrier/popup do not apply. In/out is the game's own test — `Distance(unit, sphereCollider.transform.position) > SphereRadius` (`SphereRadius = radius * lossyScale.x`, read live so it tracks the moving/resizing sphere) via `BossFightHelperAdapter.TryGetSandstormArenaSphere`. At fight-start (the boss dialog trigger) the host arms an `ArenaLockdownManager` sandstorm lockdown; after `SandstormPullDelaySeconds` every end whose local player is outside the sphere teleports to a spot **beside the first in-arena player** (not the centre, where the boss sits) + a toast. One-shot pull (no continuous keep-in), except the downed-player rescue below re-pulls a downed body left outside.

**Client presentation (F4) — the boss must be visible + not fight locally.**
- **Puppet (host-driven):** the Desert boss is classified as a **`CombatEnemy`** so it enters the client puppet system — host-driven position, animation mirrored, local AI/weapon suppressed. It carries a dialog component (its mid-fight calls), which made `ClassifyEntitySyncCategory` tag it `InteractiveNpc` and exclude it from the puppet system → the client boss ran its own AI (shot the client), showed a local pose (gun instead of the walkie-talkie), and didn't follow the host. Fix: honour the game's own `UnitType.Boss` flag (`unitSO.unitType`) before the `DialogSpeaker`/`Speakable` check, so bosses always classify as `CombatEnemy`.
- **Visible:** the composite body is assembled by the boss's own intro chain (`OnStartInteractWithBoss` → intro anim → `TriggerFight`, which hides `sandSantaAnimationSprite` + sets `BossStarted`). Both the host-first start path (`TryApplyHostStart` → `OnStartInteractWithBoss`) and the client-first commit path (`TryApplyDialogCommit`, see Entry below) run the **full** `OnStartInteractWithBoss` — a bare `TriggerFight` flips `fightStarted` (health bar) but never assembles the body (Log294: health bar, no boss, no dialog).
- **Passive (no divergence):** the boss's per-frame combat AND phase transition are suppressed — `Desert_UpdatePhases_Pre` blocks `UpdatePhasesDeltaTime` / `UpdatePhasesFixedTime` / `TransitionTo` once started (`ShouldSuppressClientBossCombat`). `TransitionTo` is critical because `BossPhase` is a *separate* MonoBehaviour whose own `Update`→`TransitionTo`→`PhaseTransition()` sets the boss invulnerable, cleared only by the (suppressed) `UpdatePhases` — without blocking it the client half-transitions and freezes.
- **Position:** the intro's `RepositionBossFromCamera` places the rig 12 m in front of the camera. Suppressed on **both** ends (`ShouldSuppressClientBossReposition`) so the boss stays at its seed-placed arena position; the client boss (puppet) follows the host. Both-ends is required because the host can now run the intro for a client-triggered start while it is **out of the arena** — repositioning to the far host camera would drag the boss (and the following client puppet) out of the arena.
- **Entry (FF14 flow, incl. client-first):** the boss starts via the dialog-commit path (Desert is a dialog boss). `ApplyIntroCutsceneGated` is Cousin-only (`GatesFightOnDialogClose`); a non-gated boss (Desert) bypasses it and applies its real commit directly — otherwise the out-of-room branch force-calls Cousin's `Introduction` (absent on Desert) and the fight never starts (Log293). `TryApplyDialogCommit` runs `OnStartInteractWithBoss` on both ends, so each **runs its own intro chain** — assembling the body and opening the intro cutscene `Dialog_DesertClauseIntro` **locally** (so the intro is NOT network-synced; broadcasting it would double-open). The sandstorm pull-in is armed in the dialog-commit path (`BroadcastDialogCommitOnce` → `TryBeginSandstormArena`) so out-of-arena players (e.g. a host far from a client-first trigger) are teleported in.
- **Combat-entry (host-authoritative — the intro→combat handoff).** Desert enters real combat through `TriggerFight`, an **animation-event on the intro clip** (`base.TriggerFight` sets `fightStarted` + `StartBossPhases`); the sandstorm presentation + the release of the intro's `Cinematic` lock are two later anim-events (`Anim_OnTriggerSandstorm` → `StartSandstorm`). These fire only if the clip actually plays through locally. On a client whose local intro animation never reaches them — **host-first, where the client is out-of-arena and the boss renderer is culled so its Animator is frozen (Log298)** — `fightStarted` stays false forever, so `IsLocalIntroPresentationActive` (`_appliedStart && !fightStarted`) stays true, the boss's snapshot is skipped, and the boss is frozen out of the puppet system (no position follow, no combat, no sandstorm). Fix: make combat-entry a **second host-authoritative event**, distinct from the intro start. The host broadcasts its own `TriggerFight` once (`IsCombatEntrySource` → `BroadcastCombatEntryOnce`, on a **separate** `_hostCombatEntryBroadcast` gate — the intro's `OnStartInteractWithBoss` already consumed `BroadcastStartOnce`'s gate in the host-first case, which is why the host used to never re-broadcast). The client applies it (`HandleHostBossEncounterStart` detects a `TriggerFight` StartSource and runs it **even after** the intro start already applied) via `TryApplyCombatEntry` → invokes `TriggerFight` (idempotent) **and releases the `Cinematic` controller-lock + invulnerability** (mirroring the `StartSandstorm` tail we skip, so the local player isn't left frozen). `fightStarted` flips → the intro guard clears → the puppet resumes (host-driven position + host-authoritative combat). Client-first is a no-op (the client's own intro anim already fired `TriggerFight`, so `IsStarted` short-circuits). Trade-off: a host-first client does not replay its own 8 s sandstorm cutscene — it snaps to the host's current combat state (the in-room/catch-up model, consistent with the dialog-model divergence below).
- **Mid-fight dialog sync:** the boss opens mid-fight dialogs (airstrike / sniper / terminator) by setting `bossNPC.dialog.graph` then `bossNPC.Interact(null)`; the passive client never opens them itself. Host detects the open (`Npc.Interact` postfix → `OnHostBossDialogInteract`, gated on `SafeStarted`), identifies the graph **by name** (`TryGetActiveMidFightDialogId` — the NodeCanvas `dialog.graph` getter returns a bound runtime instance, so reference equality against the template field fails), and broadcasts `NetBossDiscreteEvent{EventName="Dialog:<id>"}`; the client sets the same graph + `Interact`. The **close** is two-way:
  - **Host→client (existing):** the host closing its own copy broadcasts `DialogClose`; the client runs `TryFinalizeCurrentDialog`.
  - **Client→host (Log323–326):** a client reading the mid-fight dialog to the end had no way to tell the host, so the host's copy stayed open (Log323). `OnLocalDialogSpeakableChanged` now tracks an **already-started** boss's open dialog in `_localMidFightDialogKey` (via the generalized `TryMatchSpeakableToBoss(... out started)` — `started=true` ⇒ mid-fight, `false` ⇒ the pre-fight intro); on a local, non-host-driven close it sends `CommitSource="midfight-close"`. The host's `HandleClientBossDialogCommitRequest` handles it — **but must NOT `Graph.Stop`**: the dialogue tree's terminal ExecuteFunction nodes (the airstrike + the boss-AI resume, per BossSourceAudit §38) run only on **natural** completion; `Graph.Stop` aborts them and the host boss freezes in place (Log324, and again Log325 when the fallback fired). Instead `FastForwardHostMidFightDialog` advances the host's own dialog one step per frame (`BossDialogReflect.TryAdvanceActiveDialog` → `DialogController.AcceptDialogOption`) until it ends naturally — so the terminal nodes fire and the boss resumes — then the native close broadcasts `DialogClose` (the client already closed → no-op). Bounded at 300 frames; a latent/choice node that can't be fast-forwarded falls back to `Graph.Stop` so it never hangs. Log326: `advances=22 frames=22 natural=True`.
  - **Two reflection pitfalls hit here (both cost a log round):** (1) the static `DialogController.instances` stack does **not** reliably hold the live controller — only `Resources.FindObjectsOfTypeAll` reaches it (`BossDialogReflect.EnumerateControllers` unifies both, used by the reset and the advance). (2) `Graph.Stop` closes a dialog **without** running the choice-accept path, so it leaves `DialogController.InChoices=True` dangling; the client-first intro finalize left the host stuck in "waiting for a choice" mode and froze the *next* (airstrike) dialog's opening statement (Log320–322). `TryFinalizeCurrentDialog` now calls `ResetDialogChoiceState` (clears `InChoices`/`selectedButton` on every controller, resolving the fields on each instance's **actual** runtime type since they're private on a base class). Guard: the client sets `_applyingHostDialogClose` while finalizing a host-driven `DialogClose` so its own `SetCurrentSpeakable(null)` isn't mistaken for a local finish and echoed back.
- **Intro-finish sync (client read → host commits).** The intro dialog `Dialog_DesertClauseIntro` is read locally on each end; its CLOSE was previously synced host→client only, so a **client** reading its intro to the end never told the host. Now `DialogController.SetCurrentSpeakable` is tracked on both ends (`OnLocalDialogSpeakableChanged`): a non-null speaker matched to a registered, not-yet-started `RunsLocalIntroPresentation` boss (root-compare speaker.unit vs `GetHealthUnit`) records `_localIntroDialogKey`; a null speaker = the local player finished. The **host** reading its own intro is a no-op (its native chain runs dialog → sandstorm → `TriggerFight` and drives clients via the combat-entry broadcast, preserving its presentation). A **client** finishing sends a dialog-commit request with `CommitSource="intro-finish"`; the host's `HandleClientBossDialogCommitRequest` runs `CommitHostIntroDialog`. **Key fact (Log301):** the sandstorm + `TriggerFight` are animation-events gated on each end's OWN intro dialog reaching **natural** completion — `Graph.Stop(true)` merely *aborts* the dialog and does NOT execute the tree's end action that resumes the intro, so simply closing the host's dialog leaves its native intro stuck (host frozen forever, no sandstorm). So instead of hoping the native chain resumes, the host drives the two presentation beats **host-authoritatively**: (1) close its dialog (`TryFinalizeCurrentDialog` → `DialogClose` broadcast); (2) invoke `Anim_OnTriggerSandstorm` → the real `StartSandstorm` runs the sandstorm and releases the `Cinematic` lock at its tail (the host watches the ~8 s cutscene then unfreezes — native), and its prefix broadcasts the sandstorm to clients; (3) invoke `TriggerFight` → `StartBossPhases`, flowing through the combat-entry gate → broadcast so every client enters combat. (`StartBossPhases` only arms the phase timer and neither it nor the first `DivingAround`/`SetBossPike` calls `StopAllCoroutines`, so the `StartSandstorm` coroutine survives to release the lock.) Guard: the client sets `_applyingHostDialogClose` while finalizing a host-driven `DialogClose` so its own `SetCurrentSpeakable(null)` isn't mistaken for a local finish and echoed back.

- **Sandstorm presentation sync (host-authored, mirrored — Log301 Direction B).** The arena-edge sandstorm (`Anim_OnTriggerSandstorm` → `StartSandstorm`) natively fires only on the end whose local player reads the intro to the end, so the host in a client-first start (player elsewhere) and any client preempted by combat-entry never played it. It is now a host-authored discrete event, exactly like the dismount: a **prefix** on `Anim_OnTriggerSandstorm` (both ends) — `OnLocalBossSandstorm` — dedups per key (`_sandstormSeen`: `StartSandstorm` runs at most once per end, whether from the native anim-event or a mirror invoke) and, on the **host**, broadcasts `NetBossDiscreteEvent{EventName="Sandstorm"}` once; a **client** applies it via `TryApplyDiscreteEvent` → invokes `Anim_OnTriggerSandstorm` (its prefix no-ops if this end already played it). So every end plays the sandstorm + releases its own `Cinematic` lock. `CommitHostIntroDialog` schedules `TriggerFight` ~`sandstormLoopLength` (≈8 s) after the sandstorm (via a coroutine on the boss MonoBehaviour), matching the native beat — firing it immediately (Log302) preempted the sandstorm with combat-entry before it spread (client saw no arena-edge storm) and ran `SetBossPike`'s `AttachToPike` concurrently with the sandstorm's bossAnimator triggers (boss not visibly on the pike). The phase-1 pike-riding position drift below still applies once combat starts.
- **Pike-riding body visibility (cosmetic clone, both ends).** In co-op the boss body's "Sprite" renderer (a **MeshRenderer** quad, not a SpriteRenderer) flakily gets stuck `enabled=false` while riding the pike: `DesertPikeCarrier.AttachUnit` disables it on mount and `JumpTowards` re-enables it on pop-out, but that re-enable is racy in co-op and **either** end can lose it (Log309 host, Log311 client — an earlier good client run was luck). Forcing the real renderer back on fights the native burrow cycle and loses (Log310: re-enabled 127×, still off). Fix (verified Log313): never touch the real renderer — `EnsureBossPikeVisual` mirrors it onto an **owned** clone (`CoopBossPikeVisual`) parented under the real Sprite's transform, copying mesh + materials + `MaterialPropertyBlock` (or sprite/flip/color for a SpriteRenderer source) each frame; the animator keeps driving the disabled renderer's data, so the clone animates. It burrows with the body (the parent GameObject toggles active) and is only enabled while the real renderer is off, so an end where native works is untouched. Driven once per frame from a `DesertPikeCarrier.Update` postfix (`UpdateBossPikeVisual`), both ends.
- **Phase actions — dismount (F4 Stage 3):** the native dismount is `pikeCarrier.onJump`→`OnBossJump` / `onLand`→`OnBossLand`(`Terminator`) — these fire only on the host. **Root cause of "stuck on the mount" (Log284):** `DesertPikeCarrier.AttachUnit` parents the boss body to a mount point, sets `animator.enabled=false`, and its `Update()` zeroes every attached unit's `localPosition` each frame. Host postfixes the three callbacks → `OnHostBossPikeDismount` broadcasts `NetBossDiscreteEvent{EventName="BossJump"|"BossLand"}`. Since the boss is now a host-position-driven puppet, the client's `TryApplyDiscreteEvent` only **severs the boss from the pike's `attachedUnits`** (else the pike keeps zeroing it — Log286: snapped to world origin) + reparents off the mount + re-enables the animator + toggles `JumpingOffPike`; the **puppet drive owns the descent** (following the host down the real jump arc). An earlier hand-rolled descent fought the puppet drive and flung the boss ~130 m away (Log290).

- **Arena movement (F4-P2ARENA):** the P2+ fight walks the whole perimeter (`UpdatePerimeterMovement` — ~5 m/s along waypoints during the ToSniper/ToTerminator transitions, dragging the ring visual + jump points + damage sphere); the client's phase logic is suppressed so its perimeter froze at spawn (Log327). The host streams the live sphere centre as an `"ArenaPos"` discrete event (>0.2 m moved, ~10 Hz, radius stays 20 m); the client records it as a target and **glides** toward it at ~6 m/s each frame (`TrySetArenaCenter` delta-moves the perimeter root; a >25 m jump snaps), so the ring moves smoothly instead of teleporting per packet.
- **Riderless boss pike (F4-PIKELEFTOVER):** the boss's saddle-less pike (`spawnedBossPike`) is host-authoritative by **identity** (`IsRegisteredBossPikeCarrier`), not by "boss aboard" — after the phase-1 dismount the old aboard-test let the client's local perimeter jump the riderless pike (visible, diving in/out, Log329) while the host parked it underground. It now moves only via the host's replayed `PikeJump` events (phase 1, dismount, the phase-4 re-mount); parked in between. Saddled enemy pikes are unaffected.
- **Homing missiles (F4-MISSILE D1+D2):** the missile bases (`DesertMissileBase` sniper/terminator) run their own `Update`, so the client fired divergently (at its local player, in phases the host had left — Log332). D1: firing **windows** are host-authoritative — the client's own `StartMissiles` is blocked and the host's Start/Stop are mirrored (`MissileStart:`/`MissileStop:` events). Damage stays **each-end-local** (each end's real rockets target its own local player natively — `SpawnRealRocket` hard-codes `GameManager.PlayerUnit`). D2: every real rocket also spawns a **ghost visual** twin homing on each remote player's visual proxy (`ForEachRemotePlayerTransform`; both ends have proxies), with the ground marker + explosion, so everyone sees everyone hunted. Ghosts are damage-suppressed by instance id (a `CheckAndDamageUnit` prefix; the id + pooled marker are released in a `DestroyMissile` postfix), so pooled instances reused as real rockets damage normally and `AliveMissile`/phase gating is untouched. Pitfalls: `AutoPool.GetInstance` returns an `AutoPooledObject` **component** (not the GameObject), and `GameManager.Players` has no remote entries on the client (proxies are the right enumeration).
- **Full-screen barrage (F4-MISSILE D3):** `InitiateFirePattern` is broadcast (`MissilePattern:` + `patternDirection`, deferred one frame — the boss assigns the direction after the call) and the client replays the identical **native** pattern, which also natively pauses its homing for the barrage window (`useFireLinePattern`). D3b: the volley's safe lanes are rolled with the global RNG inside `FireLinePattern`, so each end diverged (Log338) — both ends reseed the RNG for that one call with a seed derived from the synced direction + the lockstep volley counter (state saved/restored around it), so the escape gaps match (Log339: identical per-volley seeds).
- **Saddled-pike + burrowing adds (F4-ADDS, Log340–348):** the boss throws minions (sticka/shoota) from **saddled enemy pikes** (`HellshrewPike2/3/4Seat`) and buries **diggas** that surface in-arena. These ride the RT3 dynamic-spawn manifest (`UnitSO.SpawnUnit` chokepoint), but six sequential bugs blocked them: **(1, a general RT3 bug)** `TickResolveDeferredHostOnly` sat *below* the `_gatedAdds.Count==0` early-return, so a boss whose client spawns **zero** local adds (Desert suppresses all phase spawns) never mirrored host-only adds — moved above the return, and diggas mirrored immediately. **(2)** the saddled pikes are owned by the `desertClausePerimeter` (via `GetPikeEnemyAsync`), not the boss, so both the capture (`NotePendingSpawn`) and attribution (`RecordSpawn`) points had to accept a perimeter owner (`TryGetEncounterKeyForDesertPerimeter`, ReferenceEquals against `perimeter`); filtering only at attribution is too late (Log342). **(3)** riders/pikes mirror-spawn at the host spawn position (origin) while the real ones are ±130 m out in the arena, so `TryQueueEnemyStateTarget`'s >100 m unsafe-apply gate rejected every snapshot (only death "applying anyway" bodies got through — the user's earlier "corpse" false report); RT3-bound entities are now exempt from that gate. **(4)** the boss's own saddle-less pike got double-driven (RT3 position-puppet *and* `PikeJump` replay) and the client perimeter's `UpdateJumping` teleports each pike to a local random jump point before `JumpTowards` — `HellshrewDesertClausePike` is now an `IsSpecialAdd` (never bound/mirrored, each end keeps its own) and the client blocks `UpdateJumping` wholesale. **(5)** snapshot-driving the pikes = a teleport storm (Log346 flicker + PhysX freeze, same class as Emperor §8.5); the saddled pike now replays the **native jump** like the boss pike — the host's `JumpTowards` postfix is generalized to broadcast `PikeJump:<hostIdx>` (RT3 host index, zero codec change) and the client resolves the mirrored pike by index and replays the real `JumpTowards`; the pike's snapshot **position** is exempt (`IsDesertEnemyPikeSnapshot`). **(6)** a mirrored pike isn't tamed the way the native attach tames it (physics off, movement disabled, `isLevelSpawning=false`), so a live rigidbody dragged by transform explodes through terrain depenetration and flings players (Log347) — `OnRuntimeMirrorSpawned` now replicates that taming, and the mirrored pike's local jump is blocked as defence-in-depth. Riders (sticka/shoota) are plain snapshot-driven puppets (smooth movers, no special handling). **Lesson:** mirroring a native unit means replicating the **state** it holds in the native flow (attach/physics/trigger), not just spawning it; snapshot-driving is wrong for teleport-style motion — replay the native method instead.
- **Downed-player rescue (F4-RESCUE):** the arena is a moving danger sphere, not a keep-in with a wall, so a **downed** player can end up abandoned outside the ring where teammates can't reach them → the fight softlocks. Every end runs `ArenaLockdownManager.TickSandstormDownedRescue`: while the fight is actually running (`TryGetActiveSandstormArenaSphere` — started, boss not terminally dead), a local player who is downed (`NetPlayerLifeManager.ShouldSuppressLocalPlayerControls`) **and** outside the sphere for 5 s is teleported in beside the group, reusing the existing `PullIn` apply (teleport + toast + dialog catch-up). The timer resets whenever the player is inside, revived, or the fight ends. Alive out-of-arena stragglers are deliberately left to walk in (the native fight drags everyone with the moving arena; co-op does not).

**Open / known divergences (registered per `BossSyncReuseMap.md` §5):**

- **Dialog model is a deliberate variant, not the Cousin RM-2b path.** `BossSyncReuseMap.md` §5.1's default is "the trigger opens the dialog; in-room players catch up; opt-outs respected." Desert instead has **each end run its own full intro chain** (the composite body must assemble via the intro on every end). It does not do RM-2b in-room *cutscene scoping* (an in-arena player who doesn't want the cutscene still gets it).
- **Intro-cutscene "native finish" sync — foundation cleaned, redo pending.** Goal (user spec): players read the intro at their own pace; when any player reads to the end, every other end still reading runs the **last step natively** (`OnDialogueFinished` → boss end presentation), instead of the current crude host→client `Graph.Stop` close. A first attempt (finish-event relay + per-frame `AcceptDialogOption` fast-forward) broke (Log297: host dialog re-opened, looped) because the **foundation was unstable**, not the sync: the client applied the boss start via TWO paths — `HostBossEncounterStart` → `TryApplyHostStart` AND the dialog commit → `TryApplyDialogCommit` (the host sends BOTH for a dialog boss: `BroadcastStartOnce` + `BroadcastDialogCommitOnce`), and `TryApplyDialogCommit` called `TryFinalizeCurrentDialog` (`Graph.Stop`) which **force-closed the intro dialog** before re-running `OnStartInteractWithBoss`; that force-close misfired the "dialog finished" detection and fast-forwarding fought the still-running intro chain. **Foundation fix (done):** (1) `_appliedStart` is now the shared start-applied gate on **both** client paths — `HandleHostBossDialogCommit` acquires it atomically and skips its intro/start apply if `HandleHostBossEncounterStart` already applied (and vice-versa), so a both-message dialog boss no longer double-applies; (2) `TryApplyDialogCommit` no longer finalizes the current dialog when `EnableFaithfulBossIntro` is on — the native intro chain plays its cutscene undisturbed. **Native-finish sync can now be redone on this stable base.** Interim: intro close stays `Graph.Stop` (works, not native, one-directional).
- **Phase-1 pike-riding position drift after combat-entry — DEFERRED.** Once the client enters combat (`TriggerFight` → `StartBossPhases` → `StartPhase(DivingAround)` → `InitDivingAround` → `SetBossPike`), the client attaches its boss to a **locally-spawned** `DesertPikeCarrier`, whose `Update()` zeroes the attached unit's `localPosition` + drives it along the perimeter each frame — fighting the host-driven puppet position (Log299: `maxErr≈132 m`, hundreds of snaps; client-first `avgErr≈13 m`). This is the same puppet-vs-pike conflict as the dismount (Log290), but now caused by the **native** phase machine rather than mod code. Not yet fixed. Two candidate directions: **(A, matches the committed puppet model)** suppress/sever the client's local pike-attach + perimeter movement so the puppet drive owns the position (note `SetBossPike` also does `animator.enabled=true` + `SetTrigger("AttachToPike")`, so it can't be skipped wholesale — sever after, à la the dismount `SeverBossFromPike`); **(B)** run the full native pike-riding on every end, keeping positions close via the shared seed (larger; risks gradual divergence).
- **Phase index not synced:** `FillBossState` carries only health → `BossState` never sets the phase index → the client boss stays at phase 0 (cosmetic; combat is host-driven and suppressed anyway).

---

## 10. Terrorbaum (Act_02_Forest) — TB

Terrorbaum (`TerrorbaumBossFightHelper : BossFightHelper`) is a non-dialog boss (`TriggerFight`), so it starts through the generic `BossFightHelperAdapter` handshake. The boss body already syncs cleanly: it classifies as `CombatEnemy`, enters the client puppet system, is host-position-driven, and its attacks mirror to the client (Log350: `Terrorbaum (Offensive)` puppet `avgErr=0.00`). Two fixes make the fight playable:

- **TB-1 — craw (add) flicker.** Terrorbaum spawns "craws" (`CrawWatcher`, `Unit_Enemy_Craw_Watcher_Base`) via `crawUnitId.SpawnUnitAsync(this)` — a boss-mono add, so they already mirror + bind through the RT3 dynamic-spawn pipeline. But `CrawWatcher` is a **standalone `MonoBehaviour` flight controller** (own `FixedUpdate`, `rb.isKinematic=false; useGravity=false`, flies the transform to its own local targets) — NOT the `NavMeshAgent`/`CustomRichAI`/`behaviourTree` that `ApplyClientEnemyPuppetMode` disables. So on the client the mirrored craw's local flight AI kept flying it ~13 m off every frame (Log350 drift diagnosis: `navMeshStillEnabled=0 aiCanMove=0 rvoEnabled=0` yet `avgErr≈7–13 m`, hundreds of repeated snap corrections) while host snapshots dragged it back → position flicker + physics thrash → heavy client lag. Fix: puppet mode now also disables known custom locomotion controllers by type name (`ClientPuppetExtraLocomotionControllers = { "CrawWatcher" }`, `NetGameplayProbeManager.cs`) so the mirror is a pure host-driven puppet (the flap animation persists — `StartFlying` already ran in `Start`; attacks still come from the host-authoritative root-replay). That alone killed the flicker (Log351: craw `avgErr` 7–13 m → ~0.4 m) but a **residual per-craw stutter** remained, clearing only when the craws died. Root cause: the craw's transform-driven kinematic **physics collider** (the non-trigger body collider `CrawWatcher` used for its own collisions) keeps generating PhysX contacts against the arena's dynamic props (crates/barrels/bricks) every frame — kinematic-alone did not remove it (`MakeClientPuppetsKinematic` is already on), exactly the Emperor-worm finding. Fix: `DisableFlyerPuppetPhysicsCollider` disables **only** that collider (non-trigger, no `Hitmesh` on its own GameObject), **keeping every damage hitbox** — the client must still be able to shoot craws (a hard requirement), and a projectile registers a hit by finding a `Hitmesh` on the collider it overlaps (`other.TryGetComponent<Hitmesh>`), so any `Hitmesh`-bearing collider stays enabled; trigger colliders (which never churn contacts) are left alone too. Once per object, guarded by `_visualOnlyFlyerCollidersDisabled`. Same lesson as the Desert enemy-pike taming (§9 F4-ADDS): mirroring a native unit means neutralizing every state/behaviour the native flow holds, not just the common AI stack.
- **TB-2 — arena-entry teleport through the floor.** Terrorbaum uses the generic LD-2 gate lockdown (`ArenaLockdownManager`), which teleported a pulled-in player to the **seal trigger's raw pivot** (`arenaPos + up*0.5`). That pivot Y is often below the standing floor (Log350: client landed at `(-103.3,-3.6,-1.0)`) → the player fell through the map. Fix: `TeleportIntoArena` now resolves a valid-floor destination via `ResolveGateTeleportDest` — land **beside a teammate already in the arena** (their feet = valid floor; the extracted `ResolveArenaEntryTarget` reused by the sandstorm pull, `includeLocal:false` here since the local player is the one being pulled in), else a downward `Physics.Raycast` to ground, else the old `arenaPos + up*0.5`. Generic to every LD-2 arena, not just Terrorbaum.
- **TB-PUPPET — client boss must not run its own mechanics (host-authority, Log356).** The boss body is a `CombatEnemy` puppet (host-position-driven), but the **fight mechanics** run from two of the boss's own `MonoBehaviour` drivers that the puppet system never touches: `BossFightHelper` itself (its `Update`/`FixedUpdate` tick `UpdatePhases` = the attacks; enabled by `TriggerFight`'s `base.enabled=true`) and `bossPhases` (`BossPhases`, its own `Update` drives phase transitions; enabled by `StartBossPhases`). When the client applies the host start it invokes `TriggerFight`, which **enables both on the client** → the client boss runs its own attacks + phase transitions locally, diverging from the host and violating host-authority (the user also saw a client that had raced ahead / rejoined run the boss with no host driving it, then the boss vanished on the seed-reconcile reload). Fix: Terrorbaum's client copy is a **pure puppet** — `ClientBossIsPurePuppet` (true only for `TerrorbaumBossFightHelper`; Desert/Witch/Emperor/Cousin keep their bespoke client sync) + `SuppressClientMechanics` disables both drivers, re-asserted every frame in `NetBossEncounterManager.Tick` (client only) so the boss stays inert however it got enabled (host-start apply, race-ahead, rejoin). The host is untouched and remains the sole authority; the client mirrors position + animation (EA attack-animation mirror) + host-authoritative health/death. `[BossPuppet] suppressed client boss mechanics` logs the one-time transition. (Consequence to watch: any boss action that was only ever produced by the client's *local* `UpdatePhases` — effect spawns not otherwise mirrored — now needs host-side mirroring; craws already ride RT3.)

### TB-D — opening-dialogue sync (host-authoritative mirror)

Terrorbaum's fight begins when a player interacts with the boss (`Npc.Interact(bossNPC)`) and opens the **boss's own dialog** (`bossNPC.dialog`, a multiple-choice tree); picking the fight option runs the already-synced `TriggerFight`. The decompile confirms Terrorbaum only overrides `TriggerFight` (no `OnStartInteractWithBoss`/`StartFight`) — the dialog→fight link lives in the `Forest5` dialogue graph (data), so it can't be bolted onto Cousin's `StartFight` gate. Previously both ends opened/read/picked that dialog **independently** ("两人都看到但不同步"): the boss didn't appear together and dialogs didn't close together.

**Root cause of the first (failed) attempt (Log354):** a boss encounter is only registered in `NetBossEncounterManager` at its **start entrypoint** (`TriggerFight`), which for Terrorbaum fires *after* the dialog is opened and the fight option is picked. So every hook that keyed off the registry at `Npc.Interact` time (open broadcast, client request) scanned an empty registry and no-op'd. Fix: **register the encounter the moment the dialog opens** (`TryResolveAndRegisterBossOwnDialog`: from the interacted NPC, scan its transform-root subtree for the owning helper; with no NPC handle — host reacting to a client request — scene-scan the single active such boss; register via `TryGetEncounterKeyForBoss`).

- **`BroadcastsBossOwnDialog(component)`** (adapter): true when the boss exposes `bossNPC.dialog` but is **not** a Desert local-intro boss (`!HasMethod("OnStartInteractWithBoss")`) — Terrorbaum → true. This single gate distinguishes TB-D from Desert (whose intro runs each-end-locally) so Desert is never regressed.
- **Open — peer broadcast, no block.** Whoever interacts keeps their own **navigable** dialog (never blocked — so the interactor can always pick the fight option; no soft-lock risk). The host broadcasts `Dialog:<id>` (via `OnHostBossDialogInteract`, now registering + firing even pre-fight for a `BroadcastsBossOwnDialog` boss); a **client** interacting instead sends an `"open"` request (`OnLocalBossOwnDialogInteract` → `SendClientBossDialogCommitRequest` source `"open"`) which the host applies by opening its own copy (`TryOpenBossOwnDialog` → `bossNPC.Interact(null)`) → its interact postfix broadcasts. Each *other* end opens a **mirror** (`TryApplyDiscreteEvent`, `id == BossOwnDialogId` → `Interact(null)`); `_bossOwnDialogLocallyOpen` dedups the originator's echo, and `_applyingHostDialogOpen` stops the mirror's own interact from re-requesting.
- **Close — on fight-start, both ends.** Terrorbaum's fight begins when someone picks the fight option (closing the picker's dialog). `CloseBossOwnDialogOnFightStart` runs at every fight-start chokepoint — host `BroadcastStartOnce`, host `HandleClientBossStartRequest` (client-picked → host started but the host's own dialog never closed), and client `HandleHostBossEncounterStart` — and finalizes any still-open opening dialog (`TryFinalizeCurrentDialog`). So no matter who picks, every end's dialog closes together and nobody is left staring at a dialog after combat begins. The host's natural close still also broadcasts `DialogClose` (redundant, idempotent). An independent **back-out** (closing the dialog without fighting) forwards nothing — the opening-dialog close is driven solely by the fight-start.
- **Room-scoping (out-of-arena players don't get the dialog).** The mirror is gated on `ArenaLockdownManager.IsLocalPlayerInActiveArena()` (reuses the LD gate-lockdown's host-authoritative in-room set — populated for Terrorbaum, Log355 `in-room=[host,client-1]`). A player across the map (being pulled into the arena later) does **not** open the dialog. Because the host-broadcast membership lags the local gate crossing by ~1 RTT, a skipped mirror is **stashed and retried in `Tick`** (`_pendingMirrorKey`) — an in-arena player whose membership just hasn't propagated gets the dialog via catch-up the moment they're in-room, while a genuinely far player never does. The host's own force-open (client-first trigger) is likewise gated: an out-of-arena host **broadcasts** the open (so in-arena clients mirror) via `BroadcastHostBossOwnDialogOpen(hostHasItOpen:false)` but does **not** pop the dialog on itself (`TryGetActiveMidFightDialogId` returns the sentinel without needing the live graph, so the broadcast works without the host opening).
- **Any in-arena player triggers (Log355).** Both the native dialog and a force-opened/mirror copy (`Interact(null)`) are fully navigable, so any in-arena player — host or client — can pick the fight option; the commit is host-authoritative via the existing `TriggerFight` block/request. An out-of-arena player is pulled in by the lockdown and enters combat via the fight-start broadcast (the boss "appears" for everyone).
- **Slice boundary.** Choice *navigation* is per-end (each reads its own copy). TB-D2 (later) can add host-authoritative option-index forwarding if lock-step navigation is wanted.

### TB-DMG / TB-TGT / TB-DLG / TB-ANIM — playable co-op fight (Log358 fixes)

Log358 (client-first start, host arriving late) broke down into four distinct defects, all fixed host-authoritatively:

- **TB-DMG — client damage never landed.** The Terrorbaum body is **permanently invulnerable** (`SetBossVars` → `bossUnit.SetInvulnerable(true)`); the only native damage path is `OnEyeHit` — the projectile dispatch fires `onHitboxHit` **after** a first (always-rejected) `ReceiveDamage`, and `OnEyeHit` (gated `fightStarted && !IsTransitioning && part==Eye`) lifts the invulnerability *just around* a second `ReceiveDamage`. Our 5.4-F reflect called a plain `ReceiveDamage` on the host → rejected by the standing invulnerability, every time (Log358: 123 hits, all `vanilla-false(invuln)`, hp frozen). Fix: on the client, `ResolveHitTargetRole` (Terrorbaum) returns `"eye"` only when the local unit is momentarily **not** invulnerable — i.e. exactly inside the local `OnEyeHit` window (so the native Eye-hitbox + fight-started gates are inherited for free) — and the sentinel `"body"` otherwise (swallowed with **no** host request: vanilla rejects those locally anyway, and letting them fall through would per-pellet spam the ordinary roster hit path). On the host, the new adapter hook `TryApplyHostBossHit` (default = old plain reflect) replicates the native window for `"eye"`: gate `fightStarted && !IsTransitioning`, `SetInvulnerable(false)` → real `ReceiveDamage` → `SetInvulnerable(true)` in a finally. Damage feeds the native `onDamageRecieved` → charge/absorb mechanics advance from client hits too.
- **TB-TGT — a remotely-started fight never surfaced on the host.** `TriggerFight` digs the tree underground (`TransitionTo(1)` → `StartDigging`), and the behaviour-tree erupt node hard-requires `aiAgent.lastTarget != null` (`U_TerrorbaumPhase2_Sequencer_0`). The tree only acquires a target through its own LOS perception — impossible from underground — so when the **client** picked the fight option while the host player was elsewhere, the host tree dug and stayed invisible forever ("boss 不在"), leaving its interact collider behind. Fix: `TickHostPurePuppetBossTarget` (host, ~4 Hz) — for a started pure-puppet boss whose `AiAgent.target`/`lastTarget` is missing or unattackable (downed / out-of-arena, reusing the Cousin-arm `IsTargetAttackable` rules), point both at the **nearest attackable player unit** (host player or client ghost via `GatherPlayerUnits`). Native perception is never overridden while its target is valid. `[BossTarget]`.
- **TB-DLG — the opening dialogue re-opened mid-fight.** Vanilla never guards a post-start `Npc.Interact` (in single-player the tree instantly moves off into combat), so the late-arriving host walked up to the underground boss's collider and got the opening dialog again — and TB-D dutifully mirrored it to the in-arena client (Log358). Fix: `ShouldBlockStartedBossOwnDialog` blocks the interact at the `Npc.Interact` prefix on **every** end once the fight has started, and `OnHostBossDialogInteract` refuses to broadcast a started own-dialog open (belt-and-braces for other open paths).
- **TB-ANIM — the two ends showed different trees.** The pure-puppet client (TB-PUPPET) mirrors *position* but its animator never runs the dig/erupt mutators, so the client showed a standing tree while the host tree was underground — the two ends disagreed about whether the boss existed at all. Fix: host postfixes on the four visibility-state mutators — `StartDigging`, `OnStartEruptAttack(pos, trigger)`, `OnEruptStartAoe`, `OnRootStart` — broadcast discrete events (`TerrorDig` / `TerrorErupt:<trigger>`+pos / `TerrorEruptAoe` / `TerrorRoot`, `[BossAnimSync]`); the client replays the **same native mutator** (animator bools/triggers + flags + the erupt reposition) via `TryApplyDiscreteEvent`. Attack damage stays host-authoritative: the replayed animations' local damage anim-events (`Anim_OnDoSlamDamage` / `Anim_OnDoEruptDamage` / `Anim_OnDoEruptSlamDamage`) are prefix-blocked on the client (`ShouldBlockClientPurePuppetBossAnimDamage`) — the host's slam already hits the ghost proxy and is forwarded to the owning client, so running the local one too would double-hit.
- Cosmetic: `FillBossState` now reports the real `fightStarted` in `[BossState]` lines (was hardcoded-default `False` even mid-fight — a log red herring in Log358).

### TB-MECH — in-fight mechanic sync (root spin + sky spikes)

Both mechanics live in the helper's (client-suppressed) Update loop, so the pure-puppet client showed static vines and no falling spikes (`TerrorbaumMechanicSync.cs`):

- **TB-ROOT — rotating thorn vines.** `UpdateRotatingRoot` rotates `rootPoints` per-frame with random speed ramps/reversals (`RootReverseRotationTimer` re-rolls on every read — not deterministically replicable). Host streams the Y angle (~8 Hz while `isDoingRoot`, only when moved >0.5°, `TerrorRootAngle` with the angle in Position.x); client eases its `rootPoints` toward it (`LerpAngle`, ~0.1 s chase). Vine damage is natively **per-end local** — `LineRendererCollision`'s Player path only tests the LOCAL player — so the correctly-angled client vine damages the client player exactly like vanilla (no forward, no double-hit). The random small-root lashes (`UpdateAttackRoot`) are host-rolled: a functional prefix re-implements the 5-line native roll on the host and broadcasts the chosen index (`TerrorRootLash:<i>`); the client SetTriggers the same root, so the dodge cue matches. `StopRoot` is mirrored (`TerrorRootStop` → client invokes the native `StopRoot(false)`) so vines retract together.
- **TB-AOE — sky spikes with landing previews.** Native chain: absorbed player bullets are fired UP (`Anim_OnSpawnProjectiles` → `SpawnProjectiles`), caught by the cloud's `TerrorbaumSkyTrigger` (IAttackReceiver) → `OnShootBulletFromSky` re-fires each DOWN from cloud height at a random spot (or forced onto a random entry of `GameManager.Players` — ghosts included, so remote players are already targeted) and drops a pooled `bulletDetectMarker` at the raycast ground point = the landing preview. None of it runs on the client (empty absorb queue + suppressed drivers). Host prefix peeks the sky-queue head (speed = native clamp(|velocity|,5,30), type/caliber/effect/vfx/damageType); postfix diffs `projectileMarkerRefs` for the new marker's ground pos and broadcasts `TerrorSky:<speed>:<ids>` + pos. Client spawns the same pooled marker at the ground point (auto-released after the flight time) and a **zero-damage** visual spike falling onto it (`PlayerWeaponFireManager.FireVisualStraight`, WS-style empty damageComps). Damage stays host-authoritative — the host's real spikes hit remote players' ghost proxies (Hitmesh-carrying, A3 forward); a damaging client replica would double-hit.
- Not mirrored (cosmetic, note for later): the upward volley visuals; the between-volley `AoeAttack` pump animation (host-Update-driven trigger).

**Known residuals (observe in the next run):** the root-spin attack's `rootDamageTriggers` (`LineRendererCollision`) still deal per-end local damage during a replayed `TerrorRoot` (possible double-hit for the client player in that phase); `ChargeAtPlayer` distance checks still read the base `playerUnit` (host player) rather than the AiAgent target; wander/charge/slam *animations* between the four mirrored states ride the generic EA attack mirror.

**Log359 verification round:** TB-TGT and TB-DLG confirmed working (host tree aimed at the client ghost, dug + erupted at it; no repeat dialog). Two follow-ups:

- **TB-DMG v2 — the client eye window never opened (hitRecv=0).** `StartBossPhases` → `SetTransitionVars` sets `bossPhases.isTransitioning = true`, and only `BossPhases.Update` (the driver TB-PUPPET disables) ever clears it — so on the client `IsTransitioning` stuck TRUE forever, permanently closing the native `OnEyeHit` gate (`!IsTransitioning`): the invulnerability was never lifted locally, every pellet resolved as `"body"` and was swallowed, and the host received zero hit requests. Fix: `SuppressClientMechanics` also clears `isTransitioning` (re-asserted every Tick like the drivers; on the client the flag is only read by `OnEyeHit`, and the host re-checks its own live `IsTransitioning` on apply). The `"body"` swallow also logs a throttled counter now (`[BossDamage] client swallowed N outside-window body hit(s)`) so a closed eye window is visible in logs instead of silent.
- **Boss/craws reported invisible on the HOST while fully simulated** (they existed, moved, erupted, died to client shots; client puppet drift vs host ≈ 0.00 — both ends agree on positions, so it is a *render/animator-state* asymmetry, not simulation). Room-LOD staleness was ruled out (`Unit.CalculateCurrentRoom` re-runs after >1 m of movement, so a teleported-in player re-resolves its room within a frame; `DistanceLODManager.SetLockedStates` is flashback-only). The concrete hiding mechanism is not derivable from the existing logs → `[BossVis]` probe (both ends, 5 s cadence during a started pure-puppet fight): GO active, position, renderer counts (enabled / `forceRenderingOff` / `isVisible`), animator enabled/cullingMode, boss room vs player room + distance. Diffing host vs client `[BossVis]` lines next run pins the mechanism.

### TB-INTRO — entrance-animation sync (Log360)

`[BossVis]` (Log360) closed the invisibility case: on the host every renderer was enabled (`forceOff=0`, animator `AlwaysAnimate`), positions matched the client — the tree was rendering the whole time, **stuck in its pre-entrance pose (buried under the soil)**. The entrance (Terrorbaum bursting out of the ground) is played by the room-entry `PlayerTrigger_StartEvent`'s persistent `Animator.SetTrigger` — fired only by the **local** player crossing it. The client crossed → its tree burst out; the host never crossed → its tree stayed buried through the whole remotely-started fight; when the host finally walked in, its own (still-unconsumed, `onlyOnce`) trigger fired and the entrance replayed mid-fight.

Fix — mirror the room-event trigger, Cousin-style ("whoever enters, the boss appears for everyone"), reusing the LD-1 GateSync channel (`NetGateState` v2 gains `Kind`: 0 = gate, 1 = room-event trigger; same message id / relay path):

- **Capture (either end):** `PlayerTrigger.Trigger` prefix snapshots `hasBeenTriggered` (`__state`); the postfix broadcasts only a **fresh** fire (false→true — refires early-return inside the native method) of an **appear-class** trigger (persistent calls include `Animator."SetTrigger"` — `HasAppearAnimatorPersistent`). `[BossIntroSync] capture`.
- **Mirror (other ends):** find the local `PlayerTrigger` at the broadcast position (deterministic gen) and invoke the real `Trigger(localPlayerGO)` under a reentry guard — full native effect: the entrance animation plays, the `MetalGate.Close` calls are idempotent alongside GateSync, and crucially the trigger's own `hasBeenTriggered` is **consumed**, so this end walking in later does not replay the entrance. Already-fired ends no-op. `[BossIntroSync] mirror`.
- **Started gate (late joiner):** `ShouldBlockBossAppearTrigger` — once a registered boss encounter within 60 m has started, a local appear-trigger fire is blocked at the prefix (a player arriving mid-fight joins the running fight; gates are handled by the arena lockdown). This also covers a session joiner whose freshly-loaded scene has an unconsumed trigger. `[BossIntroSync] blocked mid-fight appear trigger`.
- **Catch-up semantics (per the flow design):** pre-fight/dialog state → a newly-in-room player's own trigger fires natively (entrance + TB-D dialog catch-up), and the broadcast no-ops on ends that already played it; started state → no entrance, mechanics only.
- **TB-DLG2 (Log362) — the opening dialogue is AUTO-OPENED by the entrance animation.** The dialog is not a manual player interact: an animation event at the end of the soil-burst entrance calls `Npc.Interact` on whichever end plays it — so mirroring the entrance (TB-INTRO) also auto-opened the dialog on ends whose player is across the map (Log362: both the out-of-room client in run 1 and the out-of-room host in run 2 got it from their own local anim event). Room-scoped at the source: `ShouldBlockOutOfRoomBossOwnDialog` (Npc.Interact prefix) blocks a PRE-fight own-dialog interact while the local player is out of the active arena (fail-open when no arena known; never blocks the mirror/catch-up apply — `_applyingHostDialogOpen`). Catch-up on pull-in stays intact: the client path already stashes the host OPEN broadcast in the pending mirror; the host's broadcast-only branch now stashes it too (the discrete broadcast doesn't loop back to the host), so an out-of-room player pulled in pre-fight gets entrance + dialog (中途进房可补), and an AFK player who declines never does — Cousin-style.
- **TB-DLG2b (Log363) — walk-in catch-up: the consumed seal trigger starved the membership.** The TB-INTRO mirror consumes every end's seal `PlayerTrigger` (by design, so the entrance never replays) — but the arena membership feed hangs off that same trigger's `Trigger()`, so a player later WALKING in never re-fires it: they never enter the in-room set and the pre-fight dialog catch-up (which waits on membership) never runs (pull-in via the popup worked, because the teleport path reports membership directly). Fix: the LD-2e `ArenaDoorwaySensor` (a pure traversal event on the doorway, independent of `hasBeenTriggered`) now reports in-room on every flip to INSIDE (`OnLocalDoorwayTraversed` → `ReportLocalInRoom`, idempotent — the same call the teleport path uses). Plus a receive-side robustness fix: a pre-fight `Dialog:boss-open` arriving before this end registered the encounter is no longer dropped (`HandleHostBossDiscreteEvent` resolves + registers the own-dialog boss from the scene), so the deferred catch-up is always stashed.
- **Log361 regression fix — the mirror must not feed the membership.** The mirrored `Trigger()` invoke runs our own `PlayerTrigger` postfix, which fed `ArenaLockdownManager.OnLocalTriggerFired` — the far-away host got marked **in-room** by the mirror (Log361: `in-room += host … t0 by host` as a side effect of `[BossIntroSync] mirror`), so the lockdown never offered it the pull-in popup and the opening dialogue was wrongly mirrored to it (the TB-D room-scope gate read the corrupted set). Fixed in `MetalGatePatches.PlayerTrigger_Trigger_Post`: skip the membership/door-capture feed while `GateSyncManager.IsApplyingTriggerMirror` — a mirror is not a local crossing. Verified working same run: eye damage (`role=eye` applies), entrance mirror, no repeat dialog.
